# 变更摘要

- 2026-09-08：MODEL-DYNAMIC-DISPATCH-001 已整合动态选档与显式派发合同、只读角色、正式说明及直接测试；v5独立终审和完整420 passed / 11 subtests passed已通过，进入本地提交。

- 2026-09-08：MODEL-ORCHESTRATOR-SELECTION-001 取消项目对主会话模型和推理强度的固定配置；主会话由用户选择，既有合同仅约束其对子任务的分级、派发、失败关闭和收口。Luna/Terra worker 与 Terra reviewer 映射未变；全量418 passed / 11 subtests passed，R6 独立终审 approved，本地提交 `1b64734`，未推送。旧行为候选 manifest 保持原指纹，不再作为当前合同文件缓存。

- 2026-09-07：FLOW-STATUS-REVIEW-001完成五项状态与评审流程优化，实现本地提交27fe2cd，未推送。分离产品/本批与阶段/活动，补需求证据矩阵、无默认评分与复审追溯、8场景真实回复及负向/新鲜性回归。独立首轮Important经整改复审关闭，全量416 passed / 11 subtests passed；只代表本批，不代表其他软件验收。

- 2026-09-03：`TECHNICAL-ASSESSMENT-RESPONSE-001` 已关闭。技术评估回复统一按“需求 → 现象 → 代码 → 原因 → 影响 → 建议”说明问题，共享回写合同新增 `technical_assessment_summary`，humanizer 保留评估时点和修复状态。独立复审 `approved / 100 / C0-I0-M0`，完整 pytest `358 passed / 11 subtests passed`，38/38 Skill validator 和响应黑盒 19/19 通过。

- 2026-09-01：`SOFTWARE-ENGINEERING-SKILL-AUDIT-001` 已关闭。中文语言、Skill 设计、软件工程、项目管理、沟通均覆盖 38/38，形成 `190/190` 逐项评分和 `85.6/100` 系统矩阵。审计为 `C0 / 原始 I27 / 原始 M18`、`changes_recommended`；审计提交 `4929f58`，没有修改任何 Skill 或测试，整改尚未实施。

- 2026-09-01：`HUMAN-RESPONSE-CONTRACT-003` 已关闭。补齐修复位置五字段与代码形状合同：代码写入禁止局部函数定义和无独立职责的单调用点公共 helper，实际实现回写 `code_shape_check`；用户指南保持候选修订。实现提交 `91a3aea`，定向 `30 passed`，独立评审 `approved / C0-I0-M0`。

- 2026-09-01：`SOFTWARE-LIFECYCLE-GOVERNANCE-001` 统一 Skill-first 架构、接口、前端、UI、数据、记忆与工作流正式事实，删除退役平台机器附件，补齐 12 阶段生命周期矩阵和语义反转防回退测试；同一 reviewer 终审 `approved / 97 / C0-I0-M0`。实现提交 `f9654c6` 的干净克隆 `290 passed / 4 subtests passed`，Ruff、38/38 Skill、解析、案例目录、diff 与 Git clean 全绿。

- 2026-09-01：`HUMAN-RESPONSE-CONTRACT-002` 完成按工作类型的人类可读会话合同：开发只按已批准 WBS/TaskCard/ledger 报告，测试提供完整计数、覆盖范围和失败明细，Bug/修复提供根因事实与三分支 TaskCard 决策；用户指南保留为待正式批准/发布候选。独立终审 `approved / C0-I0-M0`，实现提交 `91460c2`，工作项关闭。

- 2026-08-24：完成并关闭 `MODEL-DISPATCH-RUNTIME-001`。以 Codex 原生 subagent 实现 Sol 唯一主控、Luna 简单低风险 worker、Terra 标准/复杂 worker 与 Terra/high 只读 reviewer；严格派发合同对 workflow/write policy 错配或重叠失败关闭。治理测试 Green `9 passed`，同 reviewer 终审 `approved / 96 / C0-I0-M0`；实现提交 `b270ae4` 干净克隆完整 pytest `273 passed`，Ruff、38/38 Skill validator、解析、diff 与 Git clean 全绿。

- 2026-08-23：完成并关闭 `SKILL-FULL-OPTIMIZATION-001`。动态审计并独立评分 38 个 Skill，最终有证据优化 24 个、保留 14 个；关闭失败误报、用户配置污染、进程树残留、资源不可移植、项目状态 owner 漂移和无条件人工 Gate 等 15 个 P0 finding。同 reviewer 终审 `approved / 95.0 / C0-I0-M0`，完整 pytest `262 passed / 4 subtests passed`，实现提交 `9f7e251`。

- 2026-08-23：完成并关闭 `SKILL-COMPLETENESS-P0-001` 五项 P0：同步脚本只接受含 `SKILL.md` 的目录；三个复用 Skill 统一 `doc-map.md` 回源；美术候选跨会话保留；正式设计和项目配置移除旧数量、运行时与失效路径；测试从 SHA 快照改为行为和结构不变量。同 reviewer 独立终审 `approved / 100 / C0-I0-M0`；实现提交 `fd908b4`、状态同步 `e9f9d97` 的当前 HEAD 干净克隆全绿。

- 2026-07-23：完成 `TASK-IMPLEMENT-003-P001` 项目快照可读性与状态修复。旧 ledger 无事件 ID 时改为生成确定性派生 ID，恢复被漏掉的评审/批准/完成状态；总览改为六列中文敏捷看板，卡片只显示中文标题，每列最近 10 条并可展开“更多”，详情使用独立页面和返回按钮；补齐时区、父子任务推断及 SQLite 增量外键安全回退。第五轮自动独立终审 `approved / 96 / C0-I0-M0`，相关回归 102 passed，Ruff、mypy 与 Chromium 桌面/移动端检查通过。
- 2026-07-05：完成 Superpowers 流程集成状态旧口径清理。`SF-SP-001` 到 `SF-SP-007` 已提交为 `efac627`，连同 `e048784`、`9296f58`、`3b0e9a5` 形成 `SF-SP-001` 到 `SF-SP-010` 的本地闭环；`superpowers-workflow-integration-plan.md`、closeout report、`AGENTS.md`、`runtime-brief.md` 和 `agent-session.md` 已同步更新，不再把中心脚本作为新 workflow 主控入口。
- 2026-07-05：用户确认 `SF-SP-001/002/003/004` 独立 review / 复审结果通过，并授权提交相关任务。四项已写入 `human_approved`；本轮提交范围收敛为 Superpowers `SF-SP-001` 到 `SF-SP-007` 的 workflow skill、references、tests、work item 证据和必要 memory 同步。
- 2026-07-05：完成 Superpowers 剩余收尾 review loop。已创建并整合 `SF-SP-001/002/003/004` 的独立 review / 复审子任务结果：`SF-SP-001 approved / 94`、`SF-SP-002 approved / 92`、`SF-SP-003 approved / 93`、`SF-SP-004 approved / 95`。修复项包括 `project-memory` 不能输出下一步 skill、memory ledger 示例从 `next_skill` 改为 `next_status` / `next_required_action`、`SF-SP-003` downstream reference 路径存在性、helper code 迁移结论和结构测试。四项均已写入 review 文件、evidence、work item ledger 和 review-ledger，当前停在 `pending_human_confirmation`。
- 2026-07-05：完成 Superpowers 流程集成收尾校准。正式计划不再停留在“当前只剩 `SF-SP-010`”的过期叙事；`SF-SP-010` 已提交为 `3b0e9a5`。新增 `.factory/workitems/SF-SP-010/reports/superpowers-workflow-integration-closeout-report.md`，按 ledger、review ledger 和本地 commit 区分本地闭环完成、功能评审通过但提交未闭环、未完成或部分完成三类状态。当前结论是本地闭环 `3 / 10`，不能声明整体计划已完成。
- 2026-07-05：继续补 Superpowers 收尾缺口。新增 `SF-SP-001` work item ledger、coverage evidence、coverage closure report 和 review brief，将“拆除脚本主控设计”记录为已被后续 skill-native 流程任务覆盖但仍需真实独立 review。同步收紧收尾报告：`SF-SP-002/003/004` 的旧 task review 均含单线程 fallback，不能按最新独立性硬门视为最终独立评审。
- 2026-07-05：用户确认 `SF-SP-010` 独立复审结果，状态记为 `human_approved`。当前未自动提交；只有用户明确要求提交 / commit 时才进入 `gitcommitzh` 提交流程。
- 2026-07-05：`SF-SP-010` iteration-1 独立复审通过。复审 reviewer `codex-sf-sp-010-rereviewer-20260705` 给出 `approved / 95`，确认旧下一步文案、PM 导航目标检查和 JSONL evidence 一致性均已修复；当前停在 `pending_human_confirmation`。
- 2026-07-05：修复 `SF-SP-010` iteration-1 独立 review 反馈。独立 reviewer `codex-sf-sp-010-independent-reviewer-20260705` 给出 `changes_requested / 82`，指出 Superpowers 方案仍有旧下一步文案、PM 控制面导航目标未被测试固定、JSONL evidence 使用占位命令且计数不一致；已修复并进入 `ready_for_re_review`。
- 2026-07-05：`SF-SP-009` 已提交为 `9296f58`，`SF-SP-010` 已进入文档、导航、memory 同步开发。当前收口范围是 Superpowers 方案当前进展、开发过程导航、根文档导航、`.factory/memory/doc-map.md`、summary 和测试报告入口；新增结构测试先复现导航缺口与 stale summary 缺口。
- 2026-07-05：进入并完成 `SF-SP-009` 黑盒流程 eval iteration-1 实现。新增 `skills/using-shanforge/references/black-box-flow-eval.md`，在 `using-shanforge` 中加入 `SF-SP-009` / 黑盒流程评估入口，并新增 `tests/test_black_box_workflow_eval.py` 固定六类场景、评分门、失败门和无脚本 gate 回退。场景覆盖一句话需求、bug 修复、review 反馈、压缩恢复、完成声明和自评隔离；当前状态为 `ready_for_review`，下一步是真实独立 review。
- 2026-07-05：修复 `SF-SP-009` iteration-1 独立 review 反馈。独立 reviewer 给出 `changes_requested / 84`，要求补评分公式 / 归一化并增强结构测试；已补黑盒 eval 评分公式、Actual / Max / Normalized score 证据字段、每场景 critical assertion 可评分断言，并将正式计划任务表统一为 6 类场景。当前状态 `ready_for_re_review`。
- 2026-07-05：`SF-SP-009` iteration-1 独立复审通过。复审 reviewer `codex-sf-sp-009-rereviewer-20260705` 给出 `approved / 95`，确认评分公式、结构测试和 6 类场景口径均已修复；当前停在 `pending_human_confirmation`。
- 2026-07-05：用户确认 `SF-SP-009` 独立复审结果并授权进入提交流程，提交后直接开始 `SF-SP-010` 开发。当前只允许提交 `SF-SP-009` 范围。
- 2026-07-05：根据用户指出的执行纪律问题，为 `SF-SP-008` 追加 skill-native 收尾门并完成独立 review。已撤销中心脚本 gate 方案，不新增 `factory-dispatch` / `scripts/factory-*` workflow gate；`using-shanforge` / `gitcommitzh` 在完成声明、提交或关闭 work item 前必须重读最新 work item ledger 和 review ledger，若仍有阻塞状态或 `next_required_action`，只能报告阻塞 gate 和下一步动作。根据范围复审反馈，混合 `.factory/memory/` 文件只能暂存当前任务 hunk，无法拆分时停止并拆成独立提交。主 review `approved / 94`，范围复审 `approved / 94`，已获用户 `human_approved`。
- 2026-07-05：完成 `SF-SP-008` 首版 PR 闭环与提交规则收口。新增 `skills/gitcommitzh/references/pr-closure-checklist.md`，更新 `skills/gitcommitzh/SKILL.md`、`skills/using-shanforge/SKILL.md`、`skills/using-shanforge/references/codex-tools.md` 和 Superpowers 流程方案，明确提交前必须核对 review、evidence、memory sync、work item ledger 和当前任务范围；`gitcommitzh` 只做本地提交，不创建、不推送、不合并 PR。新增 `tests/test_pr_commit_workflow_rules.py` 固定规则；`SF-SP-008` 当前为 `ready_for_review`。
- 2026-07-05：完成 `SF-SP-005` 和 `SF-SP-006` 独立复审闭环。新增 `.factory/workitems/SF-SP-005/reviews/iteration-3-independent-review.md`、`.factory/workitems/SF-SP-006/reviews/iteration-2-independent-review.md` 及对应 evidence；`SF-SP-005` 复审为 `approved / 92`，`SF-SP-006` 复审为 `approved / 95`。同时清理 workflow plan 中旧分支收尾 skill 名称的历史残留，并把 review-ledger 最新状态推进到 `pending_human_confirmation`。
- 2026-07-05：修复 `SF-SP-005` 和 `SF-SP-006` 独立 review 反馈。`SF-SP-005` 清理 `status-handling-checklist.md` 中执行 skill 协调 review 的残留，移除 `using-shanforge/references/codex-tools.md` 旧收尾入口引用，并补负向测试；`SF-SP-006` 将 review 输出拆为 `review_status` 与 `next_gate_status`，明确 receiving-code-review 的 `.factory/memory/review-ledger.jsonl` / `tasks.summary.md` 同步规则，并修正 OpenAI metadata 评分口径。两项均已进入 `ready_for_review`，等待独立复审。
- 2026-07-05：已为 `SF-SP-005`、`SF-SP-006`、`SF-SP-007` 补真实独立评审并入档。新增三份 independent review 文件，并把结果写入各 work item ledger 和 `.factory/memory/review-ledger.jsonl`：`SF-SP-005` 为 `changes_requested / 78`，`SF-SP-006` 为 `changes_requested / 84`，`SF-SP-007` 为 `approved / 95`。同步修正 Superpowers 流程方案当前进展，避免继续显示旧的 `96 / human_approved / 可进入下一阶段` 叙事。

- 2026-07-05：完成评审独立性硬门修复。新增 `tests/test_independent_review_gate.py`，并收紧 `skills/requesting-code-review/SKILL.md`、review templates、`review-score-rubric.md` 和 Superpowers 流程方案：同线程作者自检只能输出 `self_check_passed`，真实 `approved` 必须带 `reviewer_type / reviewer_id / reviewer_independence_evidence`，缺独立证据时不得写 `review_score` 或 `pending_human_confirmation`。已追加 SF-SP-007 与全局 review ledger correction，并新增修复报告、验证证据和同线程自检文件；当前状态仍为 `needs_independent_review`。
- 2026-07-05：新增项目管理控制面首版。已从项目管理 Excel 模板中提取目标先行、WBS 拆解、RACI、风险前置、沟通节奏、状态报告、变更受控和复盘闭环理念，落为 `docs/04-project-development/05-development-process/project-management-control-plane.md` 与 `.factory/pm/`。`.factory/pm/dashboard.md` 是 AI 和人类可读的管理摘要，Excel 十表对应事实文件、`wbs.md`、`risk-register.jsonl`、`change-register.jsonl` 保存当前管理事实；`.factory/pm/generated/status-dashboard.html` 是人类浏览器状态页。HTML 不是事实源，由 `using-shanforge` 按需渲染。
- 2026-07-05：推进 `SF-SP-007` 并新增首版验证与调试 gate，但 review 状态已更正为 `needs_independent_review`。`skills/verification-before-completion/` 固定 evidence before claims、完整命令、完整输出、exit code、失败数量、Red-Green 验证和完成声明检查清单；`skills/systematic-debugging/` 固定修复前根因调查、四阶段调试、数据流反向追踪、防御式校验、条件等待和 3 次修复失败后质疑架构；`skills/tdd-workflow/` 新增 `references/tdd-debugging-verification-gate.md`。此前 iteration-1 review 的 `approved / 96 / pending_human_confirmation` 是同线程作者自检，不是真实独立 review，不能作为进入 `SF-SP-008` 的依据。
- 2026-07-05：推进 `SF-SP-006` 并新增首版评审类 workflow skill，iteration-1 review 评分 `96 / 100`，当前状态为 `pending_human_confirmation`。`skills/requesting-code-review/` 固定 task review、PR review、独立 review task、review score rubric、review ledger 和人工确认门；`skills/receiving-code-review/` 固定 feedback triage、先核实再修改、逐项修复、response、修复报告和验证证据。根据 `SF-SP-005` 的流程边界修正，本轮 review skill 不声明前置、后置或下一步 skill，只输出状态、产物、证据和 `needs`。新增 `tests/test_review_workflow_skills.py` 固定 review package、中文 OpenAI 元数据、旧路径禁止项和路由回退禁止项。
- 2026-07-05：根据人工对 `SF-SP-004` 的反馈完成 iteration-2 修正。`skills/writing-plans/references/workitem-plan-template.md`、`task-brief-template.md`、`plan-review-template.md` 已从旧英文模板叙述改为中文短句；用户可见的 `checkbox`、`Ledger`、`review`、`memory sync` 等表述已中文化，代码标识符、路径、命令和状态值保留原文。`tests/test_writing_plans_skill.py` 已改为中文 references 断言，并加入旧英文模板短语禁止项。本轮状态为 `ready_for_review`，等待独立 review。
- 2026-07-05：根据人工对 `SF-SP-005` 的反馈完成 iteration-2 流程边界修正。`using-shanforge` 从“启动时选择相关 skill”升级为流程总控 / CTO，负责恢复状态、判断阶段、选择唯一下一步 skill、处理人工确认门；`subagent-driven-development`、`executing-plans`、`writing-plans` 删除或改写“与其他 skill 的关系 / 下一步执行选项 / REQUIRED NEXT SKILL”等路由内容，只保留本职输出、evidence、ledger、状态包和 `needs`。新增/更新测试固定工作 skill 不得硬编码评审、验证、提交等后继 skill。本轮状态为 `ready_for_review`，等待独立 review。
- 2026-07-05：按用户反馈精简 `brainstorming` 可视化伴侣路径说明，删除“不要保存到 `.superpowers/`”这类历史负向表述，改为正向说明持久化文件统一进入当前 work item 的 `design-assets/brainstorm/`，并同步 `tests/test_brainstorming_skill.py`。
- 2026-07-05：推进 `SF-SP-005` 并新增首版执行类 workflow skill，iteration-1 review 评分 `96 / 100`，当前状态为 `pending_human_confirmation`。`skills/subagent-driven-development/` 保留原 Superpowers 的逐任务隔离执行、实现者状态、Spec Review 后 Quality Review 和 review loop 语义，但输入输出改为 `.factory/workitems/<WORKITEM-ID>/plan.md`、`task-briefs/`、`ledger.jsonl`、`evidence/`、`reports/`、`reviews/` 和 `.factory/memory/`；`skills/executing-plans/` 作为 inline fallback，固定先批判性 review plan，再逐步执行、记录 evidence、设置 review checkpoint。新增 `tests/test_execution_workflow_skills.py` 固定本地化契约。人工确认前不得进入下一阶段。
- 2026-07-05：补充 `docs/04-project-development/05-development-process/superpowers-workflow-integration-plan.md`，明确 shanforge 的五层记忆模型：入口压缩层、主题摘要层、work item 执行层、ledger 审计层、正式文档层；同时新增 loop 结束人工确认门，要求每轮 loop 产出 execution report、verification evidence、review score，并先写入 `pending_human_confirmation`，人工确认后才允许 `done`、关闭 work item 或进入下一阶段。扩展 `tests/test_superpowers_reference_migration.py` 固定这些方案约束。
- 2026-07-05：完成 `SF-SP-004` 并通过 task review，新增首版 `skills/writing-plans/`。新版把 Superpowers 的计划生成流程本地化为 shanforge work item plan：默认写入 `.factory/workitems/<WORKITEM-ID>/plan.md` 和 `task-briefs/`，先锁定文件结构，再拆 TDD 小任务，并在计划中要求真实命令、期望输出、evidence、review gate、ledger 和 `.factory/memory/` 同步。新增 `workitem-plan-template.md`、`task-brief-template.md`、`plan-review-template.md` 和中文 OpenAI 元数据；新增 `tests/test_writing_plans_skill.py` 固定这些约束。当前状态为 `approved`，但尚未提交或进入 PR 闭环。
- 2026-07-05：推进 `SF-SP-003` 的现有 skill references 迁移切片并通过 task review。新增 `requirements-engineering` PRD 模板、`document-templates` 技术设计模板、`tdd-workflow` 根因定位清单和 evidence 模板、`gitcommitzh` 提交说明 rubric，并让对应 `SKILL.md` 指向这些 references。新增 `tests/test_superpowers_reference_migration.py` 固定迁移结果和“未来 workflow skill references 尚未完成”的边界；本切片状态为 `approved_for_slice`，不能代表 `SF-SP-003` 整体完成。
- 2026-07-05：评审并修正 `skills/brainstorming/` 中文化结果。`SKILL.md` 现改为 shanforge 状态/work item 驱动：默认不直接展开读取项目背景文件清单，而是优先消费当前对话、`project-memory` 会话卡和当前 work item brief/ledger；缺上下文时先交给 `project-memory` 恢复最小会话卡，再决定是否头脑风暴及下一步 skill；不再用提示词硬编码“固定只转 writing-plans”。头脑风暴产物保存到 `.factory/workitems/<WORKITEM-ID>/brief.md`、`ledger.jsonl`、必要的正式 `docs/04-project-development/` 文档和 memory summary。`visual-companion.md` 已中文重写，helper 脚本新增 `--workitem-id`，持久化目录改为 `.factory/workitems/<WORKITEM-ID>/design-assets/brainstorm/`，浏览器标题改为 Shanforge 口径并移除外部 Superpowers 品牌图片依赖；测试覆盖同步更新。
- 2026-07-05：启动 `SF-SP-002`，新增首版 `skills/project-memory/`，把 `factory-agent-session` 的会话恢复、读取范围、会话卡、ledger 事件模板和 current-state 更新清单拆成 skill-first references，并新增中文 OpenAI 元数据。后续会话恢复先复用已有会话卡和压缩记忆，只有缺口明确时才增量读取 runtime/current-state/doc-map/role/project/summary，并在会话卡输出“未读 / 已排除上下文”。新增 `tests/test_project_memory_skill.py` 验证 `project-memory` 的触发、读取门、排除项、ledger 防重复和元数据。
- 2026-07-05：更新 `skills/skill-creator/SKILL.md`，把用户要求的 skill 编写/修改方式沉淀为通用原则：所有项目内 skill 默认中文，改写由“丰富经验的中文语言专家和 prompt 专家”执行，中文短句但完整保留原文含义；修改前写含义保留清单，作者自检只能进入 `ready_for_review`，最终通过必须来自独立 reviewer；`references/` 承载模板/schema/rubric，skill-scoped helper code 可放在本 skill 的 `scripts/`，但不得变成全局中心脚本主控。新增 `tests/test_skill_creator_skill_principles.py` 固定这些约束。
- 2026-07-05：修正 `docs/04-project-development/05-development-process/superpowers-workflow-integration-plan.md` 的 skill/code 边界：AI 开发流程必须由 skill 衔接；目标明确、重复、确定性的 `py/js` helper code 可随 skill 打包，但必须显式声明输入输出、配套 `references/` 契约、测试和独立 review；禁止的是中心脚本主控、全局 CLI 化和隐藏执行器。
- 2026-07-05：补强 `docs/04-project-development/05-development-process/superpowers-workflow-integration-plan.md`，新增“运动员 / 裁判隔离”硬规则。实现者只能提交 report、evidence 并进入 `ready_for_review`，不得自批 `approved / done`；任务完成必须交给独立 review task 或未参与实现的 reviewer 子 agent，review、verification、memory sync 均通过后才允许关闭。
- 2026-07-04：将 `skills/stratix-nodejs-backend/` 重命名为 `skills/stratix-service/`，并基于 `/Users/uroborus/NodeProject/wps/obsync-root` 当前源码和文档事实更新 skill 本体、OpenAI 元数据与 references。新版把 Stratix 后端开发模式改为 `create-stratix` 初始化、`stratix` forge 命令生成/诊断/启动/配置/发布门禁、AI 按业务场景自主选择最小 preset/plugin；同步补充 `STRATIX_SENSITIVE_CONFIG`、`.env`、默认 key 与显式 `STRATIX_ENCRYPTION_KEY` 的加解密边界，并新增 `tests/test_stratix_service_skill.py` 固定新名称、CLI 分工、插件选择和敏感配置规则。
- 2026-07-04：新增 `docs/04-project-development/05-development-process/superpowers-workflow-integration-plan.md`，将 Superpowers 的计划、开发、评审、验证和收尾流程映射为 shanforge 的 skill-first 状态驱动方案。方案明确不照搬提示词优先的 `using-superpowers`，也不再把中心 CLI、`factory-dispatch`、action-registry 或 `scripts/` 作为新流程主控；目标形态是 `using-shanforge` 轻量入口、`project-memory` skill、各 workflow skill、`.factory/memory/` ledger 和 skill `references/` 模板体系。
- 2026-07-04 补充：`factory-agent-session` 已降级为迁移来源而非目标入口。其会话启动清单、相关性判断、会话卡模板、ledger 事件模板和 current-state 更新清单应拆入 `skills/project-memory/references/`；具体 work item 计划唯一主存储为 `.factory/workitems/<WORKITEM-ID>/plan.md`。
- 2026-07-04：将 `skills/brainstorming/SKILL.md` 按用户要求改写为精简中文短句，保留新版英文流程的全部约束：创造性工作前必用、设计批准硬门、范围拆分、2-3 方案、设计文档、规范自审、用户审阅、按需可视化伴侣和仅转入 `writing-plans`。新增 `skills/brainstorming/agents/openai.yaml` 中文界面元数据，并新增 `tests/test_brainstorming_skill.py` 固定中文内容和 OpenAI 元数据。
- 2026-06-14：新增 `skills/browser-control/` 中文 skill 和 `tests/test_browser_control_skill.py`，将本地浏览器控制收口为明确工具路由：用户指定“本地浏览器 / browser-use / 真实浏览器访问 URL”时优先使用 `browser-use` CLI；显式 `@Browser` 使用 Codex 内嵌浏览器，显式 `@Chrome` 或登录态需求使用 Codex Chrome 插件。skill 已补齐启用后的示例用法、`browser-use --headed --session browser-control --json open <URL>` 默认命令、页面状态读取、截图/点击/输入常用命令，以及 cookies、上传、表单提交、登录态和权限类动作的确认边界。
- 2026-06-01：新增 `skills/prompt-room/` Codex-native 提示词工程部 skill，承接最终剧本、导演分镜和美术资产包，输出 `prompts/comfyui-*` prompt/asset/workflow/tuning 工程产物与 `reports/comfyui-prompt-qc.md`；新增 7 个角色任务卡、ComfyUI prompting/tuning reference、schema 与结构测试。
- 2026-05-31：新增 `skills/writer-room/` Codex-native 编剧室 skill，明确由主 Codex 线程调度 11 个 Codex 子 agent 完成 brief、outline、角色、场景、初稿、诊断、改写、连续性、评分、记忆和进化建议；新增角色任务卡、artifact contract、评分 rubric、模板、schema 与结构测试，并同步 `writer-room-simulator-design.md` 的正式设计口径。
- 2026-04-21：继续把 `MG-WP-004` 推进到 reviewer guidance：`MemoryLifecycleQueueItem` 新增 `resolution_required` 与 `resolution_options`，并由 `DefaultMemoryDomainService` 按 `conflict_superseded / decay_expired / manual_override_applied` 等 lifecycle reason 直接投影推荐 resolution 与建议 note 模板，减少产品层自拼说明文案。
- 2026-04-21：扩充 `tests/test_domain_memory_governance.py`，补齐 conflict/decay queue item 的 guidance 回归；专项回归 `20 passed`。
- 2026-04-21：继续把 `MG-WP-004` 推进到更明确的 audit read model：`MemoryLifecycleAuditFilter` 新增 `latest_per_record_only`，`_project_lifecycle_audit_log()` 现会按 `created_at`/append 顺序生成真正的最新优先结果，`lifecycle_audit_summary` 也新增 `latest_by_record`，不再让产品层自己从全量事件回放当前人工结论。
- 2026-04-21：扩充 `tests/test_domain_memory_governance.py` 与 `tests/test_memory_governance_regression.py`，补齐 `latest_per_record_only`、`latest_entries` 最新优先语义和 `latest_by_record` explainability 投影；专项回归 `25 passed`。
- 2026-04-21：继续把 `MG-WP-004` 推进到 reviewer resolution taxonomy：`update_lifecycle_queue(..., resolution=...)` 现可显式持久化人工结论，`reopen_lifecycle_queue(...)` 回到 `pending` 时会清空 resolution，`load_lifecycle_audit(...)` 也开始支持按 `queue_review_status / resolution` 过滤，`lifecycle_queue_summary / lifecycle_audit_summary` 现会稳定投影 `resolution_counts`。
- 2026-04-21：补齐 `JsonlMemoryLifecycleQueueStore` 与 `JsonlMemoryLifecycleAuditStore` 的 `review_resolution / resolution` durable round-trip 缺口，并扩充 `tests/test_settings_memory_stores.py`、`tests/test_platform_scaffold.py`、`tests/test_session_inspection_service.py` 与 `tests/test_memory_governance_regression.py`，验证跨 container reopen 后 resolution 清空与 explainability 回读；本轮小范围回归 `20 passed`。
- 2026-04-21：继续把 `MG-WP-004` 推进到 queue 运维批量面：`update_lifecycle_queue(...)` 与 `reopen_lifecycle_queue(...)` 现已支持 `queue_filter` 驱动的批量 review selection，并在 domain 中显式区分“review filter 命中全集”与 `apply_lifecycle(...)` 的默认选中子集语义。
- 2026-04-20：继续把 `MG-WP-004` 推进到显式 review workflow：新增 `reopen_lifecycle_queue(...)`，并把同状态 note update 拆成独立 `review_note_updated` 审计动作；当前 memory domain 已能区分 `review_status_updated / review_note_updated / review_reopened / lifecycle_applied` 四类人工治理事件。
- 2026-04-20：扩充 `tests/test_domain_memory_governance.py`、`tests/test_session_inspection_service.py`、`tests/test_platform_scaffold.py` 与 `tests/test_memory_governance_regression.py`，补齐 `reopen`、note-only update 与 `lifecycle_audit_summary` 的主链回归；受影响主链回归仍为 `61 passed`。
- 2026-04-20：同步更新 `memory-governance-design.md`、`memory-runtime-interfaces.md`、`memory-system-detailed-design.md`、`memory-governance-implementation-plan.md`、`test-plan.md` 与 `.factory/memory/runtime-brief.md`、`api.summary.md`、`tests.summary.md`、`tasks.summary.md`、`current-state.md`，补齐显式 review workflow 的事实层收口。
- 2026-04-20：继续把 `MG-WP-004` 推进到 durable lifecycle audit trail：新增 `MemoryLifecycleAuditRepositoryPort`、`MemoryLifecycleAuditAction / Entry / Filter / Log`、`load_lifecycle_audit(...)` 与 `JsonlMemoryLifecycleAuditStore`；`update_lifecycle_queue()` 和 `apply_lifecycle()` 现会留下 `review_status_updated / lifecycle_applied` 审计轨迹，`explain_session_memory()` 也开始输出 `lifecycle_audit_summary`。
- 2026-04-20：扩充 `tests/test_domain_memory_governance.py`、`tests/test_session_inspection_service.py`、`tests/test_settings_memory_stores.py`、`tests/test_composition_registry.py`、`tests/test_settings_layer_scaffold.py` 与 `tests/test_platform_scaffold.py`，补齐 lifecycle audit trail 的 store / access-application entrypoint / container persistence 回归；受影响主链回归 `61 passed`。
- 2026-04-20：同步更新 `memory-governance-design.md`、`memory-runtime-interfaces.md`、`memory-system-detailed-design.md`、`memory-governance-implementation-plan.md`、`test-plan.md` 与 `.factory/memory/runtime-brief.md`、`api.summary.md`、`tests.summary.md`、`tasks.summary.md`、`current-state.md`，补齐 durable lifecycle audit trail 的事实层收口。
- 2026-04-20：继续把 `MG-WP-004` 推进到 durable lifecycle review queue：新增 `MemoryLifecycleQueueRepositoryPort`、`MemoryLifecycleQueueEntry / ReviewStatus / UpdateResult`、`update_lifecycle_queue(...)` 与 `JsonlMemoryLifecycleQueueStore`；`load_lifecycle_queue()` 现默认只回 `pending` actionable queue，`apply_lifecycle()` 成功后会把对应 queue item 标记为 `applied`。
- 2026-04-20：扩充 `tests/test_domain_memory_governance.py`、`tests/test_settings_memory_stores.py`、`tests/test_session_inspection_service.py`、`tests/test_composition_registry.py`、`tests/test_settings_layer_scaffold.py` 与 `tests/test_platform_scaffold.py`，补齐 queue review state 的 durable store / access-application entrypoint / container persistence 回归；受影响主链回归 `61 passed`。
- 2026-04-20：同步更新 `memory-governance-design.md`、`memory-runtime-interfaces.md`、`memory-system-detailed-design.md`、`memory-governance-implementation-plan.md`、`test-plan.md` 与 `.factory/memory/runtime-brief.md`、`api.summary.md`、`tests.summary.md`、`tasks.summary.md`、`current-state.md`，补齐 durable lifecycle review queue 的事实层收口。
- 2026-04-20：继续把 `MG-WP-004` 推进到 provider-aware lifecycle writeback：`MemoryProviderGovernanceDecision` 新增 `allow_lifecycle_writeback`，`MemoryProviderPort / MemoryProviderManagerPort` 新增 `on_lifecycle_apply(...)`，`DefaultMemoryDomainService.apply_lifecycle()` 会在本地 durable 更新后按治理决策继续同步 external provider，并刷新 session explainability / assembly manifest。
- 2026-04-20：修正 `MemoryGovernanceService.apply_lifecycle()` 未回写 session ledger 的缺口；当前 `apply_lifecycle()` 触发的 provider diagnostics 与 manifest 刷新已能被后续 `preview_recall()` 稳定回读。
- 2026-04-20：扩充 `tests/test_domain_memory_governance.py`、`tests/test_runtime_memory_provider_manager.py`、`tests/test_memory_governance_regression.py`、`tests/test_settings_profile_governance.py` 与 `tests/test_settings_layer_scaffold.py`，补齐 `lifecycle_apply` 通道、provider governance 与容器级 writeback explainability 回归；定向回归 `45 passed`。
- 2026-04-20：同步更新 `memory-governance-design.md`、`memory-runtime-interfaces.md`、`memory-system-detailed-design.md`、`memory-governance-implementation-plan.md`、`test-plan.md` 与 `.factory/memory/api.summary.md`、`tests.summary.md`、`tasks.summary.md`、`current-state.md`，补齐 provider-aware lifecycle writeback 的事实层收口。
- 2026-04-19：继续把 `MG-WP-004` 推进到 review queue 产品面：新增 `MemoryLifecycleQueueFilter / QueueItem / Queue`，`MemoryDomainService.load_lifecycle_queue()` 与 `apply_lifecycle(..., queue_filter=...)`，并让 `explain_session_memory()` 稳定投影 `lifecycle_queue_summary`；当前 lifecycle governance 已具备最小 queue/batch-apply 闭环。
- 2026-04-19：同步更新 `memory-governance-design.md`、`memory-governance-implementation-plan.md`、`memory-runtime-interfaces.md`、`memory-system-detailed-design.md` 与 `.factory/memory/api.summary.md`、`current-state.md`、`tasks.summary.md`、`tests.summary.md`，补齐 lifecycle review queue 的事实层收口。
- 2026-04-19：继续完成 `MG-WP-004` 的 apply 面：新增 `MemoryLifecycleEvaluation / ReviewResult / ApplyResult`，`MemoryDomainService.review_lifecycle / apply_lifecycle`，`application.memory.MemoryGovernanceService` 与 `MemoryAPI.review_lifecycle / apply_lifecycle`；当前 lifecycle governance 已具备 review/apply/durable writeback 的最小闭环。
- 2026-04-19：同步更新 `memory-governance-design.md`、`memory-runtime-interfaces.md`、`memory-system-detailed-design.md` 与 `.factory/memory/api.summary.md`、`current-state.md`、`tasks.summary.md`、`tests.summary.md`，补齐 lifecycle review/apply 的事实层收口。
- 2026-04-19：继续完成 `MG-WP-004` 首轮代码落地：`MemoryLifecyclePolicy` 现已补齐 conflict supersede、forced manual override 与 metadata-driven decay forget，并让 `DefaultMemoryDomainService.explain_session_memory()` 投影 scoped `lifecycle_evaluations`，用于解释 memory 为什么 retained / superseded / forgotten。
- 2026-04-19：同步更新 `memory-governance-design.md`、`memory-runtime-interfaces.md`、`memory-system-detailed-design.md` 与 `.factory/memory/api.summary.md`、`current-state.md`、`tasks.summary.md`、`tests.summary.md`，补齐 `MG-WP-004` 的事实层收口。
- 2026-04-19：继续完成 `MG-WP-005` 首轮代码落地：新增 `tests/test_memory_governance_regression.py`，将 `TC-013 ~ TC-016` 收口为独立记忆治理专项回归入口；同时把 `MemoryStatus.FORGOTTEN` 纳入默认 lifecycle state machine，并扩充 `DefaultMemoryDomainService.explain_session_memory()`，稳定输出 `recalled_memory_statuses / promotion_reasons / promotion_decisions / memory_provider_binding / recall_plan`。
- 2026-04-19：同步更新 `memory-runtime-interfaces.md`、`memory-system-detailed-design.md` 与 `.factory/memory/api.summary.md`、`current-state.md`、`tasks.summary.md`、`tests.summary.md`，补齐 `MG-WP-005` 的事实层收口。
- 2026-04-19：继续完成 `MG-WP-002 / MG-WP-003` 首轮代码落地：`RecallPlan` 现冻结显式排序指令，`DefaultRecallRanker` 与 preview explainability 按同一 plan 执行排序；`MemoryProviderManagerPort` 与 `DefaultMemoryProviderManager` 现已直接消费 `MemoryProviderGovernanceDecision`，manager 本身不再持有 writable / shared-write gate。
- 2026-04-19：扩充 `tests/test_runtime_memory_provider_manager.py`、`tests/test_runtime_memory_recall_pipeline.py` 与相关记忆主链回归，验证新的 provider decision 输入和显式排序指令；记忆主链相关回归 `55 passed`。
- 2026-04-19：同步更新 `memory-runtime-interfaces.md`、`memory-system-detailed-design.md` 与 `.factory/memory/api.summary.md`、`current-state.md`、`tasks.summary.md`、`tests.summary.md`，补齐 `MG-WP-002 / MG-WP-003` 的事实层收口。
- 2026-04-19：完成 `MG-WP-001` 首轮代码落地：新增 `src/domain/memory/governance.py`，显式建模 recall/provider/lifecycle 治理对象与 decision；`DefaultMemoryDomainService` 现会先消费领域治理决策，再调用 `RecallPlannerPort` 与 provider manager，`DefaultRecallPlanner` 也已降格为把 `RecallGovernanceDecision` 物化成带预算的 `RecallPlan`。
- 2026-04-19：新增 `tests/test_domain_memory_governance.py` 并调整 `tests/test_runtime_memory_recall_pipeline.py`，补齐 `MG-WP-001` 的领域级与执行链回归；记忆主链相关回归 `48 passed`。
- 2026-04-19：同步更新 `memory-runtime-interfaces.md`、`memory-system-detailed-design.md` 与 `.factory/memory/api.summary.md`、`current-state.md`、`tasks.summary.md`、`tests.summary.md`，把 recall 主链正式口径收紧为 `RecallGovernancePolicy -> RecallPlannerPort -> scan_memory_records() -> RecallRankerPort`。
- 2026-04-19：新增 `docs/04-project-development/04-design/memory-governance-design.md`，正式归档记忆领域治理专项设计，明确 memory domain 与 runtime/settings 的职责边界。
- 2026-04-19：更新 `docs/04-project-development/04-design/index.md` 与 `memory-system-detailed-design.md`，将记忆治理专题纳入记忆专项正式阅读顺序。
- 2026-04-19：同步更新 `.factory/memory/doc-map.md`、`architecture.summary.md`、`api.summary.md` 与 `current-state.md`，补齐记忆治理专项的压缩事实源。
- 2026-04-19：新增 `docs/04-project-development/05-development-process/memory-governance-implementation-plan.md`，将记忆治理专项下沉为 `MG-WP-001 ~ MG-WP-005` 工作包，并同步更新实施计划、测试计划、需求追踪矩阵和 `.factory/memory/tasks.summary.md`。
- 2026-04-15：新增 `docs/04-project-development/04-design/memory-system-detailed-design.md`，正式归档记忆系统的业务驱动详细设计方案。
- 2026-04-15：新增 `docs/04-project-development/04-design/infrastructure-layer-design.md`，完成基础设施层 7 个技术域建模，并定义应用层门面接口、运行时资源端口及下一轮待补齐端口清单。
- 2026-04-15：更新 `docs/04-project-development/04-design/infrastructure-layer-design.md`，明确实现阶段优先复用 Hermes 现有实现，并补齐技术域到 Hermes 模块的映射、反腐适配边界与 Hermes-backed adapter 落地规则。
- 2026-04-15：在 `src/` 落第一批基础设施代码骨架：新增 `domain/approval`、`domain/delegation`、`domain/gateway`，补齐 `ApprovalPolicyPort`、`SandboxPolicyPort`、`DelegationTransportPort`、`GatewayPort`，并加入 Hermes-backed capability/approval/delegation/gateway scaffold。
- 2026-04-15：更新 `memory-system-detailed-design.md`、`memory-runtime-interfaces.md` 与 `hermes-agent-source-analysis-report.md`，吸收 Hermes Agent 的 provider manager、archive query、bounded memory 与 delegation isolation 设计精华。
- 2026-04-15：同步更新设计索引、站点导航、需求追踪矩阵与 `.factory/memory` 摘要，补齐 `REQ-006` 的业务到设计追踪。
- 2026-08-23：`TEST-GOVERNANCE-001` 清理旧平台测试入口和失效案例目录，新增完整测试案例模板，升级人类可读测试报告，并统一案例七态与批次四态；实现提交 `c4534ba` 已通过干净克隆验证。
- 2026-08-23：`TEST-GOVERNANCE-CLOSURE-001` 正式发布测试计划 `v3.2.0` 和案例目录 `v1.0.0`，新增标准库案例/报告校验器与三份质量模板自动门；实现提交 `ca436c9` 干净克隆全绿，最终人类可读报告校验通过。
