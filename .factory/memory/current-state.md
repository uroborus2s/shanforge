# 当前状态

- 当前模式：cli_direct
- 当前阶段：IMPLEMENTATION
- 活跃任务：3
- 活跃变更：2
- 活跃缺陷：0
- 活跃 PR：0

- 角色目录总数：9
- 当前阶段主要角色：项目协调者、解决方案架构师、UX/UI 设计师、后端工程师、前端工程师、文档与记忆管理员

- 当前技术画像：抽象 Agent 平台规划画像
- 技术画像预设：custom
- 关键工程规则数：3
- 设计交付物数：1

## 最近条目
- 2026-07-05 18:36：用户已确认 `SF-SP-010` 独立复审结果，状态记为 `human_approved`。下一步只有在用户明确要求提交 / commit 时才进入提交流程；提交必须只纳入 `SF-SP-010` 范围和当前确认 hunk。
- 2026-07-05 18:20：`SF-SP-010` iteration-1 独立复审通过，reviewer `codex-sf-sp-010-rereviewer-20260705` 给出 `approved / 95`。复审确认旧下一步文案、PM 导航目标检查和 JSONL evidence 一致性均已修复；当前 gate 是 `pending_human_confirmation`，不得自动提交或关闭。
- 2026-07-05 18:14：`SF-SP-010` iteration-1 独立 review 返回 `changes_requested / 82`。已修复三项反馈：Superpowers 方案旧下一步文案、PM 控制面导航目标存在性测试、JSONL evidence 命令与计数记录；当前状态 `ready_for_re_review`，下一步必须做真实独立复审。
- 2026-07-05 18:05：`SF-SP-009` 已提交为 `9296f58`，`SF-SP-010` 已进入文档、导航、memory 同步开发。当前范围是同步 Superpowers 方案当前进展、开发过程导航、根文档导航、`.factory/memory/doc-map.md`、summary 和测试报告入口；实现完成后必须先进入真实独立 review，不能自批完成。
- 2026-07-05 17:39：`SF-SP-009` 已进入黑盒流程 eval 开发并完成 iteration-1 实现，状态为 `ready_for_review`。本轮新增 `skills/using-shanforge/references/black-box-flow-eval.md`，并在 `using-shanforge` 中加入 `SF-SP-009` / 黑盒流程评估入口；场景覆盖一句话需求、bug 修复、review 反馈、压缩恢复、完成声明和自评隔离，评分门要求总分 `>= 90` 且 critical assertion 不得为 `0`。验证：红灯 `4 failed`，绿灯 `4 passed`，邻近 workflow 回归 `26 passed`，ruff、skill validator 和 `git diff --check` 通过。下一步必须做独立 review。
- 2026-07-05 17:50：`SF-SP-009` iteration-1 独立 review 返回 `changes_requested / 84`，指出评分归一化公式缺失、结构测试偏弱，以及正式计划仍写 4 类场景。已修复：黑盒 eval reference 增加最高分、实际分和百分制总分公式；结构测试补每场景 critical assertion 与证据字段断言；正式计划改为 6 类场景。验证：加严红灯 `3 failed`，修复后 `6 passed`，邻近 workflow 回归 `28 passed`，ruff、validator、JSONL 和 `git diff --check` 通过。下一步是真实独立复审。
- 2026-07-05 17:53：`SF-SP-009` iteration-1 独立复审通过，reviewer `codex-sf-sp-009-rereviewer-20260705` 给出 `approved / 95`。复审确认评分公式、结构测试和 6 类场景口径均已修复；当前 gate 是 `pending_human_confirmation`，不得自动提交或进入 `SF-SP-010`。
- 2026-07-05 17:56：用户已确认 `SF-SP-009` 独立复审结果，状态记为 `human_approved`，并要求进入 `SF-SP-009` 提交流程，提交后直接开始 `SF-SP-010` 开发。提交必须只纳入 `SF-SP-009` 范围和当前确认 hunk，不混入既有其他脏改动。
- 2026-07-05 17:24：用户已确认 `SF-SP-008` 独立 review 结果并要求进入提交流程，提交后直接进入 `SF-SP-009` 开发。`SF-SP-008` 主 review 为 `approved / 94`，范围隔离复审为 `approved / 94`；提交必须只纳入 SF-SP-008 相关 hunk，混合 `.factory/memory/` 文件不得整文件提交。
- 2026-07-05 16:52：按用户反馈撤销 `SF-SP-008` 的中心脚本 gate 方案。`scripts/factory-*` 和 `factory-dispatch` 不作为新的 workflow gate；执行纪律改为 skill-native 收尾门：`using-shanforge` / `gitcommitzh` 在完成声明、提交或关闭 work item 前必须重读最新 work item ledger 和 review ledger，若仍有 `ready_for_review` 或 `next_required_action`，只能报告阻塞 gate 和下一步动作。`SF-SP-008` 仍需真实独立 review，不能标记完成。
- 2026-07-05 16:30：按用户要求对 `skills/stratix-service/` 做上线前评估并修正。已创建语言、测试、QC 三个子任务：语言任务建议主文件短句化并去除口号；QC 任务指出敏感配置、hex key、旧全局 CLI、插件选择风险；测试任务在 `/private/tmp/shanforge-stratix-skill-test-20260705` 实测 `@stratix/create@1.1.0` 生成应用。主 skill 已新增配置安全门、Stratix 1.1.x / `pnpm exec stratix` 探测、adapter / integration / data 选择规则；配置 reference 明确应用配置必须 JSON -> encrypt -> `STRATIX_SENSITIVE_CONFIG` 注入，普通 `.env` 不承载应用配置；脚手架示例已移除普通 env 回退。验证显示 doctor/test/config encrypt-decrypt 可用，但 build、start、release gate、openapi 在当前 Node 24 + 1.1.0 组合下有失败风险，已记录为 blocked 条件。
- 2026-07-05 13:21：按用户反馈优化 PM HTML 页面为完整项目管理视图。`.factory/pm/generated/status-dashboard.html` 已包含项目甘特图、项目任务看板、评审链路总览、WBS 和 PM 十模块入口；`.factory/pm/generated/workitems.html` 已重写为人类可读的任务详情页，按 work item 展示任务摘要、事件时间线、评审链路和每轮评审结果详情；`.factory/pm/generated/pm-details.html` 已补完整项目进度入口并同步最新 gate。新增回归断言确保关键评审锚点存在、链接只跳 HTML 视图、不再让人默认打开 `.md` / `.jsonl` 原文。
- 2026-07-05 13:17：按用户“现在开始继续做 `SF-SP-008`”的明确指令进入 `SF-SP-008`，并将该指令记录为接受 `SF-SP-005/006/007` 独立 review 结果后继续推进的人工确认事件。`SF-SP-008` 已新增 PR 闭环与提交规则首版：`gitcommitzh` 现在要求提交前核对 work item ledger、review ledger、verification evidence、memory sync 和当前任务范围；`using-shanforge` 新增提交门；`codex-tools` 明确 sandbox 收尾只能提交当前任务范围。当前状态为 `ready_for_review`，需要独立 review。
- 2026-07-05 12:47：`SF-SP-005` 和 `SF-SP-006` 已完成真实独立复审并入档。`SF-SP-005` iteration-3 复审 reviewer agent `019f3093-ba94-7ba3-8aee-3aa9d32b44ed` 给出 `approved / 92`；`SF-SP-006` iteration-2 复审 reviewer agent `019f3093-c562-70f1-915e-48f4d604e8eb` 给出 `approved / 95`。两者当前 gate 均为 `pending_human_confirmation`，不是自动 `human_approved`；下一步需用户确认是否接受复审结果并进入 `SF-SP-008`。
- 2026-07-05 12:34：已修复 `SF-SP-005` 和 `SF-SP-006` 独立 review 的阻塞项并进入 `ready_for_review`。`SF-SP-005` 修复执行 skill reference 仍协调 review 流程、旧分支收尾 skill 引用和负向测试缺口；`SF-SP-006` 修复 `review_status` / `next_gate_status` 状态契约、`receiving-code-review` memory sync 目标和 requesting metadata 评分口径。相关 workflow 回归 `28 passed`，ruff、skill validators、JSONL 和 `git diff --check` 通过。下一步是真实独立复审，不能自批。
- 2026-07-05 12:00：已按用户要求为前序任务补真实独立评审并入档。`SF-SP-005` iteration-2 独立评审为 `changes_requested / 78`，需修执行 skill reference 仍协调 Spec Review / Review 回流、正式计划旧状态叙事、`using-shanforge` Codex 参考旧入口；`SF-SP-006` iteration-1 独立评审为 `changes_requested / 84`，需修 `same_thread`/`needs_independent_review` 状态契约和 `receiving-code-review` memory/review-ledger 同步规则；`SF-SP-007` iteration-1 独立评审为 `approved / 95`，但不是 `human_approved`，且仍被 `SF-SP-005/006` 阻塞，不能进入 `SF-SP-008`。
- 2026-07-05 12:20：按用户反馈修正 PM HTML 状态页“点开仍是 Markdown / JSONL 原文”的问题。新增 `.factory/pm/generated/pm-details.html` 和 `.factory/pm/generated/workitems.html`，首页链接现在跳到 HTML 渲染详情页；Excel 十模块、WBS / work item、风险、变更和状态报告均以表格 / 列表方式查看，不直接打开 `.md` / `.jsonl` 源文件。
- 2026-07-05 11:51：按用户要求输出并刷新当前项目状态。最新有效状态以 work item ledger 为准：`SF-SP-004` 和 `SF-SP-005` iteration-2 仍为 `ready_for_review`，`SF-SP-007` 为 `needs_independent_review`，不能直接进入人工确认；PM-001 最小切片为 `done`，人类 HTML 状态页已刷新到 `.factory/pm/generated/status-dashboard.html`。
- 2026-07-05：已完成评审独立性硬门修复。`requesting-code-review`、review rubric、task/independent/PR review 模板和 Superpowers 流程方案已强制区分 `same_thread` 作者自检与真实独立评审：同线程只能产生 `self_check_passed` 和 `author_self_check_score`，不得写 `approved`、`review_score` 或进入 `pending_human_confirmation`；`approved` 必须带 `reviewer_type / reviewer_id / reviewer_independence_evidence`。新增 `tests/test_independent_review_gate.py` 固定该规则，workflow 联合回归 `27 passed`，ruff、skill validator、JSON/JSONL 解析和 `git diff --check` 通过。当前仍只是同线程自检通过，`SF-SP-007` 状态保持 `needs_independent_review`。
- 2026-07-05：新增项目管理控制面首版设计与 PM 状态页。已将项目管理 Excel 模板理念抽象为目标先行、WBS 拆解、RACI、风险前置、沟通节奏、状态报告、变更受控和复盘闭环，并接入当前 shanforge 流程：`.factory/pm/dashboard.md` 作为人类和 AI 可读的管理摘要，Excel 十表对应事实落到 `.factory/pm/`，`.factory/pm/generated/status-dashboard.html` 作为人类浏览器状态页。HTML 不是事实源，事实仍以 PM Markdown/JSONL 和 work item ledger 为准；不新增独立 PM skill，由 `using-shanforge` 按需渲染。
- 2026-07-05：`SF-SP-007` 已新增首版验证与调试 gate，但 review 状态已更正为 `needs_independent_review`：此前的 `approved / 96 / pending_human_confirmation` 来自同线程作者自检，不是真实子任务或独立 reviewer 评审，因此不能作为通过证据。`skills/verification-before-completion/`、`skills/systematic-debugging/` 和 `tdd-workflow` 融合 reference 已实现，联合 workflow 回归 `22 passed`，三个 skill validator 通过；下一步必须先补真实独立 review，不能进入 `SF-SP-008`。
- 2026-07-05：用户已确认 `SF-SP-006` iteration-1 通过，状态记为 `human_approved`，允许进入 `SF-SP-007`。下一阶段范围是验证与调试 gate：本地化 `verification-before-completion`、`systematic-debugging`，并把 TDD / 根因定位 / 完成声明前新鲜验证证据的规则收口到 shanforge work item、evidence、ledger 和人工确认门。
- 2026-07-05：进入 `SF-SP-006` 并新增首版评审类 workflow skill，当前停在 `pending_human_confirmation`：`skills/requesting-code-review/` 负责 task review、PR review、独立 review task、评分 rubric 和 reviewer approved 与人工确认分离；`skills/receiving-code-review/` 负责 review feedback triage、先核实再修改、逐项处理、response 和验证证据。已按 `SF-SP-005` 的边界修正删除 review skill 对后继 skill 的路由声明，只输出 review 产物、状态和 `needs`；新增 `tests/test_review_workflow_skills.py` 固定本地化契约、无旧 Superpowers 路径、无工作 skill 路由回退。iteration-1 review 评分 `96 / 100`，等待人工确认。
- 2026-07-05：收到人工对 `SF-SP-004` 的修改意见：`writing-plans/references` 还没有完全使用中文重写。已完成 iteration-2 修正并进入 `ready_for_review`：三份 references 模板改为中文标题、字段、章节、步骤和检查项，保留必要代码标识符、路径、命令和状态值；`tests/test_writing_plans_skill.py` 改为断言中文模板并禁止旧英文模板短语回退。当前等待独立 review，未标记 `approved / done`。
- 2026-07-05：收到人工对 `SF-SP-005` iteration-1 的修改意见：执行类 skill 仍在“与其他 skill 的关系”中协调上下游，违反流程总控边界。已完成 iteration-2 修正并进入 `ready_for_review`：`using-shanforge` 升级为流程总控 / CTO，统一判断当前阶段、work item 状态、ledger 和唯一下一步 skill；`subagent-driven-development`、`executing-plans`、`writing-plans` 改为只输出状态、产物、证据和 `needs`，不再声明前置、后置、评审、验证、提交等 skill 路由。当前等待独立 review，未标记 `approved / done`。
- 2026-07-05：`SF-SP-005` 已新增首版执行类 workflow skill，iteration-1 review 评分 `96 / 100`，并已由用户确认 `human_approved`：`subagent-driven-development` 现在以 `.factory/workitems/<WORKITEM-ID>/plan.md`、`task-briefs/` 和 `ledger.jsonl` 为输入，按隔离执行者、实现报告、evidence、Spec Review、Quality Review 和 memory sync 推进；`executing-plans` 作为无子 agent 或强耦合任务的 inline fallback，先批判性 review plan，再逐步执行并设置 review checkpoint。验证 `.venv/bin/pytest tests/test_execution_workflow_skills.py` 通过 `4 passed`，SF-SP-003/004/005 联合验证 `10 passed`，ruff、两个 skill validator 和 `git diff --check` 通过。下一步进入 `SF-SP-006`。
- 2026-07-05：已补强 Superpowers 流程集成方案的记忆与人工确认门：记忆正式分为入口压缩层、主题摘要层、work item 执行层、ledger 审计层和正式文档层；每轮 loop 结束后必须产出 execution report、verification evidence 和 review score，并进入 `pending_human_confirmation`，只有人工明确确认后才能进入 `done` 或下一阶段。
- 2026-07-05：`SF-SP-004` 已新增首版 `skills/writing-plans/` 并通过 task review，状态为 `approved`。新版保留 Superpowers 的 scope check、file structure、bite-sized TDD steps、no placeholders、自审和 plan review 语义，但默认输出改为 `.factory/workitems/<WORKITEM-ID>/plan.md`、`task-briefs/`、evidence、reports、reviews 和 ledger；不再写入 `docs/superpowers/plans`，也不把每个小步骤 commit 作为硬要求。验证 `.venv/bin/pytest tests/test_writing_plans_skill.py` 通过 `3 passed`，SF-SP-003/SF-SP-004 联合验证 `5 passed`，ruff、skill validator 和 `git diff --check` 通过。
- 2026-07-05：`SF-SP-003` 已完成现有 skill references 迁移切片并通过 task review，状态为 `approved_for_slice`。本切片新增 `requirements-engineering/references/prd-template.md`、`document-templates/references/technical-design-template.md`、`tdd-workflow/references/root-cause-checklist.md`、`tdd-workflow/references/evidence-report-template.md`、`gitcommitzh/references/commit-message-rubric.md`，并新增 `tests/test_superpowers_reference_migration.py`。定向验证 `.venv/bin/pytest tests/test_superpowers_reference_migration.py` 通过 `2 passed`，ruff 和 4 个 skill validator 均通过。`requesting-code-review / verification-before-completion / systematic-debugging` 相关 references 尚未迁移，`SF-SP-003` 不能标记整体完成。
- 2026-07-05：已评审并修正 `skills/brainstorming/` 中文化结果：新版不再通过提示词硬门控把流程固定转成 `writing-plans`，也不再自己展开读取 `.factory/memory/` 背景文件清单；它优先消费当前对话、`project-memory` 会话卡、当前 work item brief/ledger 中已压缩的信息，缺上下文时先交给 `project-memory` 恢复最小会话卡。头脑风暴产物保存到 `.factory/workitems/<WORKITEM-ID>/brief.md`、`ledger.jsonl`、必要的 `docs/04-project-development/` 正式文档和 `.factory/memory/` summary；`visual-companion.md` 已中文重写，伴侣持久化目录改为 `.factory/workitems/<WORKITEM-ID>/design-assets/brainstorm/`，并要求被采纳设计资产登记到正式设计资产位置。
- 2026-07-05：已按 Superpowers 流程集成计划推进 `SF-SP-002` 到 task review approved，新增首版 `skills/project-memory/`，把会话启动清单、相关性判断、会话卡模板、memory ledger 事件模板和 current-state 更新清单拆入 `references/`，并新增 `agents/openai.yaml`。`project-memory` 现优先复用当前对话或 `.factory/memory/agent-session.md` 中的新鲜会话卡，只在缺口明确时增量读取 runtime/current-state/doc-map/role/project/summary，并在会话卡里输出“未读 / 已排除上下文”。新增 `tests/test_project_memory_skill.py` 固定触发、读取范围、排除项、ledger 防重复和 OpenAI 元数据；定向验证 `.venv/bin/pytest tests/test_project_memory_skill.py tests/test_brainstorming_skill.py` 通过 `9 passed`，`.venv/bin/ruff check tests/test_project_memory_skill.py tests/test_brainstorming_skill.py` 通过，`python3 skills/skill-creator/scripts/quick_validate.py skills/project-memory` 与 `skills/brainstorming` 通过。该工作项尚未提交或进入 PR 闭环，不能标记整体 `done`。
- 2026-07-05：已把“中文短句、完整保留含义、作者/评审隔离、循环验证到通过”固化为 `skills/skill-creator/SKILL.md` 的通用 skill 编写与修改原则。后续新建、改写、翻译、压缩 skill 时，作者只能推进到 `ready_for_review`，必须由独立 reviewer 判断 `approved / changes_requested`；模板、schema、rubric 和长背景放入 `references/`，确定性 helper code 只作为 skill-scoped 工具存在。
- 2026-07-05：已修正 Superpowers 流程集成方案中的 skill/code 边界：AI 开发流程必须由 skill 衔接；目标明确、重复、确定性的工具可以作为 skill-scoped helper code 放入 `skills/<skill>/scripts/` 或等价 helper 目录，但必须由 `SKILL.md` 显式声明输入输出、由 `references/` 承载模板/schema/rubric 契约，并有测试与独立 review；禁止的是中心脚本主控和隐藏执行器。
- 2026-07-05：已将 Superpowers 流程集成方案补强为“运动员 / 裁判隔离”硬规则：实现者只能把任务推进到 `ready_for_review`，不得自批 `approved / done`；完成任务必须生成独立 review task 或交给未参与实现的 reviewer 子 agent，review、verification、memory sync 均通过后才允许关闭。
- 2026-07-04：已将 `skills/stratix-nodejs-backend/` 重命名为 `skills/stratix-service/`，并按 `/Users/uroborus/NodeProject/wps/obsync-root` 的当前事实重写 Stratix 后端服务 skill。新版固定 `@stratix/create` 负责 `create-stratix` 初始化、`@stratix/forge` 负责项目内 `stratix` 命令，明确 AI 按业务目标自主选择最小 template/preset/plugin 组合，不再使用旧 `@stratix/cli` 单包叙事或已移除的 tasks preset；同时补齐完整开发循环、插件选择、`.env` / `STRATIX_SENSITIVE_CONFIG` / `STRATIX_ENCRYPTION_KEY` 的开发与生产配置规则。
- 2026-07-04：新增 `docs/04-project-development/05-development-process/superpowers-workflow-integration-plan.md`，把 Superpowers 的计划、开发、评审、验证、收尾链路改造成 shanforge 的 skill-first 状态驱动方案。方案明确 `using-shanforge` 只做轻量会话入口，新增 `project-memory` skill 接管会话恢复、读取范围和 ledger 模板；不再把中心 CLI、`factory-dispatch`、action-registry 或 `scripts/` 作为新流程主控。
- 2026-07-04 补充：`factory-agent-session` 已在方案中明确为迁移来源而非目标入口。其会话卡、相关性判断、ledger 事件模板和 current-state 更新清单应拆入 `skills/project-memory/references/`；具体 work item 计划固定存放到 `.factory/workitems/<WORKITEM-ID>/plan.md`。
- 2026-07-04：已将 `skills/brainstorming/SKILL.md` 按新版流程完整改写为精简中文短句，保留实施前设计门、按需可视化伴侣、规范自审、用户审阅和仅转入 `writing-plans` 的语义；新增 `skills/brainstorming/agents/openai.yaml` 中文名称、描述和默认提示，并用 `tests/test_brainstorming_skill.py` 固定关键中文约束。
- 2026-07-04：按用户要求退役并删除本地 `skills/writer-room/`、`skills/director-room/`、`skills/art-room/`；检查 `/Users/uroborus/.codex/skills` 后未发现这三个 Codex 全局 skill 条目或 symlink。对应的三份专用结构测试已移除，后续不得再把这三个目录视为当前可用 skill。
- 2026-06-14：新增中文 `browser-control` skill，作为本地浏览器控制入口。skill 明确当用户说“用本地浏览器访问 URL / browser-use / 真实浏览器”时优先走本机 `browser-use` CLI，并固定默认用法 `browser-use --headed --session browser-control --json open <URL>` + `state` 回读；同时区分 Codex Browser、Codex Chrome、Computer Use 的适用边界，补齐表单提交、cookies、上传、登录态、权限和敏感数据的动作时确认规则。新增结构测试 `tests/test_browser_control_skill.py`。
- 2026-06-07：已修正 `art-room` 资产目录契约：项目级 `assets/` 只承载全剧复用母资产和全剧 style reference；`character_episode_state_card`、`prop_episode_state_card`、`location_episode_scene_card` 等带 episode 语义的状态卡必须写入 `{episode-id}/assets/characters/`、`{episode-id}/assets/props/`、`{episode-id}/assets/locations/` 或 `{episode-id}/assets/costumes/`。`asset-manifest`、`art-image-prompts`、`asset-index` 与 `thread-plan.output_format_contracts` 已增加 subtype 路径分流约束，避免 `c001e01` 这类分集状态卡落入全局资产目录。
- 2026-06-05：已继续优化 `art-room` 资产生产契约：`art-image-prompts.json` 新增 `copy_ready`，固定 `positive_prompt / negative_prompt / chatgpt_image_prompt / gemini_image_prompt`，便于人工直接复制给图片模型；资产准备、manifest 与 thread plan 新增创建顺序和依赖字段；角色设计 schema 新增 `body_metrics`，物品/服装设计 schema 新增 `physical_dimensions`，用于更稳定地控制身高体态、比例、长宽高、重量感和材质厚度。
- 2026-06-04：已按 `ai-drama-production-skill-system.md` 第一版落地范围修改 `writer-room`、`director-room`、`art-room`：`writer-room` 扩为 series/episode/writeback/migration 模式并补全全剧基础层；`director-room` 补资产前/资产后视频生产、render/QC/edit/audio/post 契约与 schema；`art-room` 补全剧母资产、单集前置资产、角色/物品/场景卡、短文件名、生产元数据/模型可见提示词分层 reference 与 schema。三间房定向测试、skill validator、ruff 与仓库全量回归均已通过。
- 2026-06-04：已将 AI 剧制作三 skill 协作方案中的 skill 修改开发计划细化为阶段化实施路线：先冻结方案契约，再分别修改 `writer-room` 全剧基础/写回、`director-room` 资产前分镜与资产后视频生产、`art-room` 资产卡与图像提示词、`director-room` AI 配音与后期，最后用端到端试点验证共享目录、文件交接、提示词结构、资产引用和音频引用链。
- 2026-06-04：已清理 AI 剧方案中的提示词结构，正式区分“生产元数据”和“模型可见提示词”，模型正文不再包含输出文件短名、shot_id、generation_method、asset_id、episode_id 等流程字段；AI 配音方案改为按对白句/旁白句/声音事件管理，不按每个分镜逐条配音，并加入 3 分钟视频的音频工作量估算与弱口型绑定策略。
- 2026-06-04：已将 AI 剧制作三 skill 协作方案融合 AI 配音与后期建议：明确不推荐视频模型直接生成精准对白，推荐“静音/弱口型视频 -> AI TTS 对白 -> 字幕对齐 -> 可选口型同步 -> 后期合成”的简单可操作流程；新增 `audio/voice-bible.md`、`audio/dialogue-plan.json`、`audio/audio-manifest.json`、`audio/audio-qc.md` 与音频短文件名规则，并把文件名规则收紧为不含扩展名不超过 20 字符。
- 2026-06-04：已将 AI 剧制作三 skill 协作方案重编为完整清稿，统一采用“资产前导演分镜包”和“资产后视频生产包”；新增方案修订规范，明确后续方案更新必须重编成连贯正文并审核术语、owner、交接链和修改方式，不以补丁段落堆叠。
- 2026-06-04：已补充 AI 剧制作三 skill 协作方案的正式文件修改规则与环节交接清单，固定 canonical path 原地修复、草稿可版本化、图片/视频采用 canonical + history/manifest、handoff lock 后由 owner skill 修复并触发下游刷新；同时按全剧基础、母资产、单集剧本、资产前导演分镜包、前置资产、资产后视频生产包、渲染、QC、剪辑、后期、经验写回固定每一环节生成文件和下一环节读取文件。
- 2026-06-04：新增 `docs/04-project-development/04-design/ai-drama-production-skill-system.md`，将 AI 剧制作流程收口为同一 `project/{project-name}/` 下的 `writer-room`、`director-room`、`art-room` 三 skill 协作方案；第一版明确全剧 bible、全剧母资产、单集分镜、单集前置资产、ComfyUI 渲染、QC/剪辑/后期与写回经验的目录 owner、创建时机和修复边界，后续再逐项细化文件名、schema 与资产提示词格式。
- 2026-06-04：已将独立 `prompt-room` 合并进 `director-room`，现在统一由 `director-room` 从 `./project/{project-name}/{episode-id}` 剧本包直接生成分镜、shot list、visual continuity、generation plan、双语 shot prompt draft 与双语 ComfyUI-ready prompt pack；全局 `prompt-room` symlink 已移除，`art-room` 后续 handoff 改为回到 `director-room` prompt refresh。相关 skill validator 通过，三间房定向回归 `23 passed`。
- 2026-06-03：已将本地 `skills/prompt-room/` 链接为 Codex 全局 skill：`/Users/uroborus/.codex/skills/prompt-room -> /Users/uroborus/AiProject/shanforge/skills/prompt-room`；`prompt-room` skill 结构校验通过。
- 2026-06-01：已把 `writer-room`、`director-room`、`art-room` 的文档契约统一调整为仓库根目录下 `./project/{project-name}/`，跨集资料固定在项目根，单集产物固定在 `{episode-id}/`（如 `01/`、`02/`）下；本轮 skill validator 三项通过，相关定向回归 `18 passed`。
- 2026-06-01：新增 Codex-native `prompt-room` skill，作为提示词工程部从共享生产项目根读取最终剧本、导演分镜产物与美术资产包，产出 `prompts/comfyui-prompt-brief.md`、`prompts/comfyui-style-preset.json`、`prompts/comfyui-asset-prompt-pack.json`、`prompts/comfyui-shot-prompts.json`、`prompts/comfyui-workflow-plan.json`、`prompts/comfyui-tuning-log.json` 和 `reports/comfyui-prompt-qc.md`；调优边界固定为根据 `production/comfyui-feedback.json`、`reports/comfyui-render-review.md` 或 `comfyui/renders/` 的反馈做最小有效 prompt/参数修订，prompt-room 不直接执行 ComfyUI。新增 `tests/test_prompt_room_skill.py`，定向回归 `6 passed`。
- 2026-06-01：新增 Codex-native `art-room` skill，作为美术资产部从 `./project/{project-name}/` 读取项目级 bibles 与 `{episode-id}/` 下的导演分镜产物，产出 `{episode-id}/art/asset-manifest.json`、角色/场景/道具服装设计 JSON、`{episode-id}/art/style-continuity-bible.json`、`{episode-id}/prompts/art-image-prompts.json`、`{episode-id}/art/thread-plan.json`、`{episode-id}/art/thread-results.json`、`{episode-id}/art/asset-index.json`、项目级共享 `assets/` 图片目录与单集 `{episode-id}/assets/` 参考目录；图片生产边界固定为父协调器使用 `codex_app.create_thread/read_thread/send_message_to_thread` 调用后台 Codex thread 分批生成，规划 agent 不直接生成图片。新增 `tests/test_art_room_skill.py`，定向回归 `6 passed`。
- 2026-06-01：新增并调整 Codex-native `director-room` / `writer-room` 项目目录契约：`writer-room` 默认写入 `./project/{project-name}/`，跨集资料固定为 `outline/series-outline.md`、`synopsis/story-synopsis.md`、`bible/characters.md`、`bible/scenes.md`、`bible/continuity.md`，单集最终剧本固定为 `{episode-id}/script/final-script.md`，并同步产出 `{episode-id}/reports/continuity-report.md`、`{episode-id}/reports/script-score.md`；`director-room` 固定从同一项目根读取这些标准文件，再输出 scene breakdown、shot list、storyboard plan、visual continuity bible、generation plan 与 shot prompt draft，并明确故事场景设计不承担导演分镜职责。相关 skill 回归 `12 passed`。
- 2026-05-31：新增 Codex-native `writer-room` skill，将编剧室实现为“主 Codex 调度 + 多个 Codex 子 agent 分工产出剧本”的工作流；首版包含 11 个角色任务卡、artifact envelope、评分 rubric、输出模板和 schema，并把自我进化限定为先生成 reviewable evolution notes，禁止未审批自改 skill。
- 2026-04-21：已继续把 `MG-WP-004` 推进到 reviewer guidance：`MemoryLifecycleQueueItem` 现会直接投影 `resolution_required`、推荐 `resolution_options` 与建议 note 模板，domain 会按 `conflict_superseded / decay_expired / manual_override_applied` 等 reason 给出推荐人工结论。相关专项回归 `20 passed`。
- 2026-04-21：已继续把 `MG-WP-004` 推进到更明确的 audit read model：`MemoryLifecycleAuditFilter` 新增 `latest_per_record_only`，`lifecycle_audit_summary.latest_entries` 已改成真正的最新优先，并额外投影 `latest_by_record`，可直接回答每条 memory 最近一次人工处理结果。相关专项回归 `25 passed`。
- 2026-04-21：已继续把 `MG-WP-004` 推进到 reviewer resolution taxonomy：`update_lifecycle_queue(..., resolution=...)` 现可显式持久化人工 review resolution，`reopen_lifecycle_queue(...)` 回到 `pending` 时会清空 resolution；`load_lifecycle_audit(...)` 也已支持按 `queue_review_status / resolution` 过滤，`lifecycle_queue_summary / lifecycle_audit_summary` 现会稳定输出 `resolution_counts`。相关小范围回归 `20 passed`。
- 2026-04-21：已继续把 `MG-WP-004` 推进到 queue 运维批量面：`update_lifecycle_queue(...)` 与 `reopen_lifecycle_queue(...)` 现都支持 `queue_filter` 驱动的 batch review selection；其中 review 路径按 filter 命中 queue item 全集，而 `apply_lifecycle(...)` 仍保留默认选中子集语义。相关定向回归 `38 passed`。
- 2026-04-20：已继续把 `MG-WP-004` 推进到显式 review workflow：新增 `reopen_lifecycle_queue(...)`，并把同状态 note update 拆成独立 `review_note_updated` 审计动作；当前 lifecycle governance 已能区分 `review_status_updated / review_note_updated / review_reopened / lifecycle_applied` 四类人工治理事件。相关受影响主链回归 `61 passed`。
- 2026-04-20：已继续把 `MG-WP-004` 推进到 durable lifecycle audit trail：新增 `MemoryLifecycleAuditRepositoryPort`、`load_lifecycle_audit(...)` 与 `JsonlMemoryLifecycleAuditStore`，`update_lifecycle_queue()` 和 `apply_lifecycle()` 现会留下 `review_status_updated / lifecycle_applied` 审计轨迹，`explain_session_memory()` 也开始稳定投影 `lifecycle_audit_summary`；相关受影响主链回归 `61 passed`。
- 2026-04-20：已继续把 `MG-WP-004` 推进到 durable lifecycle review queue：新增 `MemoryLifecycleQueueRepositoryPort`、`update_lifecycle_queue(...)` 与 `JsonlMemoryLifecycleQueueStore`，`load_lifecycle_queue()` 默认只回 `pending` actionable items，`apply_lifecycle()` 成功后会把 queue item 标记为 `applied`；相关受影响主链回归 `61 passed`。
- 2026-04-20：已继续把 `MG-WP-004` 推进到 provider-aware lifecycle writeback：`MemoryProviderGovernanceDecision` 现已新增 `allow_lifecycle_writeback`，`MemoryProviderPort / MemoryProviderManagerPort` 新增 `on_lifecycle_apply(...)` 通道，`DefaultMemoryDomainService.apply_lifecycle()` 会在本地 durable 更新后按领域治理继续同步 `jsonl / remote_http` provider，并把刷新后的 provider diagnostics / assembly manifest 保存回 session ledger；相关定向回归 `45 passed`。
- 2026-04-19：已继续把 `MG-WP-004` 推进到最小 review queue 闭环：`MemoryDomainService` 现已新增 `load_lifecycle_queue(queue_filter)`，`apply_lifecycle()` 也支持按 queue filter 做批量选择；`application.memory.MemoryGovernanceService` 与 `MemoryAPI` 已暴露对应 entrypoint，`explain_session_memory()` 也开始稳定投影 `lifecycle_queue_summary`。相关专项回归 `18 passed`。
- 2026-04-19：已继续把 `MG-WP-004` 推进到最小 apply 闭环：`MemoryDomainService` 现已新增 `review_lifecycle / apply_lifecycle`，`application.memory.MemoryGovernanceService` 与 `MemoryAPI` 也已暴露对应 entrypoint；当前可对 session scope 下的 lifecycle review queue 做人工 apply 并 durable 写回 memory store。相关记忆主链回归 `64 passed`。
- 2026-04-19：已继续完成 `MG-WP-004` 的首轮收口：`MemoryLifecyclePolicy` 现已支持 conflict supersede、forced manual override 与 metadata-driven decay forget；`DefaultMemoryDomainService.explain_session_memory()` 也已开始投影 scoped `lifecycle_evaluations`，可解释 memory 为什么 retained / superseded / forgotten。相关记忆主链回归 `52 passed`。
- 2026-04-19：已继续完成 `MG-WP-005` 的首轮收口：新增 `tests/test_memory_governance_regression.py`，将 `TC-013 ~ TC-016` 收口为独立记忆治理专项回归入口；同时把 `MemoryStatus.FORGOTTEN` 纳入默认 lifecycle state machine，并让 `DefaultMemoryDomainService.explain_session_memory()` 稳定输出 `recalled_memory_statuses / promotion_reasons / promotion_decisions / memory_provider_binding / recall_plan`。相关专项回归 `10 passed`。
- 2026-04-19：已继续完成 `MG-WP-002 / MG-WP-003` 的首轮收口：`RecallPlan` 现已冻结 `within_scope_order / overflow_order / overflow_fill_enabled` 这组显式排序指令，`DefaultRecallRanker` 与 `DefaultMemoryDomainService.preview_recall()` 都按 plan 执行同一套 bucket/overflow 排序；同时 `MemoryProviderManagerPort` 与 `DefaultMemoryProviderManager` 已改为直接消费 `MemoryProviderGovernanceDecision`，manager 本身不再持有 writable / shared-write 业务门槛；新增对应回归后，记忆主链相关回归 `55 passed`。
- 2026-04-19：已完成 `MG-WP-001` 首轮实现：新增 `src/domain/memory/governance.py`，显式建模 `RecallGovernancePolicy / MemoryProviderGovernancePolicy / MemoryLifecyclePolicy` 及其 decision 对象；`DefaultMemoryDomainService` 现已先生成 recall/provider 领域决策，再分别驱动 `RecallPlannerPort` 和 provider manager，默认 planner 也已改为只把 `RecallGovernanceDecision` 物化成带预算的 `RecallPlan`；新增 `tests/test_domain_memory_governance.py`，覆盖治理对象默认行为和 service 消费点。
- 2026-04-19：已新增 [memory-governance-implementation-plan.md](../../docs/04-project-development/05-development-process/memory-governance-implementation-plan.md)，把记忆治理专项从设计下沉到实施层，明确 `MG-WP-001 ~ MG-WP-005` 对已有 `TASK-007 / TASK-016 / TASK-017 / TASK-019` 的细化关系，并补齐专项测试方向 `TC-013 ~ TC-016`。
- 2026-04-19：已新增 [memory-governance-design.md](../../docs/04-project-development/04-design/memory-governance-design.md)，正式收口“记忆领域只负责治理决策，runtime/settings 只负责执行”的专项口径；文档已把 recall / promotion / lifecycle / provider / explainability 五类治理语义统一归入 `domain.memory`，并明确后续 planner/ranker/provider_manager 的降格方向。
- 2026-04-18：已清理记忆迁移债：`src/runtime/memory/runtime.py` 与 `src/runtime/memory/policy.py` 已退役，`src/runtime/memory/__init__.py` 不再导出 legacy owner；原 `tests/test_memory_runtime.py` 的现役覆盖已拆分迁入 `tests/test_domain_services.py`、`tests/test_runtime_memory_summarizer.py` 与 `tests/test_settings_memory_stores.py`，正式口径只保留 domain owner + runtime 支撑组件。
- 任务：`TASK-017` 已补齐剩余 runtime 支撑能力并转入更深 backend 治理；`TASK-016` 已从 explainability 查询面继续推进到 recall 治理拆分与 `preview_recall` 独立治理接口；`TASK-020` 已把 external `memory_provider` 推进到 durable `jsonl / jsonl_vector / remote_http` backend/source，并继续把 `remote_http` 的 response schema contract、secret rotation audit、writeback 成功/失败读模型、统一 durable secret governance provider，以及 request governance metadata resolver 收口进 settings 治理面。
- 变更：`src/runtime/session_search` 已通过 query adapter 正式接入 archive / transcript / assembly explain 查询面；`src/runtime/profile_source`、`src/runtime/rule_source`、`src/runtime/clock_identity` 已从 scaffold 落为正式实现并接入 session/memory 主链；`src/access/api/*` 已统一回收到 access-owned use case 协议，`src/access/cli/main.py` 已改成注入式 CLI gateway，`scripts/shanforge-cli` 负责本地 demo 容器装配；`src/settings/delegation/digest_store.py` 已补 child digest 持久化；`src/settings/catalog.py` 已固化基础设置层功能清单；workspace `profiles.json`、专门 `backend-bindings.json`、`provider-bindings.json` 与 profile-specific override 现已成为默认容器的正式装配来源；`settings/composition/provider_manager.py` 现会基于 provider readiness 选择默认 `llm_provider / model`，并在 provider 不可运行时回退到可用 provider；`ExecutionService` 现会冻结 `backend_ids / selected_model_binding` 到 session context，`CapabilityExecutor` 会把 step 级真实模型调用回写到 `SessionAssemblyManifest.model_bindings`；默认容器现也会把 `capability_registry / approval_policy / delegation_transport` 的 backend 选择、Hermes contract metadata、`binding_source / source_path` 与 fallback `requested_binding_id` 写入 `backend_binding_metadata`；external `MemoryProviderPort + provider_manager` 现已进入主链，`SessionAssemblyManifest` 会冻结 `memory_provider_binding`，session context 会注入经过 sanitize 和 read-only fence 的 `external_memory_recall_block`；`DefaultMemoryDomainService` 的 recall 链现已改为 `RecallGovernancePolicy -> RecallPlannerPort -> scan_memory_records() -> RecallRankerPort`，并支持 `memory_provider:jsonl / jsonl_vector / remote_http` 作为 external augmentation backend/source；同时 `RecallPlan` 现会冻结显式 `within_scope_order / overflow_order / overflow_fill_enabled` 排序指令，`DefaultRecallRanker` 与 preview explainability 都按同一计划执行排序；`MemoryProviderManagerPort` 与 `DefaultMemoryProviderManager` 也已改为直接消费 `MemoryProviderGovernanceDecision`，manager 自身不再判断 writable / shared-write gate，只负责 provider lifecycle、sanitize fence 与 diagnostics；lifecycle queue/audit 的 durable store 现在也已完整持久化 `review_resolution`，并支持跨 container reopen 后清空 resolution。其余 `remote_http`、trace-first diagnostics 与 durable secret governance 口径保持不变。
- 缺陷：无
- 2026-04-16：已将反射 / DI 技术内核外置为 sibling 项目 `shanforge-di`；`src/settings/composition/` 现只保留 `settings.py`、`container.py` 与 `component_bindings.py`，负责本地业务绑定和默认容器装配。
- 2026-04-16：`shanforge` 已接入 `shanforge-di` 作为正式依赖，默认容器现通过外部 `ServiceContainer` 选择 `llm_provider / memory_store / evidence_store / memory_dataset_store / capability_registry / approval_policy / delegation_transport` 的具体实现；原仓内 `contracts / errors / factories / loader / manifest / registry / resolver` 已移除，相关回归测试已改为集成 `shanforge-di` 的绑定层验证。
- 2026-04-16：已把 `v2` 的分层、接口 owner、`src/settings/` 归并边界、Hermes 复用规则与禁止耦合清单固化到 `AGENTS.md`，作为后续所有开发子 agent 的强制协作与编码规范入口。
- 2026-04-15：已系统重构 `docs/04-project-development/04-design/` 的主入口与专题页；`solution-overview.md`、`backend-design.md`、`database-design.md`、`deployment-architecture.md`、`frontend-adapters-and-multi-agent-coordination.md`、`action-registry-and-autonomy-policy.md` 已统一到“六层架构 + consumer-owned ports + domain owner”口径，并同步修正 draw.io 关键标签。
- 2026-04-15：已重写 [basic-capability-layer-design.md](../../docs/04-project-development/04-design/basic-capability-layer-design.md) 为 `C` 纯自研重写路线：先把 `file / web / terminal / browser / session_search / skills` 的能力包骨架、类型对象和函数签名定义完整，再在具体函数实现阶段选择性复用 Hermes 代码。
- 2026-04-15：`src/runtime/` 已新增基础能力层骨架模块：`file_access`、`web_access`、`terminal`、`browser`、`session_search`、`skills`、`rule_source`、`profile_source`、`clock_identity`；`src/settings/composition/container.py` 已把这些服务对象装配进默认容器；新增 `tests/test_basic_capability_scaffold.py` 验证骨架存在且容器可构建。
- 2026-04-15：基础能力层骨架已进一步补齐“能力包目录事实源”：新增 `CapabilityPackageDescriptor` / `CapabilityPackageRegistry`，每个 service 现在都能声明自己的 package metadata、操作清单和 provider 依赖，默认容器可统一枚举全部基础能力骨架包。
- 2026-04-16：`TASK-017` 已进入首轮实现：新增 `LocalWorkspaceProvider`、`LocalSkillCatalogProvider`、`InMemorySessionArchiveProvider` / `InMemorySearchIndexProvider`，`FileAccessService`、`SkillCatalogService`、`SessionSearchService` 已具备本地最小可用行为，默认容器现在可直接提供 `file / skills / session_search` 的第一批真实能力。
- 2026-04-16：本轮实现按函数级复用 Hermes 思路收口：文件侧吸收路径越界与设备文件防护思路，skills 侧吸收 frontmatter 解析与目录发现思路，session search 侧吸收 recent mode、query window truncation 与 current lineage 排除思路；但运行时架构仍保持 `runtime/capability + ports + settings` 自研边界。
- 2026-04-16：已完成基础设置层归并重构：原先分散的 provider、store、bridge、container 正式实现代码已统一迁入 `src/settings/`，并按 `model / memory / session / skills / workspace / approval / delegation / gateway / capability_registry / hermes` 分域，`composition / shared` 负责装配与共享支撑。
- 2026-04-16：已补齐 `web_access / terminal / browser` 的首轮 local bridge：`web` 支持 `search / fetch / extract / citation normalize`，`terminal` 支持 `run / stream / git / writeset audit` 并挂在 `approval + sandbox` 后，`browser` 支持 `open / inspect / click / type / wait / screenshot` 的最小会话化能力。
- 2026-04-16：已完成本轮回归收口：新增 `tests/test_runtime_execution_bridges.py` 覆盖 action-plane bridge，仓库全量 `uv run pytest` 通过 `137 passed`；`uv lock --check` 与 `git diff --check` 通过，`datetime.utcnow()` 弃用告警已清理。
- 2026-04-16：`TASK-016` 首轮已落地：新增 `SessionArchiveHit` / `SessionTranscriptSlice` / `SessionAssemblyManifest` 读模型，`MemoryAssemblyQueryPort`、`SessionInspectionService`、`MemoryAPI` 与 `SessionSearchQueryAdapter` 已接上默认容器；`prepare_session` 现在会把 assembly snapshot 持久化到 session context 与专门 `SessionAssemblyStorePort`，archive explain 优先读取专门 store。
- 2026-04-16：新增 `src/settings/session/assembly_store.py`，提供 `InMemorySessionAssemblyStore / JsonlSessionAssemblyStore`；默认容器现已暴露 `assembly_store` 并把它接到 `DefaultMemoryDomainService` 与 `InMemorySessionArchiveProvider`。
- 2026-04-16：扩充 `tests/test_session_inspection_service.py`、`tests/test_domain_services.py` 与 `tests/test_platform_scaffold.py` 的 assembly snapshot 断言；仓库全量 `uv run pytest` 通过 `139 passed`。
- 2026-04-16：已完成 access 层架构清理：`AgentAppAPI`、`WorkflowAPI`、`RuntimeAPI`、`MemoryAPI` 现都依赖 `src/access/ports/application_use_cases.py` 中的 access-owned 协议；`src/access/cli/main.py` 不再直接 import `settings.composition`，本地 launcher 迁移到 `scripts/shanforge-cli`。
- 2026-04-16：`TASK-016` 的 child digest 读模型已落地：新增 `src/domain/session/delegation_models.py` 定义 `SubAgentDigest`，`src/settings/delegation/digest_store.py` 提供 `InMemory/JsonlDelegationDigestStore`，`DefaultMemoryDomainService.prepare_session` 现会把 child digests 合并进 `SessionAssemblyManifest` 并通过 inspection explain 暴露出来。
- 2026-04-16：已完成 `src/runtime/` 剩余 scaffold 收口：`ProfileSourceService`、`RuleSourceService`、`ClockIdentityService` 现均具备本地可运行实现；新增 runtime-to-domain 适配器把 profile/rule/skills 接入 `DefaultMemoryDomainService`，把时钟/ID 接入 `DefaultSessionDomainService`；默认容器已移除静态 `project_scope_key="shanforge"`。
- 2026-04-16：新增 `src/settings/workspace/source_provider.py` 与 `src/settings/shared/system_identity.py`，提供 local `profile_source` / `rule_source` / `clock` / `id_generator` provider；新增 `tests/test_runtime_support_services.py` 与 `tests/test_platform_scaffold.py` 断言 runtime 支撑能力进入 session assembly 主链；仓库全量 `uv run pytest` 通过 `146 passed`，`uv run ruff check` 通过。
- 2026-04-16：已补齐基础设置层骨架事实源：新增 `src/settings/catalog.py` 作为功能域/能力清单入口；新增 `src/settings/model/embedding_provider.py`、`src/settings/gateway/http_client.py`、`src/settings/session/blob_store.py`、`src/settings/session/search_index.py`、`src/settings/session/vector_index.py`；`component_bindings.py` 现已注册 `embedding_provider / http_client / blob_store / search_index / vector_index` skeleton families。
- 2026-04-16：新增 `src/settings/workspace/profile_catalog.py`，`LocalProfileSourceProvider` 现可从 workspace profile catalog 解析 `default_profile_id`、`default_model` 与 `backend_ids`；`LocalRuleSourceProvider` 现支持 profile-specific `rule-bundle.json`；默认容器已按 profile 选择 `llm_provider` 与 memory/evidence/dataset backend，并在需要时落到 profile-scoped JSONL root。
- 2026-04-16：`SessionAssemblyManifest` 现已补齐结构化 explainability 投影：新增 `selected_model`、`model_bindings` 与 `backend_bindings`；`ExecutionService` 会把默认 `provider/backend/model` 绑定冻结到 session context，`CapabilityExecutor` 会记录每个 prompt step 的真实模型调用，inspection explain 现可同时回答“默认怎么装配”和“实际怎么执行”。
- 2026-04-16：已完成 Hermes-backed governance adapter 契约收口：`HermesCapabilityRegistryAdapter`、`HermesApprovalPolicyAdapter`、`HermesDelegationTransportAdapter` 现统一暴露 `contract_metadata()`；默认容器会把 `capability_registry / approval_policy / delegation_transport` 的选择与 bridge metadata 冻结进 session context，并通过 `SessionAssemblyManifest.backend_bindings` 对外解释。
- 2026-04-16：已完成 backend 来源治理首轮收口：新增 `src/settings/workspace/backend_catalog.py`，`LocalProfileSourceProvider` 现会合并 workspace `backend-bindings.json` 与 profile-specific backend override；默认容器会按专门 backend source 选择 governance adapters，并在 Hermes bridge 不可用时回退到 local，同时把 `binding_source / source_path / requested_binding_id` 写入 `SessionAssemblyManifest.backend_bindings`。
- 2026-04-17：已完成 provider 来源治理首轮收口：新增 `src/settings/workspace/provider_catalog.py` 与 `src/settings/composition/provider_manager.py`，`LocalProfileSourceProvider` 现会合并 workspace `provider-bindings.json` 与 profile-specific provider override；默认容器会只选择 `contract_ready` 的 provider，并在 provider 不可运行时回退到可用 provider，同时把 `requested_provider_id` 和来源元数据保留在 `SessionAssemblyManifest.selected_model`，不再被 step 级 `model_bindings` 覆盖。
- 2026-04-17：已完成 external `MemoryProviderPort + provider_manager` 首轮落地：新增 `src/domain/memory/assembly_models.py` 定义 `MemoryProviderBinding`，`src/domain/memory/ports.py` 新增 `MemoryProviderPort / MemoryProviderManagerPort`，`src/runtime/memory/provider_manager.py` 负责 single-provider lifecycle、read-only recall fence 和 child digest 默认无 shared write capability 的治理，`src/settings/memory/provider.py` 提供 `none / in_memory` 基础设置实现；默认容器现会从 `backend-bindings.json` 选择 `memory_provider`，并把 `memory_provider_binding` 与 `external_memory_recall_block` 投影到 session assembly explain。
- 2026-04-17：已完成 recall 治理拆分与 durable memory provider/source 首轮增强：`src/domain/memory/ports.py` 新增 `RecallPlannerPort / RecallRankerPort`，`src/runtime/memory/recall_planner.py` 与 `src/runtime/memory/recall_ranker.py` 已落地默认实现；`DefaultMemoryDomainService` 现通过 `scan_memory_records()` 拉取候选，再由 ranker 做预算和排序，不再把 recall 预算、排序与 augmentation 混在 store 查询里；`src/settings/memory/provider.py` 新增 `JsonlAugmentationMemoryProvider`，默认容器可通过 `memory_provider:jsonl` 把 provider-owned snapshot/turn/digest state 落到 profile-scoped JSONL root，并继续把 binding/source metadata 投影到 assembly explain。
- 2026-04-17：已完成 `preview_recall` 首轮落地：新增 `RecallPreview` 读模型与 `application/memory/inspection_service.py`，`MemoryInspectionUseCase` 和 `MemoryDomainService` 现已显式暴露 `preview_recall`；`DefaultMemoryDomainService.preview_recall()` 会基于冻结的 `SessionAssemblyManifest`、已保存的 `external_memory_recall_block` 与当前 memory store 状态生成只读 preview，不触发新的 provider 写副作用；`MemoryAPI` 现聚合 session inspection 与 memory preview 两条 use case。
- 2026-04-17：已完成 `preview_recall` 第二轮 explainability 增强：`RecallPreview` 新增 `scope_breakdowns`、`record_rankings` 与 `augmentation_preview`；`DefaultMemoryDomainService.preview_recall()` 现会显式解释 per-scope budget、overflow 候选排序轨迹，以及 `external_memory_recall_block` 的 provider/source provenance；对应测试已补齐并保持仓库全量 `164 passed`。
- 2026-04-17：已完成 remote/vector 路线的本地 durable 首轮落地：`RecallQuery` 现携带 `query_text`，`DefaultMemoryProviderManager` 会合并 provider `contract_metadata()` 与可选 `prefetch_diagnostics()`；`src/settings/memory/provider.py` 新增 `JsonlVectorAugmentationMemoryProvider`，默认容器可通过 `memory_provider:jsonl_vector` 基于 provider-owned snapshot / turn / digest state 做向量式 rank/prefetch；`preview_recall` 现可从 stored diagnostics 回读 `retrieval_kind / hit_count / hit_ids / source_breakdown`；仓库全量 `166 passed`。
- 2026-04-17：已完成 remote bridge 首轮落地：`src/settings/gateway/http_client.py` 现支持 `file://` transport 作为 settings-layer 本地测试桥，`src/settings/memory/provider.py` 新增 `RemoteAugmentationMemoryProvider`，默认容器可通过 `memory_provider:remote_http` 拉取远端 recall block 与 hits；`preview_recall` 现可回读 `endpoint_url` 与 `retrieval_kind=remote_http`；仓库全量 `167 passed`。
- 2026-04-17：已把 `remote_http` 推进到真实网络 transport：`LocalHttpClientProvider` 现支持 `file:// + http(s)` JSON transport，`RemoteAugmentationMemoryProvider` 已支持 `sync_endpoint_url / session_end_endpoint_url / delegation_endpoint_url` 三条可选远端写回；`contract_metadata()` 现显式声明 `writeback_supported=true`，`preview_recall` 可回读 `writeback_enabled`；仓库全量仍为 `167 passed`。
- 2026-04-17：已完成 `remote_http` 的治理增强：`LocalHttpClientProvider` 现支持 `request options -> headers / bearer_token(_env) / retry_status_codes / max_retries / timeout_seconds`，并记录 transport report；`RemoteAugmentationMemoryProvider` 现会把这些治理元数据与 `writeback_reports` 回灌到 `prefetch_diagnostics()`，`DefaultMemoryDomainService` 会把更新后的 provider diagnostics 回写进 session context 与 assembly manifest；仓库全量 `169 passed`。
- 2026-04-17：已完成 `remote_http` 的下一轮治理增强：`settings/workspace/backend_catalog.py` 现支持 `backend_binding_metadata.metadata_file` 外置 durable source；`LocalHttpClientProvider` 现支持 `signature_secret(_env) / signature_key_id` 的 `hmac-sha256` 签名头；`RemoteAugmentationMemoryProvider` 现支持 `*_failure_policy=record|ignore|raise`，并把写回失败收口到 `writeback_reports`；仓库全量 `170 passed`。
- 2026-04-17：已完成 `remote_http` 的又一轮治理增强：`settings/workspace/backend_catalog.py` 现会把 `metadata_file` 内联的相对 `*_file` 路径解析到稳定绝对路径，`LocalHttpClientProvider` 现支持 `bearer_token_file / signature_secret_file` 并对 `hmac-sha256` 采用 `method + path + query + body_sha256 + timestamp` 的 canonical 签名串；`RemoteAugmentationMemoryProvider` 现支持 `prefetch_response_validation=record|ignore|raise`，并把 `response_validation_error` 投影进 preview explainability；仓库全量 `171 passed`。
- 2026-04-17：已完成 `remote_http` 的再下一轮治理增强：`RemoteAugmentationMemoryProvider` 现支持 `secret_catalog_file`，可从 durable secret catalog 里解析 `default_signature_key_id / signature_keys / default_bearer_token_id / bearer_tokens`，把 key rotation 和 bearer/signature secret 选择收口在 settings 层；`sync/session-end/delegation` 现支持 `*_response_validation`，并把写回响应校验失败收口到 `writeback_reports[*].response_validation_error`；`LocalHttpClientProvider` 的 transport report 现可回读 `signature_key_id / bearer_token_id / secret_catalog_source_path`；仓库全量 `173 passed`。
- 2026-04-17：已继续收口 `remote_http` 治理面：`RemoteAugmentationMemoryProvider` 现会把 `remote_memory_prefetch_v1 / remote_memory_writeback_ack_v1` 作为内建 response contract 投影进预览诊断与 `writeback_reports`，并把 `response_contract / response_contract_source / response_keys` 固化为 explainability 字段；`LocalHttpClientProvider` 现可回读 `signature_key_selection_source / bearer_token_selection_source`，形成 secret rotation audit；`writeback_reports` 也已稳定回读 `request_kind / response_ok / response_status / response_message / response_report_id`；仓库全量 `174 passed`。
- 2026-04-17：已把 durable secret governance provider 从 `RemoteAugmentationMemoryProvider` 中抽离：新增 `src/settings/workspace/secret_catalog.py` 的 `LocalSecretCatalogProvider`，统一负责 `secret_catalog_file` 的加载、相对路径解析、key rotation 选择与 selection-source 审计，并补回 metadata-only `signature_key_id / bearer_token_id` fallback；`component_bindings.py` 与 `src/settings/catalog.py` 已同步注册 `secret_catalog` family/capability，相关专项回归 `23 passed`，`uv run ruff check` 通过。
- 2026-04-17：已继续减少 `remote_http` legacy metadata 开关：新增 `src/settings/memory/remote_http_metadata.py` 的 `RemoteHttpMetadataResolver`，统一 canonical `bearer_token*`、legacy `auth_bearer_token*` alias fallback，以及 prefetch/writeback validation/failure policy 的解析；`RemoteAugmentationMemoryProvider` 现只消费该 resolver 输出的 canonical request options，相关专项回归 `24 passed`，`uv run ruff check` 通过。
- 2026-04-17：已继续扩展 `remote_http` metadata resolver：`RemoteHttpMetadataResolver` 现进一步统一 `recall_endpoint_url / sync_endpoint_url / session_end_endpoint_url / delegation_endpoint_url`、`recall_response_contract / sync_response_contract / session_end_response_contract / delegation_response_contract`，并兼容 legacy `endpoint_url / prefetch_response_contract / writeback_response_contract` fallback；新增容器级回归验证 canonical 键会覆盖 legacy alias，专项回归 `22 passed`，仓库全量 `uv run pytest` 通过 `176 passed`。
- 2026-04-17：已把 `remote_http` metadata resolver 提升成 request governance 读模型：新增 `RemoteHttpRequestGovernance`，统一承载 `recall/sync/session_end/delegation` 的 endpoint、request_options、response_contract、response_validation_mode 与 failure_policy；`RemoteAugmentationMemoryProvider` 现直接消费 governance 读模型，不再保留分散的 metadata wrapper；新增 resolver 直测与容器级回归验证 canonical `recall_response_validation / sync_failure_policy` 会覆盖 legacy alias，专项回归 `24 passed`，仓库全量 `uv run pytest` 通过 `178 passed`。
- 2026-04-17：已继续推进跨 backend explainability 一致性：`JsonlAugmentationMemoryProvider`、`JsonlVectorAugmentationMemoryProvider` 与 `RemoteAugmentationMemoryProvider` 现统一投影 `query_terms / source_breakdown / result_truncated / budget_trace / rank_trace / hit_provenance / contract_trace / access_trace / writeback_trace`，`DefaultMemoryProviderManager` 会从 trace 统一回填 legacy 顶层 diagnostics；上一轮已新增 `src/domain/memory/augmentation_diagnostics.py`，让 `DefaultMemoryDomainService.preview_recall()` 在回读冻结 session/manifest diagnostics 时也复用同一套 trace-first normalizer，本轮则继续把 preview diagnostics 完全收口成 canonical trace-first 字段，不再输出 `legacy_aliases`，并把 transport auth、retry/timeout、secret selection、prefetch validation 和 writeback policy/error 摘要继续压进统一 trace；相关专项回归 `31 passed`，仓库全量 `uv run pytest` 通过 `181 passed`。
- 2026-04-15：已把主执行链正式改为 `ExecutionService -> SessionDomainService / MemoryDomainService -> domain ports -> settings/runtime capability`；基础设置层实现现在承接领域层仓储端口与 provider 端口，默认容器不再把记忆业务主链建立在 legacy runtime owner 上，并新增 `tests/test_application_execution.py` 验证应用层只做薄编排。
- 2026-04-15：已新增 [layered-domain-interface-catalog.md](../../docs/04-project-development/04-design/layered-domain-interface-catalog.md)，把六层架构细化为“层 -> 领域 -> 接口 owner -> 下行依赖”总表，并先落 access/application/domain/runtime 的接口骨架，不做实现。
- 2026-04-15：已按“用户界面层 / 接口网关层 / 业务调度层 / 业务模型层 / 基础能力层 / 基础设置层”统一重构主架构文档；正式收口“`src/settings/` 属于基础设置层实现分区，不是额外层次”。
- 2026-04-15：已把基础设施层第一批正式代码骨架落地到 `src/`：新增 `domain/approval`、`domain/delegation`、`domain/gateway`，补齐 `ApprovalPolicyPort`、`SandboxPolicyPort`、`DelegationTransportPort`、`GatewayPort`，并加入 Hermes-backed adapter scaffold 与容器开关。
- 2026-04-15：已吸收 `/Users/uroborus/AiProject/hermes-agent` 的记忆系统设计精华，更新 [memory-system-detailed-design.md](../../docs/04-project-development/04-design/memory-system-detailed-design.md)、[memory-runtime-interfaces.md](../../docs/04-project-development/04-design/memory-runtime-interfaces.md) 与 [hermes-agent-source-analysis-report.md](../../docs/04-project-development/02-discovery/hermes-agent-source-analysis-report.md)，正式补齐 provider manager、archive query、snapshot policy 与 Hermes 可复用能力判断。
- 2026-04-15：已将 [infrastructure-layer-design.md](../../docs/04-project-development/04-design/infrastructure-layer-design.md) 收紧为“设计先行、实现优先复用 Hermes”的口径，并补齐技术域到 Hermes 模块的复用映射、反腐适配边界和 Hermes-backed adapter 落地规则。
- 2026-04-15：新增 [infrastructure-layer-design.md](../../docs/04-project-development/04-design/infrastructure-layer-design.md)，把基础设施层正式建模为 7 个技术域，并收口“应用层门面接口 + 运行时资源端口”的对外服务界面。
- 2026-04-15：新增 [memory-system-detailed-design.md](../../docs/04-project-development/04-design/memory-system-detailed-design.md)，把 `memory-system-business-requirements.md` 的业务约束正式下沉为系统分层、领域模型、存储分桶、源代码骨架、对外服务界面和基础设施端口设计，并给出主 Agent 的业务评估与改进顺序。
- 2026-04-15：新增 [memory-system-business-requirements.md](../../docs/04-project-development/03-requirements/memory-system-business-requirements.md)，固化本轮关于记忆分桶、混合技术栈项目装配、Skill 按需加载和多 Agent 协作的业务共识。
- 2026-04-15：已新增 `08-子系统定义图` 与 `09-记忆系统跨层调用图`，并补充 `core-subsystems.md`、更新设计索引；当前正式子系统收口为记忆系统、模型网关、能力系统。
- 2026-04-15：已把真实源码迁移到层级骨架：`src` 第一层文件夹就是层、第二层文件夹就是模块；完成 `access / application / runtime / settings` 重排，并将 application-owned ports、runtime-owned ports、memory/evidence/dataset store 拆到各自模块。
- 2026-04-15：新增 [core-subsystems.md](../../docs/04-project-development/04-design/core-subsystems.md)，正式定义当前 3 个子系统：记忆系统、模型网关、能力系统，并补齐记忆系统从 access -> application -> runtime -> context -> settings 的跨层调用链。
- 2026-04-15：已把代码骨架映射再收紧为“`src` 第一层文件夹就是层、第二层文件夹就是模块、模块本身就是内聚领域”；当前正式目录表达统一为 `src/access`、`src/domain`、`src/application`、`src/runtime`、`src/settings`。
- 2026-04-15：已按“消费者定义向下依赖接口、实现层负责实现”重写分层接口口径，并更新 `07-分层接口总表图`；明确业务定义层不是接口定义层，也不再保留统一 `ports_layer` 设计。
- 2026-04-15：已把多页 `v2-architecture-views.drawio` 拆成 7 个单页 `drawio` 文件，兼容只显示第一页的预览场景，并新增单页清单入口。
- 2026-04-14：已补齐 draw.io 的 `06-层间依赖图` 与 `07-分层接口总表图`，并把记忆系统明确表达为“对外内聚、对内分层”的统一子系统。
- 2026-04-14：已在架构文档中明确 `Memory Runtime` 属于平台核心能力层中的 `memory_runtime` 模块，不是并列独立大层；同时补齐了层间依赖关系和每层对外函数接口口径。
- 2026-04-14：`v2` 代码骨架映射已正式调整为“`src` 第一层=架构层、第二层=模块”；后续已进一步修订为接口归消费者所在层的 `ports/`，不再保留统一 `ports_layer`。
- 2026-04-14：新增 [architecture-layer-code-mapping.md](../../docs/04-project-development/04-design/architecture-layer-code-mapping.md)，明确目标骨架，以及当前 `src/` 到目标骨架的过渡映射关系。
- 2026-04-14：新增 `docs/04-project-development/04-design/assets/v2-architecture-views.drawio`，补齐 `v2` 系统分层图、功能模块总览图、`Memory Runtime` 子模块图和 `src/` 实现映射图。
- 2026-04-14：新增 `memory-runtime-design.md`，将记忆系统正式独立为 `Memory Runtime`，并确立“事件/evidence 为第一事实源，记忆为二级蒸馏资产”的设计原则。
- 2026-04-14：补充 `Memory Distillation Pipeline v1`，明确首版采用“规则治理 + 选择性 LLM 候选生成 + 样本沉淀”，暂不把自训练模型作为前提。
- 2026-04-14：新增 `memory-runtime-interfaces.md` 与四个子设计文档，明确 `prepare_session / distill_session / recall` 对外界面，以及 `session ledger / promotion / recall / distillation-learning` 的实现边界。
- 2026-04-14：`src/` 已落最小记忆闭环：新增 `domain.memory`、`runtime.memory`、`InMemoryMemoryStore / InMemoryEvidenceStore`，并将 recall / distill / promotion 接入 `ExecutionService`、`ContextEngine` 和默认容器。
- 2026-04-14：记忆初始专项回归曾验证 recall 仅消费 accepted memory、长时记忆进入 context segment、第二次 session 可召回第一次 session 的 app memory；该批覆盖已在 2026-04-18 迁入当前主链测试文件。
- 2026-04-14：补齐 `MemorySummarizerPort`、`MemoryDatasetStorePort` 和 `MemoryDistillationSample`，默认 `null summarizer` 不依赖 LLM，但已具备候选草案和训练样本落盘接口。
- 2026-04-14：默认容器新增 `JSONL-backed` memory/evidence/dataset store；跨容器实例可保留记忆并继续 recall。
- 2026-04-14：promotion gate 已抽离为 `MemoryPromotionPolicy`，当前支持 kind 级 confidence threshold、allowed scope 和 draft kinds。
- 2026-04-14：新增 `LLMMemorySummarizer`，容器在显式配置 `memory_summarizer_provider/model` 时可使用真实 LLM runtime 生成候选草案。
- 2026-04-14：`LLMMemorySummarizer` 已收紧输出契约，只接受 `title/body` 作为 candidate draft schema；`kind/scope/confidence` 由运行时控制，默认忽略模型 override。
- 2026-04-14：promotion policy 已支持通过 settings / env 外置化；容器可按配置构建 `MemoryPromotionPolicy`。
- 2026-04-14：记忆蒸馏链已完成幂等化：同一 session 重复 distill 时，evidence、memory record 和 dataset sample 不再重复脏写。
- 2026-04-14：`v1` 记忆系统主闭环已完成并通过仓库全量 `uv run pytest` 回归。

- 2026-06-04：已更新 AI 剧三 skill 协作方案，补入角色卡、物品卡、场景卡和视频制作提示词完整示例，并新增旧项目迁移方案、迁移固定文件与保守/快速迁移模式。
- 2026-06-05：`art-room` 已强化图片输出格式契约：每个计划/manifest/prompt/thread/QC 资产必须携带 `output_format`，明确 `deliverable_kind / file_format / minimum_resolution / background_policy / alpha_policy / canvas_aspect_ratio / required_views / composition_layers / qc_checks`；角色、物品和服装资产必须区分中性背景 master card、透明 cutout、多视角 turnaround、detail crop 和 scale reference，视频 reference frame / shot override 必须是 16:9 或项目定义画幅，并明确 foreground / midground / background。`director-room` 的后资产 ComfyUI pass 也必须消费该字段，禁止把透明 cutout、neutral card、turnaround sheet 或 detail crop sheet 当作 I2V/FLF2V 场景帧。
- 2026-06-06：`art-room` 已收紧输出目录纪律：`art/` 根目录只保留当前有效的 `series-asset-plan.md / series-thread-plan.json / series-thread-results.json`，`{episode-id}/art/` 根目录只保留固定 episode handoff 文件；所有 `*-audit* / *-review* / *-score* / *-rewrite* / *-after-fix*`、重试诊断、worker scratch 和 run-specific 中间文件必须进入 `reports/`、`audits/`、`reviews/` 或 `runs/{run-id}/`，替换根目录当前文件前必须把旧版本归档进对应 run 目录。

## 下一步建议

- 优先继续收 recall preview 的跨 backend explainability 余项，把当前已统一的 trace-first diagnostics 继续推进到“少重复、少平铺”的稳定读模型。
- 继续把 recall diagnostics/explainability 接到统一 provider 语义上，重点继续减少 normalize/backfill 阶段仍保留的 legacy 顶层 diagnostics，并把 provider-specific 字段继续压进 `access_trace / contract_trace / writeback_trace` 的统一语义，而不是回退到仅 remote_http 可解释。
- 再评估 `web / terminal / browser` 的治理细节与 provider profile 化，不与当前 memory/backend 治理并发扩散。
- 检查任务人天估算是否真实合理，仅在必要时再细化到 0.5 人天精度
- 若进入设计或实施阶段，先确认 `docs/04-project-development/04-design/technical-selection.md` 已明确框架、模块、后台范围和编码规则
- 下一轮基础设施实现优先把 Hermes-backed `CapabilityRegistryPort` 从 wrapper scaffold 推进到真实 `tools/registry.py` / `model_tools.py` 桥接，再补 gateway session context 与 approval state bridge
- 继续推进 `REQ-006` 时，优先把当前 in-memory 实现扩成可插拔 persistence adapter，而不是把记忆逻辑再塞回 `Context Builder`
- 下一轮优先补 `ProfileResolverPort`、`WorkspaceRuleBundlePort`、`SkillCatalogPort` 和 provider bindings 的正式来源，继续把 profile / 项目规则 / skill / 子任务装配治理做成一等对象
- 下一轮优先移除静态 `project_scope_key` 口径，并补 provider bindings 的正式持久化来源，先解决可解释性、历史回查和多 profile 扩展性
- 下一轮优先补更细的 recall diagnostics/explainability，重点是继续让 preview 读取侧 trace-first、顶层 alias 仅兼容，而不是再把治理逻辑堆回 store 查询
- 下一轮优先补更细粒度的 promotion policy、样本筛选规则和真实 LLM summarizer adapter，而不是直接训练专用模型
- 下一轮优先补 lifecycle review queue 的 durable 持久化与人工审核工作流，而不是继续堆 provider writeback 分支
- 下一轮优先补 policy 配置外置化、dataset 去重/筛选规则和真实 provider 的结构化输出校验，而不是直接训练专用模型
- `v1` 记忆系统主闭环已完成；后续优先转入增强项，如 recall diagnostics/explainability、policy 管理界面、dataset 审核工作流
- 若 UX/UI 需要可视化评审，优先登记真实设计交付物而不是只写文字
- 若工作项进入收尾，确认关联 PR 已完成评审并合并
- 阶段切换前先更新正式文档，再刷新 `/.factory/memory/` 压缩记忆
- 2026-04-15：已把 `agent-platform-architecture.md`、`system-architecture.md`、`module-boundaries.md`、`architecture-layer-code-mapping.md`、`infrastructure-layer-design.md`、`memory-runtime-interfaces.md` 全部统一到单向依赖链口径：`access -> application -> domain -> runtime/basic-capability -> settings`。
- 2026-04-15：已明确“记忆业务 owner 在 `src/domain/memory/`，基础能力层只提供技术能力，基础设置层只负责接口实现与装配”，并新增 `src/access/ports/application_use_cases.py`、`src/application/ports/domain_services.py`、`src/domain/*/ports.py`、`src/runtime/ports/*.py` 作为正式接口骨架。
