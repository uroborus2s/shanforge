# Skill Updates Summary

## 2026-07-04

- 2026-07-06 补充：新增 `skills/stratix-admin-web/`，作为 Stratix 管理后台前端开发规范 skill。触发范围覆盖 `web-admin`、`admin-page`、`admin-crud`、管理后台页面和后台公共组件；核心流程固定为先总结页面清单和相似组件，再开发已确认复用的公共 UI 组件，最后逐页实现页面逻辑。组件提升规则按 ponytail 口径收紧：两个不同页面出现相同 UI 和交互契约才提取公共组件，不写万能表格、万能表单或为未来预留的框架化抽象。
- 2026-07-06 补充：新增 `FLOW-CONTRACT-001` 草稿需求和实施方案，用于后续统一优化 workflow skill。正式需求为 `docs/04-project-development/03-requirements/process-workflow-contract-requirements.md`，正式实施方案为 `docs/04-project-development/05-development-process/process-workflow-contract-implementation-plan.md`。当前已完成流程契约设计、任务拆解、实施前独立评审和用户人工确认；reviewer `codex-flow-contract-001-pre-reviewer-20260706` 给出 `approved / 94`。下一阶段按任务升级 `using-shanforge`、`project-memory`、`requirements-engineering`、`document-templates`、`writing-plans`、执行、评审和验证类 skill。
- 2026-07-06 补充：`FLOW-CONTRACT-001` 已创建顺序实施队列 `.factory/workitems/FLOW-CONTRACT-001/implementation-queue.md`。本会话不实施任务；后续从 `FLOW-TASK-003` 开始，按任务号顺序逐项实施并逐项 review。
- 2026-07-06 补充：`FLOW-TASK-003` 已将 `document-templates` 推进到 `ready_for_review`。新增正式文档治理规则，要求新增正式文档同步 `docs/index.md` 或 `.factory/memory/doc-map.md`，修改正式文档追加 `版本历史`，临时文档只进入 work item evidence / reports / reviews 或 PM generated；新增 `formal-document-template.md` 固定中文 `版本信息` 和 `版本历史`。验证：`uv run pytest tests/test_sf_sp_010_documentation_navigation.py` 通过 `8 passed`。
- 2026-07-06 补充：`FLOW-TASK-003` 独立 review 已通过并进入 `pending_human_confirmation`。Reviewer `codex-flow-task-003-reviewer-20260706` 给出 `approved / 94`；review 明确无 Critical / Important，1 个 Minor 审计精度备注不阻塞。人工确认前不得进入后续 skill 改造任务。
- 2026-07-06 补充：`FLOW-TASK-004` 已将 `requirements-engineering` 推进到 `pending_human_confirmation`。新增四类场景 `new_project / add_requirement / change_requirement / fix_bug`、需求版本规则、baseline 影响分析、领域模块映射和 baseline 变更建议；`prd-template.md` 增加版本信息、版本历史、场景分类、领域模块映射和 baseline 变更建议。iteration-1 review 要求修正 `tasks.summary.md` 当前焦点，已修复并通过复审 `approved / 95`。
- 2026-07-06 补充：`FLOW-TASK-005` 已将 `using-shanforge` 推进到 `pending_human_confirmation`。流程总控现在明确是四类场景 `new_project / add_requirement / change_requirement / fix_bug`、baseline work item、gate 和关闭规则的唯一 owner；缺 evidence、review、verification、人工确认或最终审计问题报告时阻塞关闭、提交和进入下一阶段；人工确认包必须包含最终审计问题报告、阻塞问题、已修复问题、残留风险和验证证据，不能只输出评分。黑盒流程 eval 已补 FLOW-CONTRACT 五个场景。独立 review `approved / 96`，无 Critical / Important / Minor。
- 2026-07-06 补充：`FLOW-TASK-006` 已将 `project-memory` 推进到 `pending_human_confirmation`。新增事实源优先级：正式文档和 work item ledger 高于 memory summary；summary 只写 ID、状态、当前 gate、关键约束和索引，不复制完整正文；PM generated 非事实源，`.factory/pm/generated/status-dashboard.html` 只能作为展示视图。`doc-map.md` 已同步事实源边界。独立 review `approved / 95`，无 Critical / Important / Minor。
- 2026-07-06 补充：`FLOW-TASK-007` 已将 `writing-plans` 推进到 `pending_human_confirmation`。计划现在明确只能生成候选执行输入，不执行代码；work item plan 和 task brief 模板必须包含设计方案、接口设计、UI 或 `N/A`、测试设计、开发、单测、review、集成测试；模板已加入缺测试设计、UI N/A 缺原因和占位语失败断言。独立 review `approved / 95`，无 Critical / Important / Minor。
- 2026-07-06 补充：`FLOW-TASK-008` 已将 `executing-plans` 和 `subagent-driven-development` 推进到 `ready_for_review`。两个执行类 skill 现在都有任务 gate：缺设计方案、接口设计、UI 或 N/A 原因、测试设计时阻塞；缺 verification evidence、evidence、implementer report、review checkpoint 或 ledger 事件时不得进入 `ready_for_review`；完成状态只能回写 `ready_for_review`、`blocked` 或 `needs_user_input`。`subagent-driven-development` 还明确子 agent 不决定下一步 skill，只执行分配的 task brief。
- 2026-07-06 补充：`FLOW-TASK-008` 独立 review 已通过并进入 `pending_human_confirmation`。Reviewer `codex-flow-task-008-reviewer-20260706` 给出 `approved / 94`；无 Critical / Important / Minor；最终审计问题报告为 `.factory/workitems/FLOW-CONTRACT-001/reports/FLOW-TASK-008-final-audit-issue-report.md`。人工确认前不得进入 `FLOW-TASK-009`。
- 2026-07-06 补充：`FLOW-TASK-009` 已将 `requesting-code-review`、`receiving-code-review` 和 `verification-before-completion` 推进到 `ready_for_review`。Review 现在明确作者自检不能 `approved`，N/A 必须由 reviewer 明确接受或拒绝，未被接受的 N/A 不得通过 review；receiving 处理 N/A 反馈时必须按接受、拒绝或待澄清分支处理；verification 关闭 gate 要求新鲜命令、exit code、输出和 evidence，且无 evidence 不能关闭。Review 不能替代 verification，verification 不能替代 human confirmation。
- 2026-07-06 补充：`FLOW-TASK-009` 独立 review 已通过并进入 `pending_human_confirmation`。Reviewer `codex-flow-task-009-reviewer-20260706` 给出 `approved / 95`；无 Critical / Important / Minor；最终审计问题报告为 `.factory/workitems/FLOW-CONTRACT-001/reports/FLOW-TASK-009-final-audit-issue-report.md`。人工确认前不得进入 `FLOW-TASK-010`。
- 2026-07-06 补充：`FLOW-TASK-010` 已将 `document-templates` baseline 模板推进到 `ready_for_review`。新增 `project-baseline-template.md`、`backend-module-design-template.md`、`database-design-template.md`、`api-design-template.md` 和 `frontend-ui-design-template.md`；五个模板均包含中文版本信息和版本历史，数据库模板包含 ERD，API 模板引用 `openapi.yaml`。验证 `uv run pytest tests/test_sf_sp_010_documentation_navigation.py` 通过 `9 passed`，ruff 通过。
- 2026-07-06 补充：`FLOW-TASK-010` 独立 review 已通过并进入 `pending_human_confirmation`。Reviewer `codex-flow-task-010-reviewer-20260706` 给出 `approved / 95`；无 Critical / Important / Minor；最终审计问题报告为 `.factory/workitems/FLOW-CONTRACT-001/reports/FLOW-TASK-010-final-audit-issue-report.md`。人工确认前不得进入 `FLOW-TASK-011`。
- 2026-07-06 补充：`FLOW-CONTRACT-001` 已把项目级测试治理和记忆生命周期补入后续 skill 改造范围。测试治理覆盖 `TEST-ENV-* / TEST-API-* / TEST-UI-* / TEST-BB-* / TEST-REL-*`、测试环境启动责任和实际端口记录；记忆治理明确启动不能只读 `current-state.md`，已关闭任务下一次 memory sync 后从 current-state 降级到 summary/history，ledger/evidence/review/report 不删除。
- 2026-07-06 补充：`FLOW-CONTRACT-001` 的 `project-memory` 后续改造口径已从“启动读取顺序”修正为“条件读取链”。已有新鲜会话卡时不得继续读取 memory 文件；只有会话卡缺失、过期或不匹配时才读 `agent-session.md`；仍缺关键事实才读 `runtime-brief.md` / `current-state.md` 的最小片段。
- 2026-07-06 补充：`stratix-service` 已按最新制品库复测结果收紧。当前 latest 为 `@stratix/create@1.1.1`、`@stratix/core@1.1.1`、`@stratix/forge@1.1.3`、`@stratix/database@1.1.0`。主 skill、CLI workflow、OpenAI metadata 和测试已更新：旧 2026-07-05 失败只保留为历史兼容注记，不再作为永久 blocked 结论；默认 API 示例改为最小 `--preset testing`；上线门禁提升为 doctor、测试、build、manifest、release gate、openapi、start、decrypt、runtime injection 全部新鲜通过。新增 `production-readiness-report-2026-07-06.md` 记录 latest 结果：build/test/manifest/release/openapi 和 sensitive config 加解密已通过，模板不再读取 `PORT/HOST`，但 doctor 仍有 `.stratix/project.json` 版本期望问题，start/discovery 仍因 `stratix.generated.js` 源码路径失败，结论继续 blocked。
- 2026-07-05 补充：根据用户“bug 已解决”复测要求，`stratix-service` 已完成新一轮测试子任务和 code review 子任务。Code review 发现 `runtime-realities.md` 仍暗示普通 `.env` 可承载应用配置，`ecosystem-map.md` 仍列普通 env key；已统一改为应用配置只从 `sensitiveConfig` 读取，插件/基础设施配置以 `sensitiveConfig.database|redis|ossp|wasV7|gateway` 路径描述，并新增测试扫描全部 `references/*.md` 的旧 env 口径。测试子任务确认公开 npm latest 未变，官方对照项目仍不能完整跑通：pnpm 11 初装被 ignored-builds 阻断，官方 build 因 `operationId` 类型失败，`stratix start --help` 无法 resolve `@stratix/core`，运行时 `STRATIX_SENSITIVE_CONFIG` 注入未验证；skill 修正项目可通过 config validate/encrypt/decrypt 和最小 build 修正，但上线结论仍为 `blocked`。
- 2026-07-05 补充：完成旧全局流程脚本与废弃 CLI skill 清理。已删除 `software-factory-cli`、旧流程脚本、旧动作注册表和旧 CLI 功能测试；新 workflow 默认走 `using-shanforge`、`project-memory` 和具体工作 skill。`document-templates` 不再推荐旧项目纳管脚本，改为先交给 `using-shanforge` 判断是否需要项目纳管。新增 `tests/test_deprecated_skill_cleanup.py` 固定这些边界。
- 2026-07-05 补充：`ui-ux-pro-max` 已从上游 `nextlevelbuilder/ui-ux-pro-max-skill` 的远端 `main` 更新到 `4baa399d00da806f83ed93652172f66943205153`。本次替换采用上游 `.claude/skills/ui-ux-pro-max/` 作为可直接运行的 skill 版本，更新了主 `SKILL.md`、设计系统脚本、产品/颜色/图标/动效/移动端等数据集，并新增 `app-interface / design / draft / google-fonts / motion` 数据和 `angular / laravel / threejs` 栈数据。验证：`python3 skills/ui-ux-pro-max/scripts/search.py "saas dashboard professional" --design-system -p "Smoke"` 成功，`python3 skills/skill-creator/scripts/quick_validate.py skills/ui-ux-pro-max` 通过。
- 2026-07-05 补充：`stratix-service` 已按生产化复测结果再次收紧。主 skill 现在不再绑定 Stratix 1.1.x 固定假设，要求先查 npm dist-tags、项目实际安装版本、`pnpm exec stratix --help`、`list templates` 和 `list presets`；新增“官方对照项目”归因方法，用 `@stratix/create@latest` 干净模板与 skill 生成项目跑同一命令矩阵，区分 skill 问题和 Stratix 工具链 / 模板问题。QC 子任务无 P0/P1，指出 metadata 和测试覆盖 P2 已修复；生产化测试子任务判定当前 latest 工具链仍 `blocked`：pnpm 11 ignored-builds、forge 1.1.2 doctor 与 `.stratix/project.json` 版本期望冲突、`operationId` 类型导致 build 失败、release gate 缺 security/audit 脚本、start 无法解析 ESM-only `@stratix/core`、官方模板仍从 `process.env.PORT` / `PORT/HOST` 读取 server 配置，运行时 `STRATIX_SENSITIVE_CONFIG` 注入无法验证。新增 `skills/stratix-service/reports/production-readiness-report-2026-07-05.md` 和 `skills/stratix-service/reports/stratix-toolchain-bug-report-2026-07-05.md`。
- 2026-07-05 补充：`SF-SP-009` 已新增黑盒流程 eval 契约。`using-shanforge` 现在在用户要求 `SF-SP-009`、黑盒流程 eval 或 workflow 回归评估时读取 `references/black-box-flow-eval.md`；该 reference 定义 `fast smoke` / `full regression`、证据格式、`2/1/0` 评分、`>= 90` 通过门和 critical assertion 失败门，覆盖一句话需求、bug 修复、review 反馈、压缩恢复、完成声明和自评隔离。不新增中心脚本 gate，不替代独立 review、人工确认、提交或 PR 闭环。
- 2026-07-05 补充：根据 `SF-SP-009` 独立 review 反馈，黑盒 eval reference 已补评分归一化公式：每条 critical assertion 单独按 `2/1/0` 计分，最高可能得分为纳入场景 critical assertion 总数乘以 2，总分为 `round(实际得分 / 最高可能得分 * 100)`；证据格式新增 Actual / Max / Normalized score。结构测试同步增强，防止只存在六个标题但断言空泛。
- 2026-07-05 补充：按用户要求完成 `stratix-service` 上线前评估与修正。语言子任务建议将主文件改为短句、去口号化；QC 子任务指出敏感配置、hex key 示例、旧全局 CLI 和插件选择风险；测试子任务在 `/private/tmp/shanforge-stratix-skill-test-20260705` 用 `npx --yes @stratix/create@1.1.0` 和项目内 `pnpm exec stratix` 实测生成 API 应用。已将主 skill 改为配置安全门：新生成应用配置默认全部来自 `sensitiveConfig`，所有应用配置先写 JSON，再加密为 `STRATIX_SENSITIVE_CONFIG` 注入；禁止 `DB_HOST / REDIS_HOST / WPS_APP_SECRET` 这类普通 env 回退；项目内优先 `pnpm exec stratix` 并先确认 Stratix 1.1.x；插件选择压缩为 adapter / integration / data 三类。`scaffolds.md` 示例已移除 `process.env.DB_* / REDIS_* / HOST / PORT` 回退，`environment-config.md` 明确普通 `.env` 不承载应用配置，`cli-workflow.md` 记录 2026-07-05 实测风险：doctor/test/config encrypt-decrypt 可用，但 build、start、release gate、openapi 在当前 Node 24 + 1.1.0 组合下有失败项，遇到时应标记 blocked 而不是宣称上线通过。
- 2026-07-05 补充：针对 `SF-SP-008` 执行纪律缺口，`using-shanforge` 和 `gitcommitzh` 已追加 skill-native 收尾门，并撤销中心脚本 gate 方案。完成声明、提交或关闭 work item 前必须重读最新 `.factory/workitems/<WORKITEM-ID>/ledger.jsonl` 和 review ledger；若仍有阻塞状态或 `next_required_action`，只能报告继续 review / human confirmation / fix。`gitcommitzh` 还补充了混合 `.factory/memory/` 文件范围隔离规则：只能暂存当前任务 hunk，无法拆分时停止并拆成独立提交。
- 2026-07-05 补充：新增 `SF-SP-008` PR 闭环与提交规则首版。`gitcommitzh` 现在包含 PR 闭环与提交前置检查，新增 `references/pr-closure-checklist.md`，要求提交前核对 work item ledger、review ledger、verification evidence、memory sync 和当前任务范围；`using-shanforge` 新增提交门，明确提交不能替代 review 或人工确认；Codex 工具参考不再说“提交全部工作”，改为只提交当前任务范围。
- 2026-07-05 补充：`SF-SP-005` 和 `SF-SP-006` 已完成独立复审。`SF-SP-005` execution workflow skill 修复后为 `approved / 92`，`SF-SP-006` review workflow skill 修复后为 `approved / 95`；两者当前 gate 均为 `pending_human_confirmation`。`subagent-driven-development`、`using-shanforge`、`requesting-code-review`、`receiving-code-review` 的关键规则已由负向测试固定。
- 2026-07-05 补充：已修复 `SF-SP-005` 和 `SF-SP-006` 独立 review 反馈。`subagent-driven-development` 的状态处理 reference 现在只生成 review input 和 `needs: review`，不再协调 Spec/Quality Review 流转；`using-shanforge` Codex 参考不再指向旧分支收尾 skill；`requesting-code-review` 明确 `review_status` / `next_gate_status`；`receiving-code-review` 明确 memory sync 目标；`requesting-code-review` OpenAI metadata 不再要求同线程输出 `review_score`。
- 2026-07-05 补充：`SF-SP-005`、`SF-SP-006`、`SF-SP-007` 已补真实独立评审。`SF-SP-005` 执行类 workflow skill 当前为 `changes_requested / 78`，`SF-SP-006` 评审类 workflow skill 当前为 `changes_requested / 84`，`SF-SP-007` 验证与调试 gate 当前为 `approved / 95`。`SF-SP-007` 的通过不等于人工确认，也不能覆盖前置 `SF-SP-005/006` 的阻塞项；下一步应先修复独立 reviewer 指出的 Important 问题并重新 review。

- 2026-07-05 补充：已修复评审独立性硬门。`requesting-code-review` 现在明确同线程作者自检只能输出 `self_check_passed`，禁止写 `approved`、`review_score` 或 `pending_human_confirmation`；真实 `approved` 必须带 `reviewer_type`、`reviewer_id` 和 `reviewer_independence_evidence`。`task-review-template.md`、`independent-review-task-template.md`、`pr-review-template.md` 和 `review-score-rubric.md` 已同步新增 `author_self_check_score / review_score` 分离字段。新增 `tests/test_independent_review_gate.py`，当前仅同线程自检通过，仍待独立 review。
- 2026-07-05 补充：新增首版 `skills/verification-before-completion/` 和 `skills/systematic-debugging/`，作为 `SF-SP-007` 的验证与调试 gate，但 review 状态已更正为 `needs_independent_review`。`verification-before-completion` 固定完成声明前的新鲜验证证据、完整命令、完整输出、exit code、失败数量、完成声明检查清单和 Red-Green 验证模板；`systematic-debugging` 固定修复前根因调查、四阶段调试、数据流反向追踪、防御式校验、条件等待和 3 次修复失败后质疑架构。此前 iteration-1 review 是同线程作者自检，不是真实独立 review；补真实独立 review 前不得进入 `SF-SP-008`。
- 2026-07-05 补充：新增首版 `skills/requesting-code-review/` 和 `skills/receiving-code-review/`，作为 `SF-SP-006` 的评审类 workflow skill，并完成 iteration-1 review。`requesting-code-review` 固定 task review、PR review、独立 review task、评分 rubric、review ledger 和人工确认门；`receiving-code-review` 固定 feedback triage、技术核实、逐项处理、response 和验证证据。两者均已移除前置 / 后置 / 下一步 skill 路由声明，只回写状态、产物、证据和 `needs`。新增 `tests/test_review_workflow_skills.py`，当前状态为 `pending_human_confirmation`，等待人工确认。
- 2026-07-05 补充：`SF-SP-004` iteration-2 已根据人工反馈补齐 `writing-plans/references` 中文化。`workitem-plan-template.md`、`task-brief-template.md`、`plan-review-template.md` 的标题、字段、章节、步骤、检查项和输出说明已改为中文短句；只保留必要代码标识符、路径、命令、状态值和产品名。`tests/test_writing_plans_skill.py` 已加入中文模板断言和旧英文模板短语禁止项。当前状态 `ready_for_review`，等待独立 review。
- 2026-07-05 补充：`SF-SP-005` iteration-2 已根据人工反馈修正流程边界。`using-shanforge` 现在是流程总控 / CTO，负责判断当前环节和唯一下一步 skill；`subagent-driven-development`、`executing-plans`、`writing-plans` 不再写“与其他 skill 的关系”、不再硬编码 review / verification / commit 等后继 skill，只回写状态、产物、证据和 `needs`。当前状态 `ready_for_review`，等待独立 review。
- 2026-07-05 补充：新增首版 `skills/subagent-driven-development/` 和 `skills/executing-plans/`，作为 `SF-SP-005` 的执行类 workflow skill，并完成 iteration-1 review。`subagent-driven-development` 固定 work item plan、task brief、ledger、evidence、implementer report、Spec Review、Quality Review 和状态处理清单；`executing-plans` 固定当前会话 inline 执行 fallback，先 review plan，再逐步执行并设置 review checkpoint。新增 `tests/test_execution_workflow_skills.py`，当前状态为 `pending_human_confirmation`，等待人工确认。
- 2026-07-05 补充：新增首版 `skills/writing-plans/` 并通过 task review，作为 `SF-SP-004` 的计划生成 workflow skill。`SKILL.md` 固定已批准 spec、需求、设计或 work item brief 进入计划生成；默认输出 `.factory/workitems/<WORKITEM-ID>/plan.md`、`task-briefs/`、evidence、reports、reviews 和 ledger；references 新增 work item plan 模板、task brief 模板和 plan review 模板；`agents/openai.yaml` 已使用中文入口元数据。新增 `tests/test_writing_plans_skill.py`，当前状态为 `approved`。
- 2026-07-05 补充：`SF-SP-003` 已完成已有 skill 的 references 迁移切片并通过 task review。`requirements-engineering` 新增 PRD 模板，`document-templates` 新增技术设计模板，`tdd-workflow` 新增根因定位清单和 evidence 模板，`gitcommitzh` 新增提交说明 rubric；对应 `SKILL.md` 均已指向新增 references。本切片已通过定向测试、ruff 和 skill validator，状态为 `approved_for_slice`。`requesting-code-review / verification-before-completion / systematic-debugging` 的 references 仍需随对应 workflow skill 创建继续迁移。
- 2026-07-05 补充：已评审并修正 `brainstorming` skill 中文化后的流程口径。新版 `SKILL.md` 改为基于 shanforge 当前阶段、work item 状态和已压缩上下文决定是否头脑风暴及下一步 skill；默认不展开读取 `.factory/memory/` 背景文件清单，优先消费当前对话、`project-memory` 会话卡、当前 work item brief/ledger，缺上下文时先交给 `project-memory` 恢复最小会话卡。已移除“固定只转 `writing-plans`”的硬编码流程，保留创意澄清、一次一个问题、2-3 方案、用户批准和自审规则。头脑风暴输出固定路由到 `.factory/workitems/<WORKITEM-ID>/brief.md`、`ledger.jsonl`、必要的 `docs/04-project-development/` 正式文档与 memory summary。`visual-companion.md` 已中文重写，`start-server.sh` 新增 `--workitem-id`，持久化目录改为 `.factory/workitems/<WORKITEM-ID>/design-assets/brainstorm/`，并移除外部 Superpowers 品牌图片依赖。
- 2026-07-05 补充：新增首版 `skills/project-memory/`，作为 Superpowers 流程集成阶段 1 的会话恢复与记忆同步 skill。`SKILL.md` 现要求先复用已有会话卡和压缩记忆，若同一 work item 已有新鲜会话卡则不得重复读取源 summary；只有缺口明确时才增量读取 runtime/current-state/doc-map/role/project/summary。`references/` 已包含会话启动清单、相关性判断门、会话卡模板、memory ledger 事件模板和 current-state 更新清单；会话卡模板新增“未读 / 已排除上下文”。新增 `tests/test_project_memory_skill.py` 固定结构约束。
- 2026-07-05 补充：`skills/skill-creator/SKILL.md` 已新增通用 skill 编写/修改原则：所有项目内 skill 默认中文；编写者按中文语言专家和 prompt 专家标准，用短句完整保留原文含义；改写前建立含义保留清单；作者只能推进到 `ready_for_review`，由独立 reviewer 决定 `approved / changes_requested`；模板、schema、rubric 和长背景进入 `references/`，确定性 helper code 只能作为 skill-scoped 工具存在。新增 `tests/test_skill_creator_skill_principles.py` 锁定这些规则。
- 2026-07-05 补充：流程集成方案已明确 skill 可以携带确定性 helper code。`py/js` helper 只能作为某个 skill 的封闭工具存在，必须由 `SKILL.md` 写明触发、输入、输出和风险边界，模板/schema/rubric 放在 `references/`，并通过测试和独立 review；不得把全局 `scripts/`、中心 CLI 或隐藏执行器作为流程主控。
- 2026-07-05 补充：Superpowers 流程集成方案已补入独立评审任务规则。实现类 skill 后续必须把实现完成态限制为 `ready_for_review`，由独立 `requesting-code-review` 任务或未参与实现的 reviewer 子 agent 决定 `approved / changes_requested`；rubric、review task 模板和输出格式应进入对应 skill 的 `references/`。
- 已将 `skills/stratix-nodejs-backend/` 重命名为 `skills/stratix-service/`。新版 skill 以 `/Users/uroborus/NodeProject/wps/obsync-root` 为事实来源，明确当前工具链是 `@stratix/create@1.1.0` 的 `create-stratix` 与 `@stratix/forge@1.1.0` 的项目内 `stratix` 命令，而不是旧 `@stratix/cli` 单包。skill 已补齐 AI 自主选择 template/preset/plugin、完整开发循环、`obsync-root` 根质量命令、插件选择决策树，以及 `.env` / `STRATIX_SENSITIVE_CONFIG` / `STRATIX_ENCRYPTION_KEY` 的开发和生产环境规则；新增 `references/environment-config.md` 与 `tests/test_stratix_service_skill.py` 固定关键约束。
- 新增 `docs/04-project-development/05-development-process/superpowers-workflow-integration-plan.md`，作为 shanforge 集成 Superpowers 计划、开发、评审链路的实施方案。文档将目标架构调整为 skill-first：`using-shanforge` 做轻量入口，新增 `project-memory` skill 接管会话恢复、读取范围和 ledger 模板，文档模板和固定方法统一放入对应 skill 的 `references/`。`writing-plans`、`subagent-driven-development`、`executing-plans`、`requesting-code-review`、`receiving-code-review`、`verification-before-completion`、`systematic-debugging` 为优先改造对象；`factory-agent-session` 仅作为迁移来源，不再作为目标主控。
- 已将 `skills/brainstorming/SKILL.md` 从新版英文流程改写为精简中文短句，完整保留创造性工作前必用、实施前设计批准硬门、范围拆分、2-3 方案、设计文档、规范自审、用户审阅、按需浏览器可视化伴侣和仅调用 `writing-plans` 的规则。新增 `skills/brainstorming/agents/openai.yaml`，提供中文 `display_name`、`short_description` 和默认提示；新增 `tests/test_brainstorming_skill.py` 锁定中文化后的关键约束与 OpenAI 元数据。
- 已按用户要求退役并删除本地 `skills/writer-room/`、`skills/director-room/`、`skills/art-room/`。检查 `/Users/uroborus/.codex/skills` 后未发现这三个 Codex 全局 skill 条目或 symlink，因此全局侧无可删除对象；当前可见来源均为仓内已删除目录。同步删除 `tests/test_writer_room_skill.py`、`tests/test_director_room_skill.py`、`tests/test_art_room_skill.py`，避免已退役 skill 的结构测试继续要求目录存在。

## 2026-06-20

- 已将“修 Bug 必须先定位根因，禁止用未验证兜底替代修复”沉淀为 bug 修复类 skill 的共同约束：`tdd-workflow` 新增 Bug 根因先于修复规则和根因记录模板；`ai-regression-testing` 新增 Bug 根因修复门槛，明确回归测试不是为兜底方案背书；`ai-first-engineering` 新增团队级 Bug 修复纪律；`python-uv-project` 新增 Python 调试/修复时的根因与防兜底规则。新增 `tests/test_bug_fix_root_cause_skill_rules.py` 固定这些约束，避免后续 skill 演进时丢失。

## 2026-06-14

- 新增 `skills/browser-control/`，作为中文 Codex skill 收口本地浏览器控制工作流。触发词覆盖“本地浏览器 / browser-use / 真实浏览器 / Codex Browser / Codex Chrome / 访问 URL / 截图 / 页面状态 / localhost 调试”；当用户明确要求“用本地浏览器访问 URL”时，默认优先使用 `browser-use --headed --session browser-control --json open <URL>`，再读取 `state` 确认 URL、标题、可见文本和元素索引。skill 同时写明 Codex Browser、Codex Chrome、Computer Use 的路由边界，以及表单提交、上传、cookies、登录态、权限和敏感信息的动作时确认规则。

## 2026-06-04

- 2026-06-07 补充：`art-room` 已修正分集资产状态卡的目录路由。项目级 `assets/characters|locations|props|costumes|style` 只放全剧母资产和全剧 style reference；`character_episode_state_card`、`prop_episode_state_card`、`location_episode_scene_card` 必须写入对应 `{episode-id}/assets/characters|locations|props|costumes`。`asset-manifest`、`art-image-prompts`、`asset-index` 和 `thread-plan.output_format_contracts` 现在都按 `asset_type + asset_subtype` 约束 canonical path，`c001e01` 这类分集状态卡不得再落入全局目录。
- 2026-06-06 补充：`art-room` 已新增输出目录纪律，防止审计和运行中间文件继续平铺到 `art/` 根目录。项目级 `art/` 根目录仅保留当前有效 `series-asset-plan.md / series-thread-plan.json / series-thread-results.json`；episode 级 `{episode-id}/art/` 根目录仅保留固定 handoff 文件。所有 `*-audit* / *-review* / *-score* / *-rewrite* / *-after-fix*`、重试诊断、worker scratch 和 run-specific 中间文件必须路由到 `reports/`、`audits/`、`reviews/` 或 `runs/{run-id}/`。
- 2026-06-05 补充：`art-room` 已新增强制 `output_format` 输出格式契约，覆盖 asset prep、asset manifest、art image prompts、thread plan 和 asset index/QC；字段包括 `deliverable_kind / file_format / minimum_resolution / background_policy / alpha_policy / canvas_aspect_ratio / required_views / composition_layers / qc_checks`。角色/物品/服装资产要求中性背景 master card、透明 alpha cutout、多视角 turnaround、detail crop 和尺度参考；视频 reference frame / shot override 要求 16:9 或项目定义画幅、alpha 禁用、foreground/midground/background 三层构图。`director-room` 的 ComfyUI asset prompt pack 现也必须保留 `output_format`，后续 shot prompt 与 prompt QC 必须拒绝把透明 cutout、neutral card、turnaround sheet 或 detail crop sheet 当作 I2V/FLF2V 场景帧。
- 2026-06-05 补充：`art-room` 已新增可直接提交给图片模型的 `copy_ready` 提示词契约，包含 `positive_prompt / negative_prompt / chatgpt_image_prompt / gemini_image_prompt`；资产准备和线程计划新增 `creation_order / creation_phase / depends_on_assets / blocks_assets / dependency_reason / priority`；角色卡 schema 新增 `body_metrics`，物品卡 schema 新增 `physical_dimensions`，用于资产审核时清楚查看创建顺序、依赖顺序、身高体态、比例、长宽高、重量感和材质厚度。
- 已按 `ai-drama-production-skill-system.md` 修改三间房 skill 本体与测试：`writer-room` 现支持全剧基础层、单集剧本、最终成片 canon 写回和旧项目故事迁移模式；`director-room` 现覆盖资产前分镜包、资产后视频生产包、render manifest、shot QC、edit decision list、AI TTS 对白计划、audio manifest 和 post/delivery QC；`art-room` 现覆盖全剧母资产、单集 asset prep、角色卡/物品卡/场景卡、短文件名、资产 prompt 的 `production_metadata` / `model_visible_prompt` 分层。
- 新增或扩展三间房 schema 与 reference：`director-room` 增加 video/render/QC/edit/audio/post schema 和 edit/audio/delivery agent cards；`art-room` 增加 `references/asset-card-prompt-templates.md`、资产卡设计 schema、`asset_subtype` 与短文件名约束；相关测试已扩展到新增路径、schema、QC 状态和提示词分层，并通过三间房定向回归、skill validator、ruff 与仓库全量 `pytest`。
- 已将独立 `prompt-room` 合并进 `director-room`：`director-room` 现在直接从 `./project/{project-name}/{episode-id}` 剧本包生成分镜、镜头清单、视觉连续性、generation plan、双语 shot prompt draft 和双语 ComfyUI-ready prompt pack。
- `director-room` 新增 prompt-engineering 阶段角色：prompt director、style preset、asset conditioning、shot prompt engineer、workflow parameter、prompt QC 与 ComfyUI feedback tuning；原独立 `skills/prompt-room/` 目录与 `tests/test_prompt_room_skill.py` 已移除。
- `director-room` 分镜说明契约固定包含中英双语结构：`基础设定 / Basic Setup`、`氛围和画质 / Atmosphere and Image Quality`、`画面内容 / Shot Panels`，每个镜头必须包含中英双语的景别、构图、运镜、画面内容、光线色彩和连续性锚点。
- JSON prompt 契约已改为双语字段：草案使用 `prompt_zh / prompt_en / negative_prompt_notes_zh / negative_prompt_notes_en`，最终 ComfyUI prompts 使用 `positive_prompt_zh / positive_prompt_en / negative_prompt_zh / negative_prompt_en`。
- 已移除 Codex 全局 `prompt-room` symlink，保留全局 `director-room` 作为统一入口；`art-room` 的后续 handoff 改为回到 `director-room` prompt refresh。

## 2026-06-03

- 已将 `skills/prompt-room/` 链接到 Codex 全局 skills：`/Users/uroborus/.codex/skills/prompt-room -> /Users/uroborus/AiProject/shanforge/skills/prompt-room`。
- `prompt-room` 本地 skill 通过 `python3 skills/skill-creator/scripts/quick_validate.py skills/prompt-room` 校验。

## 2026-06-01

- `writer-room`、`director-room`、`art-room` 的文档契约已统一到仓库根目录下的 `./project/{project-name}/`，跨集公共资料放在项目根，单集产物放在 `{episode-id}/`，例如 `01/`、`02/`。
- 新增 `skills/prompt-room/`，作为 Codex-native 提示词工程部，从共享生产项目根读取最终剧本、导演分镜产物和美术资产包，输出 ComfyUI-ready prompt、资产 conditioning、workflow 参数计划、QC 报告与调优日志。
- `prompt-room` 固定读取 `script/`、`bible/`、`director-brief.md`、`camera-plan.md`、`shots/`、`storyboard/`、`continuity/`、`production/generation-plan.json`、`prompts/shot-prompts-draft.json` 以及 `art/` / `prompts/art-image-prompts.json` / `art/asset-index.json`。
- `prompt-room` 固定写入 `prompts/comfyui-prompt-brief.md`、`prompts/comfyui-style-preset.json`、`prompts/comfyui-asset-prompt-pack.json`、`prompts/comfyui-shot-prompts.json`、`prompts/comfyui-workflow-plan.json`、`prompts/comfyui-tuning-log.json` 与 `reports/comfyui-prompt-qc.md`。
- 新增 7 个提示词工程角色任务卡：prompt director、style preset、asset conditioning、shot prompt engineer、workflow parameter、prompt QC 与 ComfyUI feedback tuning。
- 新增 `skills/art-room/`，作为 Codex-native 美术资产部，从共享生产项目根读取导演分镜产物，输出一致化角色、场景、道具、服装、风格和镜头参考图资产计划。
- `art-room` 固定从 `./project/{project-name}/` 读取项目级 `bible/characters.md`、`bible/scenes.md` 以及 `{episode-id}/` 下的剧本、导演分镜、镜头、storyboard、visual continuity、generation plan 和 draft prompts；固定写入 `{episode-id}/art/`、`{episode-id}/prompts/art-image-prompts.json`、项目级共享 `assets/` 图片目录和 `{episode-id}/assets/` 镜头参考目录。
- `art-room` 的图片生成边界固定为父协调器先生成 `{episode-id}/art/thread-plan.json`，再使用 `codex_app.create_thread` / `read_thread` / `send_message_to_thread` 调用后台 Codex thread 分批生成图片；规划 agent 不直接生成图片。
- 新增 9 个美术资产角色任务卡：art director、asset breakdown、character design、environment design、prop costume design、style continuity、image prompt、thread plan 与 asset QC。
- 新增 `skills/director-room/`，作为 Codex-native 导演分镜与镜头生产拆解部门，把标准化最终剧本包转换为视频生产清单。
- `writer-room` 项目目录契约改为 `./project/{project-name}/`：跨集资料固定写入 `outline/series-outline.md`、`synopsis/story-synopsis.md`、`bible/characters.md`、`bible/scenes.md`、`bible/continuity.md`，单集资料固定写入 `{episode-id}/script/final-script.md`、`{episode-id}/reports/continuity-report.md`、`{episode-id}/reports/script-score.md` 等路径。
- `director-room` 固定从同一个 `./project/{project-name}/` 读取项目级 `bible/` 与 `{episode-id}/script/`、`{episode-id}/reports/` 下的标准文件；若存在等价旧文件名，只允许在项目目录内归一化，不再创建独立 director-room 输入目录。
- skill 明确故事场景设计只负责故事层场景，不承担导演分镜与镜头生产拆解。
- 新增 8 个导演分镜角色任务卡：director、scene breakdown、shot planner、cinematographer、storyboard、visual continuity、generation strategy 与 shot prompt。
- 首版输出契约覆盖 `{episode-id}/shots/scene-breakdown.json`、`{episode-id}/shots/shot-list.json`、`{episode-id}/storyboard/storyboard-plan.md`、`{episode-id}/continuity/visual-continuity-bible.json`、`{episode-id}/production/generation-plan.json` 和 `{episode-id}/prompts/shot-prompts-draft.json`，并补齐 agent envelope 与 JSON schema。

## 2026-05-31

- 新增 `skills/writer-room/`，将编剧室从 Python 调度 LLM 的方案收口为 Codex-native skill。
- skill 明确主 Codex 线程作为制片主任 / 调度器，子 Codex agent 分别承担 showrunner、story architect、character、scene、dialogue、script doctor、rewrite、continuity、evaluator、memory librarian 与 learning evolution 职责。
- 新增 `agents/*.md` 角色任务卡、`references/` 调度与评分契约、`assets/templates/` 输出模板和 `schemas/` 结构化结果契约。
- 自我进化口径固定为先产出 `memory/evolution-notes.md` 和失败模式记录；修改 skill 本体前必须取得用户明确批准。

## 2026-05-02

- `skills/crawler4j-model-project/` 已对齐 `crawler4j 0.4.0` 的 `core-native-v2` 模块协议。
- skill 主体、references 与 `agents/openai.yaml` 现统一切到：
  - `crawler4j module init`
  - `interface/component/workflow/page-action/data/candidate/cleanup/page create`
  - `.crawler4j/manifest.lock.json`
  - `check structure/release/full`
  - `package build/verify`
  - `host devlink/install/debug config`
- 已显式标记旧叙事为迁移对象或禁用项：
  - `TaskScript`
  - `TaskFlow`
  - `TaskSpec`
  - `WorkflowSpec`
  - `module_runtime.py`
  - `env_selectors/`
  - `ui_extension`
  - `config_schema.json`
  - `crawler4j init-model`
  - `crawler4j new`
  - `crawler4j add-workflow`
  - `crawler4j add-ui`
- 新版 skill 也补上了宿主边界：目录源码只能走 `host devlink`，正式安装只走 ZIP 或 GitHub `owner/repo`，不把 `.whl` 当模块安装格式。
