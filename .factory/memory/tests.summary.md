# 测试摘要

- 当前阶段：`FLOW-STATUS-REVIEW-001 / active`
- 当前验证结论：父与独立reviewer均复验全量416 passed / 11 subtests passed、行为6 passed，Ruff/shape/diff和25文件指纹通过；FLOW-SR-REV-I-01复审关闭，本范围approved。前两轮失败及oracle校准保留。回源：本工作项evidence/verification.md与reviews/independent-review.md。

- 2026-09-07：上一 UI 批次完整 pytest 406 passed / 11 subtests passed；三页 × 320/390/430 浏览器检查通过；不是 A/B、真机或美术批准。

- 2026-09-06：入口合同首轮 RED `2 failed`，评审整改 RED `2 failed`，聚合整改 RED `1 failed`；最终定向 `24 passed`。FLOW-S12 的四类 mutation 均被拒绝，fast-path 聚合为 `30/30`。

- 2026-09-05：UI 候选与设计流程定向 56 passed；独立前向 SwiftUI 与 ASCII/cp1252 输出验证通过。review 首轮 3 Important 均关闭；固定数据/语义测试不是美术画面通过证据。

- 2026-09-04：关键页面确认门和路由边界初始 RED `2 failed / 11 passed`；评审整改 RED `1 failed / 20 passed`。定向与相邻流程 `40 passed`；Memory 同步后首次全仓为 `359 passed / 1 failed / 11 subtests passed`，补齐最新 ledger 事件身份后最终全仓 `360 passed / 11 subtests passed`。两份 Skill validator、全仓 Ruff、ledger 和 diff check 通过。

- 2026-09-03：技术评估回复合同首轮 Red `3 failed / 9 passed`，历史时点、示例语境和评审整改分别形成 `1 failed / 11 passed` 的定向 Red；最终定向 `12 passed`。完整质量门为 `358 passed / 11 subtests passed`，响应黑盒 `19 passed`，Ruff、38/38 validator、TaskCard 图、ledger、代码形态和 diff check 全部通过。

- 2026-09-02：最终报告写入后重新执行完整质量门。系统 Python 运行 validator 因缺 PyYAML 未实际执行，uv 沙箱运行因缓存权限未实际执行；获准读取现有 uv 缓存后为 `38/38 passed`。首次 memory closeout 把当前工作项写成 `none`，导致主线 ledger 快照测试 `355 passed / 1 failed`；根因是恢复器要求会话卡保留最近已关闭工作项，状态字段修正后重新执行完整测试。最终有效结果为 pytest `356 passed / 11 subtests passed`、Ruff passed、响应黑盒 `18 passed`、TaskCard 图 11/11、work item ledger 69 条和 session ledger 2 条均合法且唯一、无嵌套命名函数/lambda、diff check passed。

- 2026-09-02：T08 首轮全量为 `316 passed / 2 failed / 4 subtests passed`，根因为 `writing-plans/SKILL.md` 错误拥有项目级下一动作；修复和评审整改后为 `322 passed / 4 subtests passed`。Memory closeout 又发现会话卡字段名与 ledger 身份不一致，`321 passed / 1 failed`；修复后最终仍为 `322 passed / 4 subtests passed`。评审整改定向 41 passed；Ruff、38/38 validator、黑盒 9/9、JSONL 和 diff check 通过。

- 2026-09-01：`SOFTWARE-ENGINEERING-SKILL-AUDIT-001` 审计提交 `4929f58`；覆盖验证 `5 reviewers / 38 skills / 190 scores`，重算矩阵 `85.6`；全量 pytest `294 passed / 4 subtests passed`，Ruff、工作项 JSONL、diff check 通过，`skills/` 与 `tests/` 无 tracked 改动。

- 2026-09-01：`HUMAN-RESPONSE-CONTRACT-003` 实现提交 `91a3aea`；定向合同测试 `30 passed`，Ruff、3 个 Skill validator、工作项 JSONL 与 diff check 通过；独立 reviewer 复验同样为 `30 passed`。

- 2026-09-01：生命周期矩阵语义反转 Red 为 `1 failed / 10 passed`，修复后治理专项 `27 passed`；同一 reviewer 四个反向探针全部拒绝、正向矩阵通过。

- 2026-09-01：`HUMAN-RESPONSE-CONTRACT-002` 实现提交 `91460c2`；工作项范围 `25 passed, 1 deselected`，Ruff、四个变更 Skill validator、JSONL 与 diff check 通过；并行 `SOFTWARE-LIFECYCLE-GOVERNANCE-001` 正式文档重写使关联旧锚点基线暂为 `19 passed, 2 failed`，故只声明 `task_scope_passed / repository_baseline_partial`。

- 2026-08-23：T02/T03/T04 定向验证分别为 `35 passed / 4 subtests`、`44 passed`、`26 passed`；T05 隔离完整候选 `245 passed / 4 subtests passed`，详见 `.factory/workitems/SKILL-FULL-OPTIMIZATION-001/evidence/verification.md`。

- 2026-07-30：并行任务 `PM-DASHBOARD-005-T01` 第七轮路线 Green
  `2 passed / 4 subtests`，聚焦快照 `10 passed / 4 subtests`，Ruff check/format
  通过。真实快照为 32 节点、5 根、3 张唯一主板任务、0 空状态格，第二次生成
  `cache_hit=true`。站内 `.html` 链接 `31,607 checked / 0 missing`。1440×900 与
  390×844 Chrome 验收无横向溢出，移动状态段单列，路线根/子节点/任务入口通过，
  console error 0。全仓后像 `219 passed / 7 failed / 4 subtests`，七个失败 node ID
  与修改前完全一致且均在本任务禁止范围。独立任务评审新鲜复跑相同结果，结论为
  `approved / 99 / C0-I0-M0`；下一 Gate 是用户 UI 验收。
- 2026-07-29：并行任务 `PM-DASHBOARD-005-T01` 聚焦快照合同 `5 passed`，
  Ruff check/format 通过；第三轮 UI 整改已用 1440×900 与 390×844 验证六页 Tab、
  需求分组与分类、当前工作链接、详情返回和路线图到每日进展的下钻链。页面级无
  横向溢出、控制台错误 `0`；最终第二次生成 `cache_hit=true`。全仓最近一次为
  `214 passed / 7 failed`，7 项均为允许路径外既有失败。独立接手复核
  `approved / 96 / C0-I0-M0`，但第四轮 390×844 黑盒复现确认：移动路线图初始
  不可见、路线卡缺少下钻、分组层级错误和终态被 closeout 重开。第四轮已完成
  `1 failed / 4 passed → 5 passed`，Ruff 与 diff check 通过；独立评审整改又完成
  `1 failed / 5 passed → 6 passed`，覆盖重叠需求、需求链接 DOM 和显式终态覆盖。真实
  双次生成 generation id 一致且第二次 `cache_hit=true`。390×844 /
  1440×900 验证六个 Tab、
  174/174 看板操作、6/6 路线操作、`FLOW-TASK-015` 位于“已完成”、页面级溢出 0、
  控制台错误 0；重复需求组 0、无效需求链接 0。全仓为 `215 passed / 7 failed`，
  7 项均为允许路径外既有失败；同一 reviewer 复核为
  `approved / 100 / C0-I0-M0`。第五轮 UI 退回已完成只读 DOM 诊断：顶部路线阶段
  `5 个 / 0 链接`，计划阶段 `5 个 / 2 链接`，任务性质组 `8`、需求/工作项组 `40`、
  可折叠任务性质组 `0`。两个根因和第五轮方案获批后，Red 分别按 `0/5` 阶段链接和
  静态嵌套 section 失败；Green `2 passed`，聚焦 `7 passed`，Ruff 通过。真实快照
  阶段链接/锚点 `5/5`、原生折叠业务组 `40/40`、标签任务 `174/174`、旧嵌套容器
  `0`，双生成 ID 一致且第二次 `cache_hit=true`。全仓 `216 passed / 7 failed`，
  失败集合与上一轮相同。自动浏览器因本地 `file://` 安全策略和 Chrome 沙箱限制
  未运行；独立 reviewer 新鲜复跑聚焦 `7 passed` 与两条 Ruff 均通过，并确认真实 HTML
  结构计数，结论 `approved / 97 / C0-I0-M0`。双视口视觉与交互继续等待用户 UI 验收。
  第六轮针对“同一计划 fragment 不等于路线下钻”和“状态优先全历史主板不可读”建立
  3 项 Red，最小实现后 `3 passed`，聚焦快照 `8 passed`；Ruff check 与 format check
  均通过。真实快照包含 5 个唯一阶段页，03 有 8 个直接子步骤（7 完成、1 当前），
  当前主板为 7 张任务卡、终态历史 167 条折叠。双生成 ID 一致且第二次
  `cache_hit=true`。全仓 `217 passed / 7 failed`，七项失败与既有范围外基线相同；
  本地浏览器策略禁止自动访问 `file://`。独立评审 Iteration 1 为
  `changes_requested / 86 / C0-I2-M0`；缺状态反例和三条移动端 CSS 合同整改后，
  聚焦仍为 `8 passed`，同一 reviewer 复审为 `approved / 98 / C0-I0-M0`。
  视觉验收仍等待用户。
- 2026-07-27：T03 首轮评审整改验证通过：Validator 直接回读 T02 的 45 条
  状态转移，验证 6 条 Gate 子集、5 个权限负例和 5 组职责分离负例；Ruff 与
  diff check 通过，等待 Iteration 2 独立复审。
- 2026-07-27：T03 首轮静态契约验证通过：
  `roles=6 raci_rows=14 gates=6 transitions=6 negative_cases=5`，Ruff、
  ledger 20 行、5 个必需产物和 diff check 通过。
- 2026-07-27：T02 最终验证通过：validator、4+5 负例、golden digest、Ruff、
  work item/review ledger、收口契约、current-state 边界和 diff check 均通过。
- 2026-07-27：T02 Iteration 3 整改验证通过：唯一 `data` canonical payload 与
  `schema_version` 已固化，golden digest 为 `sha256:da62145f…afdcd8`；
  work item ledger 15 行、review ledger 6 个 T02 事件可解析。
- 2026-07-27：T02 Iteration 2 整改验证通过：
  `state_negative_cases=4 governance_negative_cases=5`，Ruff 通过；
  work item ledger 13 行，review ledger 4 个 T02 事件可解析。
- 2026-07-27：T02 Iteration 1 整改验证通过：
  `models=6 agents=6 audit_fields=10 transitions=45 negative_cases=4`；
  work item ledger 11 行、review ledger 2 个 T02 事件、5 个 memory 授权文件通过。
- 2026-07-27：T02 静态契约验证通过：`models=5 agents=6 common_fields=6
  baseline_na=3`，产物 `5/5` 存在，ledger 9 行可解析；整体黑盒、UI、API 和
  发布回归因本任务只定义文档契约而为 N/A。
- 2026-07-27：EAD 评审收口验证通过。Ledger 最新状态为
  `pending_human_confirmation`，Gate 决策对象、原因、三类选项和下一动作完整；
  本地 commit 因具体产品路径尚未人工批准而未创建。
- 2026-07-27：Skill 联合收口验证通过。独立评审 `approved / 96 / C0-I0-M1`；
  Go 既有复审 `approved / 98 / C0-I0-M0`。Index 完整目标的 3 个失败均由 HEAD
  既存 runtime-skill 目录与其他工作项 digest 漂移解释，本任务白名单外暂存为 0。
- 2026-07-27：`STATE-RECONCILIATION-001-T01` 状态治理验证通过：
  `closed=12`、唯一对账事件 `12`、JSONL 有效、6/6 提交祖先检查通过。
  8 个实际待办保持非终态，独立评审 `approved / 99 / C0-I0-M1`。
  未运行产品测试，因为本批次只新增 ledger 终态。
- 2026-07-27：`FLOW-CONTRACT-001` 收口状态迁移 Red 两轮均为 `2 failed, 55 passed`，
  根因分别为旧 `in_progress` 断言、通用 ledger 回源缺失、父 WorkItem/Task 状态身份混用和零活动任务未覆盖；
  最小修正后同组 `57 passed`，Ruff、JSONL、15/15 队列、commit/close 唯一事件和 diff check 通过。
  其他 WorkItem 只做只读盘点，未运行其产品测试。
- 2026-07-27：`FLOW-TASK-015` 正式实施完成 Runtime Red/Green `1 failed, 7 passed -> 8 passed`，
  规定组合 `57 passed`，Ruff 和 9/9 Skill validator 通过。首轮独立实现 Review
  `changes_requested / 76 / C0-I3-M0`；旧自动人工 Gate 冲突、测试假通过和状态投影漂移已完成整改，
  同一 Reviewer 复审已 `approved / 98 / C0-I0-M0`，当前只执行精确本地提交。
- 2026-07-23：`TASK-IMPLEMENT-003-P001` 项目快照第五轮整改验证通过。项目知识与站点相关回归 `102 passed`；受影响 Python/测试 Ruff 通过；`mypy src` 为 `279 source files` 全通过；Chromium 桌面 1440×900、移动 390×844 的六列看板、每列最近 10 条与“更多”、中文卡片标题、详情页返回、无横向溢出和零 console/page error 全通过；连续两次 snapshot 第二次 cache hit。独立终审 `approved / 96 / C0-I0-M0`。
- 2026-07-21：`TASK-IMPLEMENT-002-R001` final candidate 验证通过。最终沙箱全量 1272 passed，5 个 Chromium Mach 权限阻断 viewport 在沙箱外 browser 文件 13/13 中通过，因此 1277 unique tests 均有通过结果；final trace 10，性能 atomic P95 c1/c8 `4.218542/97.205ms`、projection `3.314917/18.715208ms`，final builder/validator、Ruff/format/diff 通过。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/TASK-IMPLEMENT-002-R001-final-candidate-verification.md`。
- 2026-07-20：`TASK-IMPLEMENT-002-P001` 最终计划验证 exit 0：scope SHA `ec7e2f69…13cfd`、plan SHA `b88d431d…8f8be1`、review SHA `b3363876…eb8f1` 精确匹配；T01–T08 8/8、需求 11/11、UI N/A/测试/命令/边界 8/8、占位 0、staging/release/append-only/quarantine/replay 和整体候选 Review 路径通过，两份 JSONL 与 diff check 通过。未运行产品 pytest/Ruff/mypy/性能测试，原因是本轮为计划任务且无产品代码授权。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/TASK-IMPLEMENT-002-P001-final-plan-verification.md`。
- 2026-07-20：R001 正式文档激活后验证通过。正式 validator artifacts `6/6`、candidate/formal manifest/root matched、内容验收/授权/激活事件 `1/1/1`、权限边界通过；expect activation `0` 的负向检查按预期以 `activation_event_count_mismatch` 拒绝。全仓 `832 passed / 0 failed / 0 skipped / 0 not_run`，Ruff 0、format 299、mypy 0/236、Skill 38/38、docs-stratego、lock、JSONL、runtime Skill absence 和 diff check 通过。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/TASK-DELIVERY-001-R001-post-activation-verification.md`。
- 2026-07-20：`TASK-DELIVERY-001-R001` 最终复验通过。Validator：source 10/10、artifacts 6/6、implemented 15/123、remaining 108/123、root `632e705a…869c`；文档 6/6、metadata 6/6、相对链接和禁止事实通过；6/6 负向攻击全部拒绝。全仓正式门禁：pytest 832、Ruff 0、format 299、mypy 0/236、Skill 38/38、lock、diff、manifest JSON、runtime Skill 目录边界通过；docs-stratego exit 0。独立复审 `approved / 100 / 0-0-0`。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/TASK-DELIVERY-001-R001-verification.md`；Decision：`.factory/workitems/FLOW-CONTRACT-001/reviews/TASK-DELIVERY-001-R001-independent-review.md`。
- 2026-07-20：R002 本地正式实现发布和发布后验证通过。事务内与事务外均验证 candidate 665 artifacts / 46 governance inputs；新鲜全仓 `832 passed / 0 failed / 0 skipped / 0 not_run`，Ruff 0、format 299/299、mypy 0/236、Skill 38/38、uv lock、diff check 和 runtime Skill absence 通过；candidate attacks 17/17、release transaction attacks 8/8 拒绝。Formal manifest `eae82c90…77c0a`，released event 唯一计数 1。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/TASK-IMPLEMENT-001-R002-post-release-verification.md`。
- 2026-07-19：TASK-SKILL-002 完成 32 个剩余工作 Skill 的状态契约 RED→GREEN。初始 `2 failed / 2 passed`，实现后合同测试 `4 passed`；目标组合 11、相邻 Skill/Gate 136、全仓 828 passed。Ruff 通过，format `298 files already formatted`，mypy `0/236 source files`，diff check、33 对象 SHA-256、work item ledger JSONL 和 review ledger JSONL 校验均 exit 0。默认用户级 uv cache 受沙箱限制，改用 work item 事务缓存并保持 `--frozen --offline --no-sync` 后复验通过，归类为环境路径而非代码失败。

- 2026-07-19：TASK-QUALITY-002 删除 runtime Skill 链路完成 RED→GREEN。结构测试先因 `src/runtime/skills` 存在而失败，删除后 `1 passed`；7 个相邻文件 `59 passed`；最终 Ruff `0`、format `296/296`、mypy `0/236 source files`、pytest `817 passed / 0 failed / 0 skipped / 0 not_run`、diff check 通过。首轮全量唯一文档断言失败已按平台中立要求整改并复验。扩展 `mypy src tests` 暴露 220 个 tests 目录既有类型错误，不属于 `pyproject.toml` 的 `files=["src"]` 正式门禁，未扩入本删除任务。

- 2026-07-19：P001 开发实施最终验证完成。T08 target `22 passed`、T01–T08 project-control `519 passed`、全仓 `809 passed`，failed/skipped/not_run=`0/0/0`；T08 changed-file Ruff `All checks passed`、format `20 files already formatted`、隔离 mypy `10 source files`、diff check 均通过。T08 Spec/Quality Re-review 均 `approved / 100 / 0-0-0`。全仓 Ruff/mypy 仍有既有 `33/73` 项，明确记录为全局静态 debt，未伪报通过。

- 2026-07-19：P001 T06 最终独立复审通过。Spec Recheck 5、Quality Re-review 均 `approved / 98 / 0-0-0`；主代理新鲜复验 target `89 passed`、T05+T06 `131 passed`、direct POSIX `1 passed`，Ruff/mypy/format 全通过。T01–T07 已完成，当前由 T08 执行跨切片集成、性能门、全仓 pytest、Ruff、mypy 和 diff check。

- 2026-07-19：P001 T04 最终独立复审通过：权限自授权 Critical 已由 exact-context independent grant 关闭，Spec/Quality Re-review 均 `approved / 100 / 0-0-0`。主代理新鲜复验 target `38 passed`、相邻 `303 passed`、contract/provenance `74 passed`，Ruff/mypy/format 全通过。T06 第三轮整改与 T08 尚未完成。

- 2026-07-19：P001 T07 最终独立复审通过：Spec Re-review 2 与 Quality Re-review 均 `approved / 100 / 0-0-0`；目标 `64 passed`、相邻 `404 passed`、定向攻击 `8 passed`，Ruff/mypy 通过。T04 正在独立 Spec Re-review，T06 正在第二轮 deadline 整改，P001 尚未完成。

- 2026-07-19：P001 T02 Quality Re-review `approved / 100 / 0-0-0`；contracts+position `73 passed`、adjacent disposition `197 passed`，Ruff/mypy 通过。T01/T02/T03/T05/T06/T07 当前联合专项为 `388 passed / 0 failed / 0 skipped`，受影响 Ruff 通过、mypy 31 source files 通过。T04 尚在实现，T06/T07 仍有各自 review gate，因此不能据此声称 P001 完成。

- 2026-07-19：P001 T05 独立 Quality Re-review 通过：目标 `42 passed`、相邻 `249 passed`、I-001/I-002/M-001 定向攻击 `10 passed`，独立 connection probe 确认 temporary reader/owner close 与 post-close guard，Ruff、mypy 通过。T07 Spec 整改作者验证为目标 `34 passed`、contracts+T07 `44 passed`、Ruff/mypy 通过，仍等待独立 Spec/Quality gate。

- 2026-07-19：P001 并行层新鲜联合验证通过：`tests/test_project_control_contracts.py` + position + disposition + evidence 为 `303 passed / 0 failed / 0 skipped`；受影响路径 Ruff `All checks passed!`；mypy `Success: no issues found in 27 source files`。T03 Spec/Quality 已批准，T02/T05 的作者整改验证包含在本次联合回归中；它们仍须各自完成独立复审，T07 尚在整改，不能据此声称并行层或 P001 完成。

- 2026-07-19：`TASK-IMPLEMENT-001-P001` 实施计划验证与独立复审通过。作者首次 validator `187/189` 暴露 2 个精确文字缺口，修正后 `189/189`；独立 Review 发现 `P001-I-001` 后增加 5 条 fixture-owner 防回退断言，最终 `194/194 passed`、Node syntax、scope JSON、计划范围 diff check exit 0。独立复审 `approved / 96 / 0-0-0`。本轮未修改产品代码，因此未重跑产品 pytest/Ruff/mypy；产品基线仍引用 R020 发布后新鲜 `287 passed`。

- 2026-07-19：R020 正式发布与新鲜复验通过。事务 `DESIGN-RELEASE-TX-R020-G001` 写入后验证 49/49、docs 37 文件/7 目录、0 failed/skipped/not_run；事务内 pytest 287/287。完成前独立复验中，首次命令因默认用户级 UV 缓存越出沙箱而在测试启动前 exit 2；显式使用事务内 `UV_CACHE_DIR` 后同一 pytest 参数通过 `287 passed`，十个 R020 测试消费者 Ruff `All checks passed!`，Node syntax 和 `git diff --check` exit 0。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/TASK-DESIGN-001-R020-post-activation-verification.md`。

- 2026-07-18：R019 发布后验证器最小修复完成 RED→GREEN，正式复验 38/38、37 文件/7 目录、0 failed/skipped/not_run；Catalog 重建 SHA-256 `bbe65dd2...0638`、217 records/123 workflows，`git diff --check` 通过。激活前 `uv run pytest -q --tb=no` 为 28 failed/259 passed；精确 rollback/docs 前像隔离对照为 2 failed/285 passed，确认 27 个新增失败来自 10 个测试消费者未随文档 IA 迁移。前后像 ruff 均为 591 项，归类为未变化既存基线。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/TASK-DESIGN-001-R019-repository-test-regression-root-cause.json`。

- 2026-07-18：R019 正式发布预演通过，preflight root `4e2106b4...c4d6e`，68 文件/17 目录前像和 38 目标后像、链接、Catalog 重建及回滚副本全部通过；正式写入阶段返回 `failed=0`，after-image root `d887ca63...18002`。随后 `node ...R019-formal-release.mjs --verify` 退出 1，稳定复现 `baseline file count drift: 37/68`。调查确认这是 post-release verifier 在读取已提交事务状态前错误执行 preimage-only 检查；根因明确但未修复，最终激活和全量发布后验证尚未执行。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/TASK-DESIGN-001-R019-post-release-verifier-root-cause.json`。

- 2026-07-12：`FLOW-CONTRACT-001 / TASK-DESIGN-001 / WP-03` 验证通过。`cp02@0.2.0 --phase wp03` 退出 0：14/14 生命周期阶段、4/4 运行状态机、96 条运行转换、8/8 RouteRule、7/7 Gate、8/8 接口、8/8 需求覆盖，12 个路由、7 个 Gate、6 个幂等 fixture 全部符合预期；`cp01@0.5.0` 回归退出 0，96/96 Artifact 转换正例、24 负例、9 正例和 11 External Ref 状态无回退；`bootstrap` 退出 0。完整 `cp02` 按预期退出 1，且只报告 WP-04 尚缺 `workflow=123` 和 `action_spec>=1`；`source-red` 按预期退出 2。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/TASK-DESIGN-001-verification.md#wp-03`。

- 2026-07-08：`DOC-FACTORY-RESTRUCTURE-001` 破坏性重做型全量文档结构迁移验证通过。`uv run pytest tests/test_doc_factory_restructure.py` 通过 `9 passed`；受迁移影响测试集合通过 `72 passed`；对应 ruff 通过 `All checks passed!`；`uvx --from docs-stratego docs-stratego source validate --repo-path .` 通过，`pages=56`；`jq empty .factory/memory/graph/traceability.json .factory/project.json .factory/tech-profile.json .factory/multi-agent-board.json` 通过；`git diff --check` 通过。验证范围覆盖任务执行契约登记、旧资产删除、当前入口无断链、`.factory` 破坏性迁移边界、正式署名、当前配置 JSON、work item 标准结构和旧测试口径更新。

- 2026-07-09：`FLOW-TASK-015` 完整软件项目会话归因正式方案验证通过。新增 `tests/test_full_project_session_workflow_routing.py`，Red 为 `3 failed, 1 passed`，Green 为 `4 passed`；邻近流程验证 `uv run pytest tests/test_full_project_session_workflow_routing.py tests/test_black_box_workflow_eval.py tests/test_project_memory_skill.py tests/test_writing_plans_skill.py tests/test_execution_workflow_skills.py tests/test_review_workflow_skills.py tests/test_verification_debugging_workflow_skills.py` 通过 `45 passed`；ruff 通过；`using-shanforge` quick_validate 通过；目标文件 `git diff --check` 通过。`docs-stratego source validate` 因 uv 缓存访问权限未完成。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/FLOW-TASK-015-verification.md`。

- 更新时间：2026-07-20 14:13:42
- 当前验证重点：R002 已正式发布并完成发布后全量验证；后续验证范围属于第 8/8 步交付与收尾，不能从本阶段自动推导部署或远端动作

## 当前质量门

- `docs-stratego source validate --repo-path .`
- `.factory/project.json` JSON 解析
- `.factory/memory/graph/traceability.json` JSON 解析

## 最近验证

- 2026-07-08：`DOC-FACTORY-RESTRUCTURE-001` 文档与工厂结构治理验证通过。新增 `tests/test_doc_factory_restructure.py` 固定任务执行契约、`.factory` 事实边界、设计资产 Markdown 迁移和 work item 标准结构；验证 `uv run pytest tests/test_project_memory_skill.py tests/test_doc_factory_restructure.py tests/test_sf_sp_010_documentation_navigation.py` 通过 `20 passed`，`docs-stratego source validate --repo-path .` 通过并报告 `pages=76`，`git diff --check` 通过。Evidence：`.factory/workitems/DOC-FACTORY-RESTRUCTURE-001/evidence/TASK-001-verification.md`。
- 2026-07-07：`FLOW-CONTRACT-001` 输出持久化和会话可见性契约验证通过。新增结构测试固定 `FLOW-REQ-018`、`using-shanforge` 完成输出与持久化契约、当前会话可见性协议、`project-memory` 写入边界，以及子 agent / 自循环不得只静默写文件。验证：ledger JSONL 逐行解析 `ok`；`uv run pytest tests/test_project_memory_skill.py` 通过 `6 passed`；`uv run ruff check tests/test_project_memory_skill.py` 通过；`using-shanforge` 和 `project-memory` quick_validate 均通过。`docs-stratego source validate` 已在沙箱外运行，失败于既有 `docs/04-project-development/04-design/assets/v2-architecture-pages/index.md` 位于 assets 目录且是 Markdown，非本轮新增文件。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/output-persistence-contract-verification.md`。
- 2026-07-06：`FLOW-TASK-010` baseline 设计模板验证和独立 review 验证通过。新增结构测试先复现缺口：`uv run pytest tests/test_sf_sp_010_documentation_navigation.py` 返回 `1 failed, 8 passed`，失败点为 5 个 baseline 设计模板缺失；新增 project baseline、backend module、database、API 和 frontend UI 模板后，同一命令通过 `9 passed`，`uv run ruff check tests/test_sf_sp_010_documentation_navigation.py` 通过。独立 reviewer 复跑同两条命令，pytest 为 `9 passed`，ruff 为 `All checks passed!`。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/FLOW-TASK-010-verification.md`。
- 2026-07-06：`FLOW-TASK-009` review / verification 验证通过。新增结构测试先复现缺口：`uv run pytest tests/test_review_workflow_skills.py tests/test_verification_debugging_workflow_skills.py` 返回 `2 failed, 11 passed`，失败点为 review 缺作者自检不能 approved、N/A 必须由 reviewer 接受 / 拒绝断言，以及 verification 缺关闭前检查新鲜命令、exit code、输出和 evidence、无 evidence 不能关闭、review / verification / human confirmation 分离断言；补齐规则后同一命令通过 `13 passed`，`uv run ruff check tests/test_review_workflow_skills.py tests/test_verification_debugging_workflow_skills.py` 通过。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/FLOW-TASK-009-verification.md`。
- 2026-07-06：`FLOW-TASK-008` 执行类 skill 验证通过。新增结构测试先复现缺口：`uv run pytest tests/test_execution_workflow_skills.py` 返回 `2 failed, 7 passed`，失败点为 `executing-plans` / `subagent-driven-development` 缺任务 gate、缺设计 / 测试 / evidence 阻塞断言，以及 `subagent-driven-development` 缺子 agent 不决定下一步 skill 断言；补齐规则后同一命令通过 `9 passed`，`uv run ruff check tests/test_execution_workflow_skills.py` 通过。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/FLOW-TASK-008-verification.md`。
- 2026-07-06：`FLOW-TASK-007` 计划编写验证通过。新增结构测试先复现缺口：`uv run pytest tests/test_writing_plans_skill.py` 返回 `1 failed, 3 passed`，失败点为 `writing-plans` 缺“计划只能生成候选执行输入，不执行代码”口径，计划模板和任务 brief 缺设计方案 / 接口设计 / UI 或 N/A / 测试设计 / 开发 / 单测 / review / 集成测试，以及缺测试设计、UI N/A 缺原因、占位语失败断言；补齐后同一命令通过 `4 passed`，`uv run ruff check tests/test_writing_plans_skill.py` 通过。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/FLOW-TASK-007-verification.md`。
- 2026-07-06：`FLOW-TASK-006` 项目记忆验证通过。新增结构测试先复现缺口：`uv run pytest tests/test_project_memory_skill.py` 返回 `1 failed, 4 passed`，失败点为 `project-memory` 缺事实源优先级、summary 不复制完整正文、PM generated 非事实源和 `.factory/pm/generated/status-dashboard.html` 非唯一事实源断言；补齐规则与 `doc-map.md` 事实源边界后，同一命令通过 `5 passed`，`uv run ruff check tests/test_project_memory_skill.py` 通过。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/FLOW-TASK-006-verification.md`。
- 2026-07-06：`FLOW-TASK-005` 流程总控验证通过。新增结构测试先复现缺口：`uv run pytest tests/test_black_box_workflow_eval.py` 返回 `2 failed, 5 passed`，失败点为 `using-shanforge` 缺四类场景 / baseline work item / 缺 evidence 阻塞关闭 / 最终审计问题报告断言，以及既有 `SF-SP-009` 计划断言旧口径；补齐规则与 FLOW-CONTRACT 黑盒 eval 场景后，同一命令通过 `7 passed`，`uv run ruff check tests/test_black_box_workflow_eval.py` 通过。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/FLOW-TASK-005-verification.md`。
- 2026-07-06：`FLOW-TASK-004` 需求工程流程验证通过。新增结构测试先复现缺口：`uv run pytest tests/test_requirements_engineering_skill.py` 返回 `2 failed, 2 passed`，失败点为 `requirements-engineering` 缺四类场景 / 需求版本 / baseline 影响 / 领域模块 / baseline 变更规则，`prd-template.md` 缺版本和影响分析字段；补齐后 `uv run pytest tests/test_requirements_engineering_skill.py tests/test_superpowers_reference_migration.py` 通过 `8 passed`，`uv run ruff check tests/test_requirements_engineering_skill.py tests/test_superpowers_reference_migration.py` 通过。review feedback 修复后同一验证仍通过。
- 2026-07-06：`FLOW-TASK-003` 文档治理规则验证通过。先新增结构测试复现缺口：`uv run pytest tests/test_sf_sp_010_documentation_navigation.py` 返回 `2 failed, 6 passed`，失败点为 `document-templates` 缺正式文档登记规则和正式文档模板；补齐规则与 `formal-document-template.md` 后，同一命令通过 `8 passed`。Evidence：`.factory/workitems/FLOW-CONTRACT-001/evidence/FLOW-TASK-003-verification.md`。
- 2026-07-06：`stratix-service` latest 制品库复测并修正 skill 门禁。npm latest：`@stratix/create@1.1.1`、`@stratix/core@1.1.1`、`@stratix/forge@1.1.3`、`@stratix/database@1.1.0`。临时项目 `/private/tmp/shanforge-stratix-retest-20260706/skill-api-prod` 验证：create/help/list 通过；默认安装下 test/build/doctor/manifest/release/openapi 通过，但 start 仍无法 resolve core；显式 latest 后 test/build/manifest/release/openapi 和 config validate/encrypt/decrypt 通过，生成配置读取 `sensitiveConfig.server`，`.env.example` 不含 `PORT/HOST`，启动日志识别 `STRATIX_SENSITIVE_CONFIG`，但 doctor 因 `.stratix/project.json` 期望 `@stratix/core ^1.1.0` 失败，start/discovery 因 `stratix.generated.js` 源码路径失败。结构回归：`.venv/bin/pytest tests/test_stratix_service_skill.py` 通过 `11 passed`；`.venv/bin/ruff check tests/test_stratix_service_skill.py`、`python3 skills/skill-creator/scripts/quick_validate.py skills/stratix-service`、`git diff --check -- skills/stratix-service tests/test_stratix_service_skill.py` 均通过。
- 2026-07-05：`stratix-service` 根据用户“bug 已解决”复测。Code review 子任务发现 linked references 仍残留普通 env 配置口径，修复后新增测试扫描全部 `skills/stratix-service/references/*.md`，防止 `DB_HOST / REDIS_HOST / WPS_APP_SECRET / OSSP_SECRET_KEY / UPSTREAM_URL` 等旧口径回流。验证：`.venv/bin/pytest tests/test_stratix_service_skill.py` 通过 `10 passed`；`.venv/bin/ruff check tests/test_stratix_service_skill.py` 通过；`python3 skills/skill-creator/scripts/quick_validate.py skills/stratix-service` 通过。测试子任务在 `/private/tmp/stratix-skill-test-PNQNO3` 复测 current latest，create/list/help、doctor、test、OpenAPI、config validate/encrypt/decrypt 通过；官方模板初装仍被 pnpm 11 ignored-builds 阻断，官方 build 仍因 `operationId` 类型失败，`stratix start --help` 仍无法 resolve `@stratix/core`，运行时注入未验证，结论仍 `blocked`。
- 2026-07-06：本地 `shanforge-cli` demo 删除验证通过。已删除 `scripts/shanforge-cli`、`src/access/cli/` 和 `tests/test_access_cli.py`；仍有价值的 runtime 回归改用测试侧 manifest fixture；`tests/test_deprecated_skill_cleanup.py` 已加防回流断言。验证：`.venv/bin/pytest tests/test_deprecated_skill_cleanup.py tests/test_platform_scaffold.py tests/test_infrastructure_scaffold.py tests/test_composition_container.py tests/test_settings_profile_governance.py` 通过 `41 passed`；对应 `.venv/bin/ruff check` 通过；引用扫描只剩删除记录和负向断言；`find scripts -maxdepth 1 -type f -print | sort` 仅剩 `scripts/sync-codex-skills`；`git diff --check` 通过。
- 2026-07-05：旧全局流程脚本清理验证通过。新增/更新 `tests/test_deprecated_skill_cleanup.py`，固定旧流程脚本、旧动作注册表、`software-factory-cli` 和旧 CLI 功能测试均不存在；当前入口文档和配置不再引用旧脚本入口；`document-templates` 不再推荐旧仓库脚本。验证：`.venv/bin/pytest tests/test_deprecated_skill_cleanup.py tests/test_project_memory_skill.py tests/test_pr_commit_workflow_rules.py tests/test_sf_sp_010_documentation_navigation.py tests/test_crawler4j_model_skill_integration.py` 通过 `21 passed`；对应 `.venv/bin/ruff check` 通过；`python3 skills/skill-creator/scripts/quick_validate.py` 对 `skills/document-templates`、`skills/project-memory`、`skills/using-shanforge` 均通过；`.factory/project.json` 和 `config/software-factory.defaults.json` JSON 解析通过；旧命令入口扫描只命中负向断言测试；`git diff --check` 通过。
- 2026-07-05：PM 首页需求实时跟踪表布局修复并验证通过。新增结构断言要求 `status-dashboard.html` 的需求表和任务状态表使用独立 class、固定列宽、任务状态表任务列加宽，并要求关联任务 chip 带状态文本；红灯为 `1 failed, 4 passed`。修复后 `uv run pytest tests/test_project_management_control_plane.py` 通过 `5 passed`；`uv run ruff check tests/test_project_management_control_plane.py`、`python3 skills/skill-creator/scripts/quick_validate.py skills/using-shanforge`、Python HTML parser 和 `git diff --check` 均通过。浏览器自动控制读取 `file://` 页面被 Browser URL policy 阻止，未绕过。
- 2026-07-05：skills deprecated cleanup 验证通过。新增 `tests/test_deprecated_skill_cleanup.py`，固定旧全局流程脚本、旧动作注册表、废弃 CLI skill 和旧功能测试不得回退；固定 `document-templates` 不再推荐旧仓库脚本。验证：`.venv/bin/pytest tests/test_deprecated_skill_cleanup.py tests/test_project_memory_skill.py tests/test_skill_creator_skill_principles.py` 通过；对应 `.venv/bin/ruff check` 通过；`python3 skills/skill-creator/scripts/quick_validate.py skills/document-templates` 通过；`git diff --check` 通过。
- 2026-07-05：Superpowers 状态旧口径和中心脚本活跃入口清理验证通过。新增/更新断言要求计划与 closeout report 显示 `10 / 10` 本地闭环、活跃入口不再推荐旧脚本命令、TDD loop 明确隔离 Test Writer / Implementer / Verifier / Reviewer。验证：`.venv/bin/pytest tests/test_sf_sp_010_documentation_navigation.py tests/test_execution_workflow_skills.py tests/test_project_memory_skill.py tests/test_pr_commit_workflow_rules.py` 通过 `22 passed`；对应 `.venv/bin/ruff check` 通过；`git diff --check` 通过。
- 2026-07-05：Superpowers `SF-SP-001` 到 `SF-SP-007` 提交前验证通过。`SF-SP-001/002/003/004` 已写入用户 `human_approved`，`SF-SP-005/006/007` 已有独立 review 和人工确认；本轮只暂存 Superpowers 相关 hunk，排除 PM、Stratix 和三间房删除。验证：`.venv/bin/pytest tests/test_project_memory_skill.py tests/test_superpowers_reference_migration.py tests/test_sf_sp_010_documentation_navigation.py tests/test_pr_commit_workflow_rules.py tests/test_independent_review_gate.py tests/test_writing_plans_skill.py tests/test_execution_workflow_skills.py tests/test_review_workflow_skills.py tests/test_verification_debugging_workflow_skills.py` 通过 `43 passed`；对应 `.venv/bin/ruff check` 通过；`SF-SP-001/002/003/004/005/006/007/010` work item ledger 与 `.factory/memory/review-ledger.jsonl` 逐行 JSONL 解析通过，计数 `4 + 7 + 7 + 7 + 10 + 9 + 7 + 8 + 45 = 104`；`git diff --cached --check` 通过。
- 2026-07-05：PM 需求生命周期页和首页需求实时跟踪表新增并验证通过。先加结构测试复现缺少 `.factory/pm/generated/requirements-lifecycle.html` 的红灯失败 `2 failed, 3 passed`；随后加严要求 `status-dashboard.html` 首页必须直接包含“需求实时跟踪表”和 `SF-SP-001..010` 页内锚点，红灯为 `1 failed, 4 passed`；补齐页面、PM 首页内嵌表、PM WBS、渲染规则和项目管理控制面文档后，`uv run pytest tests/test_project_management_control_plane.py` 通过 `5 passed`；`uv run ruff check tests/test_project_management_control_plane.py` 通过；`python3 skills/skill-creator/scripts/quick_validate.py skills/using-shanforge` 通过；Python HTML parser 解析 4 个生成页通过；`git diff --check` 通过。
- 2026-07-05：Superpowers 流程集成收尾校准将结构测试从“当前只剩 `SF-SP-010`”更新为“`SF-SP-010` 已提交为 `3b0e9a5`，整体计划进入 001-007 收尾闭环”。验证：`.venv/bin/pytest tests/test_sf_sp_010_documentation_navigation.py tests/test_superpowers_reference_migration.py` 通过 `8 passed`；`.venv/bin/ruff check tests/test_sf_sp_010_documentation_navigation.py tests/test_superpowers_reference_migration.py` 通过；`.factory/workitems/SF-SP-010/ledger.jsonl` 与 `.factory/memory/review-ledger.jsonl` JSONL 解析通过，计数 `8 + 29 = 37`；`git diff --check` 通过。
- 2026-07-05：继续 Superpowers 收尾，补 `SF-SP-001` ledger / coverage evidence / review brief，并把收尾测试更新为最新独立性硬门口径。验证：`.venv/bin/pytest tests/test_sf_sp_010_documentation_navigation.py tests/test_superpowers_reference_migration.py tests/test_pr_commit_workflow_rules.py tests/test_independent_review_gate.py` 通过 `18 passed`；对应 ruff 通过；`SF-SP-001`、`SF-SP-010` ledger 与 review-ledger JSONL 解析通过，计数 `1 + 8 + 29 = 38`；`git diff --check` 通过。
- 2026-07-05：`stratix-service` 二次生产化复测新增对照归因和 latest 工具链报告。红灯阶段新增断言后 `.venv/bin/pytest tests/test_stratix_service_skill.py` 曾失败，暴露 bug report 中文复现/期望修复字段缺失；修复后目标测试通过 `10 passed`。最终验证：`.venv/bin/pytest tests/test_stratix_service_skill.py` 通过；`.venv/bin/ruff check tests/test_stratix_service_skill.py` 通过；`python3 skills/skill-creator/scripts/quick_validate.py skills/stratix-service` 通过。生产化测试在 `/private/tmp` 用 `@stratix/create@latest` 官方对照项目复测当前 latest：`@stratix/create/core/database@1.1.0`、`@stratix/forge@1.1.2`；`pnpm test:run`、`build-manifest`、`openapi generate`、config validate/encrypt/decrypt 通过；`pnpm install` 初次被 pnpm 11 `esbuild` ignored-builds 阻断，`doctor` 因 forge 版本期望冲突失败，`pnpm build` 因 `operationId` 类型失败，`release gate` 因缺 security/audit 脚本失败，`stratix start` 因 `@stratix/core` exports 解析失败，运行时 `STRATIX_SENSITIVE_CONFIG` 注入无法验证，结论为 `blocked`。
- 2026-07-05：`SF-SP-010` 独立 review 反馈后加严导航与 stale 方案测试。新增链接目标存在性、Superpowers `## 17. 下一步` 当前状态和 doc-map PM 控制面映射断言后，目标测试 `.venv/bin/pytest tests/test_sf_sp_010_documentation_navigation.py tests/test_superpowers_reference_migration.py` 通过 `7 passed`；ruff 通过；SF-SP-010 ledger 与全局 review-ledger JSONL 解析通过，复审入档后为 `6 + 28 = 34` 条；`git diff --check` 通过。独立复审 reviewer 给出 `approved / 95`。
- 2026-07-05：`SF-SP-010` 新增文档、导航、memory 同步结构测试。红灯 `.venv/bin/pytest tests/test_sf_sp_010_documentation_navigation.py tests/test_superpowers_reference_migration.py` 为 `2 failed, 4 passed`，暴露根导航缺少 `memory-governance-implementation-plan.md`、开发过程索引缺少测试报告入口，以及 Superpowers 方案 / memory 仍停在 `SF-SP-009` 开发叙事。补齐导航、当前进展和 summary 后，同一命令通过 `6 passed`；`.venv/bin/ruff check tests/test_sf_sp_010_documentation_navigation.py tests/test_superpowers_reference_migration.py` 通过；SF-SP-010 ledger 与全局 review-ledger JSONL 解析通过；`git diff --check` 通过。
- 2026-07-05：`SF-SP-009` 新增黑盒流程 eval 结构测试。红灯 `.venv/bin/pytest tests/test_black_box_workflow_eval.py` 失败 `4 failed`，原因是缺少 `black-box-flow-eval.md`、`using-shanforge` 入口和计划状态；补齐 reference、入口和计划同步后，绿灯 `.venv/bin/pytest tests/test_black_box_workflow_eval.py` 通过 `4 passed`。`.venv/bin/ruff check tests/test_black_box_workflow_eval.py` 通过；`python3 skills/skill-creator/scripts/quick_validate.py skills/using-shanforge` 通过；邻近 workflow 回归 `.venv/bin/pytest tests/test_execution_workflow_skills.py tests/test_review_workflow_skills.py tests/test_verification_debugging_workflow_skills.py tests/test_pr_commit_workflow_rules.py tests/test_black_box_workflow_eval.py` 通过 `26 passed`；`git diff --check` 通过。
- 2026-07-05：`SF-SP-009` 独立 review 反馈后加严黑盒 eval 测试。新增评分公式、每场景 critical assertion 和证据字段完整性断言后，红灯 `.venv/bin/pytest tests/test_black_box_workflow_eval.py` 为 `3 failed, 3 passed`；补齐 reference 和正式计划口径后，目标测试通过 `6 passed`。邻近 workflow 回归通过 `28 passed`，ruff、`skills/using-shanforge` validator、SF-SP-009 ledger / review-ledger JSONL 和 `git diff --check` 均通过。
- 2026-07-05：`stratix-service` 上线前评估新增配置安全和 CLI 升级防线测试。先用 `.venv/bin/pytest tests/test_stratix_service_skill.py` 复现新增约束缺失：`2 failed, 5 passed`；补齐 `STRATIX_SENSITIVE_CONFIG` 配置安全门、项目内 `pnpm exec stratix`、禁用普通 env 回退和 2026-07-05 工具链实测风险记录后，定向测试通过 `7 passed`。`.venv/bin/ruff check tests/test_stratix_service_skill.py`、`python3 skills/skill-creator/scripts/quick_validate.py skills/stratix-service`、范围内 `git diff --check` 均通过。测试子任务在 `/private/tmp/shanforge-stratix-skill-test-20260705` 实测：`npx --yes @stratix/create@1.1.0 list/generate`、`pnpm exec stratix doctor`、默认测试、`build-manifest`、`config validate/encrypt/decrypt` 通过；`pnpm build` 因 `operationId` 类型失败，`release gate` 因缺少 security/audit 脚本失败，`openapi` 为 unknown command，`stratix start` 在 Node 24 下因 `createRequire(...).resolve('@stratix/core')` 与 exports 条件失败。
- 2026-07-05：为避免 `ready_for_review` 被误报为完成，已撤销中心脚本 gate 方案，改用 skill-native 收尾门。`tests/test_pr_commit_workflow_rules.py` 现在断言 `using-shanforge` / `gitcommitzh` 必须在完成声明、提交或关闭前重读最新 work item ledger 和 review ledger，并禁止重引入脚本 gate；根据独立范围检查反馈，又补充同一 `.factory/memory/` 文件混有其他任务条目时只能暂存当前任务 hunk，无法拆分时停止并拆成独立提交。验证：`.venv/bin/pytest tests/test_pr_commit_workflow_rules.py` 通过，`5 passed`；`.venv/bin/ruff check tests/test_pr_commit_workflow_rules.py` 通过；两个相关 skill validator 通过；主独立 review `approved / 94`，范围隔离复审 `approved / 94`。
- 2026-07-05：PM HTML 页面已补齐项目甘特图、项目任务看板、评审链路总览和人类可读 work item 详情页，并纳入最新 `SF-SP-008`。项目任务看板主列已固定为“未开始任务 / 正在进行 / 已完成 / 已经审批”。`tests/test_project_management_control_plane.py` 已新增断言，覆盖关键评审锚点、`accepted_for_sf-sp-008` 最新 gate、页面链接不得指向 `.md` / `.jsonl` 原文，以及跨 HTML 页面锚点可解析。验证：`.venv/bin/pytest tests/test_project_management_control_plane.py` 通过，`5 passed`；`.venv/bin/ruff check tests/test_project_management_control_plane.py` 通过；HTMLParser 解析 3 个生成页和模板通过；101 个 `href` 均指向 HTML 页面或有效锚点；任务范围 `git diff --check` 通过。
- 2026-07-05：同步用户确认后，补齐 `.factory/memory/review-ledger.jsonl` 中 `SF-SP-005/006/007` 的 `human_approved -> SF-SP-008` 状态链，并更新 `tests/test_independent_review_gate.py` 断言独立 review 与人工确认分离。复验 `.venv/bin/pytest tests/test_independent_review_gate.py tests/test_pr_commit_workflow_rules.py tests/test_execution_workflow_skills.py tests/test_review_workflow_skills.py tests/test_verification_debugging_workflow_skills.py tests/test_superpowers_reference_migration.py tests/test_writing_plans_skill.py` 通过，`32 passed`；对应 ruff 通过；SF-SP-005/006/007/008 ledger 与 review-ledger JSONL 解析通过；`git diff --check` 通过。
- 2026-07-05：`SF-SP-008` 新增 `tests/test_pr_commit_workflow_rules.py`，先复现 `gitcommitzh` 缺少 PR 闭环与提交前置检查、缺 `pr-closure-checklist.md`、流程总控缺提交门、正式计划未记录 `SF-SP-008` 规则，红测 `4 failed`；补齐后 `.venv/bin/pytest tests/test_pr_commit_workflow_rules.py` 通过，`4 passed`；联合 workflow 回归 `.venv/bin/pytest tests/test_pr_commit_workflow_rules.py tests/test_superpowers_reference_migration.py tests/test_execution_workflow_skills.py tests/test_review_workflow_skills.py tests/test_independent_review_gate.py tests/test_verification_debugging_workflow_skills.py tests/test_writing_plans_skill.py` 通过，`32 passed`；ruff 通过；`skills/gitcommitzh` 与 `skills/using-shanforge` validator 均通过；SF-SP-005/006/007/008 ledger 与 review-ledger JSONL 解析通过；`git diff --check` 通过。
- 2026-07-05：`SF-SP-005` / `SF-SP-006` 独立复审入档后，相关 workflow 回归 `.venv/bin/pytest tests/test_execution_workflow_skills.py tests/test_review_workflow_skills.py tests/test_independent_review_gate.py tests/test_superpowers_reference_migration.py tests/test_writing_plans_skill.py tests/test_verification_debugging_workflow_skills.py` 通过，`28 passed`；对应 `.venv/bin/ruff check` 通过；6 个相关 skill validator 均通过；SF-SP-005/006/007 ledger 与 `.factory/memory/review-ledger.jsonl` JSONL 解析通过；旧 review 路由/旧分支收尾 skill 残留扫描无业务文件命中；尾随空白扫描和 `git diff --check` 通过。
- `uv lock --check`
- `uv run pytest`
- `git diff --check`

## 新增验证方向

- REQ / MOD / API / TASK 编号一致
- Workflow DSL、ModelPolicy、Capability 和 AgentResponse schema 可校验
- 业务 App 不直接依赖基础设施 adapter
- 高风险执行必须经过 approval / sandbox
- Session ledger 是第一事实源，memory promotion 不能覆盖原始 evidence
- recall 只消费 accepted memory，并保留 source refs 与 diagnostics

## 最近验证结果
- 2026-07-05 13:21：PM HTML 页面测试已扩展并通过。验证范围包括 `status-dashboard.html`、`pm-details.html`、`workitems.html`、PM 渲染规则和 HTML 链接锚点，并覆盖 `SF-SP-008` 与项目状态四列看板；结果为 `5 passed`，ruff 通过，HTMLParser 与 101 个链接 / 锚点检查通过，任务范围 `git diff --check` 通过。
- 2026-07-05：`SF-SP-008` 新增 PR 闭环与提交规则结构测试。红测 `.venv/bin/pytest tests/test_pr_commit_workflow_rules.py` 先失败 `4 failed`；实现后通过 `4 passed`；联合 workflow 回归通过 `32 passed`；`.venv/bin/ruff check tests/test_pr_commit_workflow_rules.py tests/test_superpowers_reference_migration.py` 通过；`python3 skills/skill-creator/scripts/quick_validate.py skills/gitcommitzh` 与 `skills/using-shanforge` 均通过；SF-SP-005/006/007/008 ledger 与 `.factory/memory/review-ledger.jsonl` JSONL 解析通过；`git diff --check` 通过。
- 2026-07-05：修复 `SF-SP-005` / `SF-SP-006` 独立 review 反馈后，相关 workflow 回归 `.venv/bin/pytest tests/test_execution_workflow_skills.py tests/test_review_workflow_skills.py tests/test_independent_review_gate.py tests/test_superpowers_reference_migration.py tests/test_writing_plans_skill.py tests/test_verification_debugging_workflow_skills.py` 通过，`28 passed`；对应 `.venv/bin/ruff check` 通过；`using-shanforge`、`subagent-driven-development`、`executing-plans`、`writing-plans`、`requesting-code-review`、`receiving-code-review` validator 均通过；SF-SP-005/006/007 ledger 与 review-ledger JSONL 解析通过；`git diff --check` 通过。
- 2026-07-05：前序任务真实独立评审入档后，更新 `tests/test_independent_review_gate.py`，让硬门测试同时覆盖旧同线程审批 correction 和新真实独立 reviewer 入档路径。验证：`.venv/bin/pytest tests/test_independent_review_gate.py tests/test_review_workflow_skills.py tests/test_execution_workflow_skills.py tests/test_superpowers_reference_migration.py` 通过，`18 passed`；`.venv/bin/ruff check tests/test_independent_review_gate.py tests/test_review_workflow_skills.py tests/test_execution_workflow_skills.py tests/test_superpowers_reference_migration.py` 通过；SF-SP-005/006/007 ledger 与 `.factory/memory/review-ledger.jsonl` JSONL 解析通过；`git diff --check` 通过。
- 2026-07-05 12:20：PM HTML 状态页修正为“总览页 -> HTML 详情页”，不再直接链接 `.md` / `.jsonl` 源文件。新增 `.factory/pm/generated/pm-details.html` 和 `.factory/pm/generated/workitems.html`；`.venv/bin/pytest tests/test_project_management_control_plane.py` 通过，`5 passed`；`.venv/bin/ruff check tests/test_project_management_control_plane.py` 通过；HTMLParser 解析 3 个生成页和模板通过；54 个 `href` 均指向 HTML 渲染页或锚点；任务范围 `git diff --check` 通过。
- 2026-07-05 11:51：按需刷新 PM 状态页后，复跑 `.venv/bin/pytest tests/test_project_management_control_plane.py` 通过，`4 passed`；`.venv/bin/ruff check tests/test_project_management_control_plane.py` 通过；PM JSONL 解析通过；Python `HTMLParser` 解析 `.factory/pm/generated/status-dashboard.html` 通过；任务范围 `git diff --check` 通过。
- 2026-07-05：评审独立性硬门修复新增 `tests/test_independent_review_gate.py`。红测先复现 `4 failed, 1 passed`，定位到 `requesting-code-review` 仍允许同线程 fallback、rubric/templates 缺 reviewer 独立性元数据、方案缺硬门、SF-SP-007 correction 未直接失效 `pending_human_confirmation`。修复后 `.venv/bin/pytest tests/test_independent_review_gate.py` 通过，`5 passed`；联合 `.venv/bin/pytest tests/test_independent_review_gate.py tests/test_verification_debugging_workflow_skills.py tests/test_review_workflow_skills.py tests/test_execution_workflow_skills.py tests/test_writing_plans_skill.py tests/test_superpowers_reference_migration.py` 通过，`27 passed`；对应 `.venv/bin/ruff check` 通过；`requesting-code-review` skill validator 通过；JSON/JSONL 解析和 `git diff --check` 通过。`uv` 当前不在 PATH，本轮沿用 `.venv/bin/*`。
- 2026-07-05：新增项目管理控制面与人类 HTML 状态页后，新增 `tests/test_project_management_control_plane.py` 固定 Excel 不是事实源、HTML 只展示、`dashboard.md` 是默认管理摘要、PM JSONL 台账可解析、不新增独立 PM skill、状态页由 `using-shanforge` 按需渲染等约束。验证：`.venv/bin/pytest tests/test_project_management_control_plane.py` 通过，`4 passed`；`.venv/bin/ruff check tests/test_project_management_control_plane.py` 通过；`python3 skills/skill-creator/scripts/quick_validate.py skills/using-shanforge` 通过；`python3 -m json.tool .factory/project.json` 通过；`python3 -m json.tool .factory/memory/graph/traceability.json` 通过；逐行解析 `.factory/pm/risk-register.jsonl` 与 `.factory/pm/change-register.jsonl` 通过；Python `HTMLParser` 解析 `.factory/pm/generated/status-dashboard.html` 与模板通过；尾随空白扫描和 `git diff --check` 通过。
- 2026-07-05：`SF-SP-007` 新增 `tests/test_verification_debugging_workflow_skills.py`，先用 `.venv/bin/pytest tests/test_verification_debugging_workflow_skills.py` 复现验证与调试 gate 相关 skill、references、OpenAI 元数据和 TDD 融合 reference 缺失失败 `6 failed`；补齐 `verification-before-completion`、`systematic-debugging` 和 `tdd-workflow/references/tdd-debugging-verification-gate.md` 后，定向测试首次 `1 failed, 5 passed`，暴露 `systematic-debugging` 主文件缺少“条件等待”可见语义，修正后联合回归 `.venv/bin/pytest tests/test_verification_debugging_workflow_skills.py tests/test_review_workflow_skills.py tests/test_execution_workflow_skills.py tests/test_writing_plans_skill.py tests/test_superpowers_reference_migration.py` 通过，`22 passed`；联合 ruff 通过；`verification-before-completion`、`systematic-debugging`、`tdd-workflow` validator 均通过；SF-SP-006/SF-SP-007 ledger 与 review-ledger JSONL 解析通过；旧路由 / 旧路径短语扫描无匹配；`git diff --check` 通过。
- 2026-07-05：`SF-SP-006` 新增 `tests/test_review_workflow_skills.py`，先用 `.venv/bin/pytest tests/test_review_workflow_skills.py` 复现评审类 workflow skill 目录、references 和 OpenAI 元数据缺失失败 `5 failed`；补齐 `requesting-code-review` 与 `receiving-code-review` 后，定向测试通过 `5 passed`。随后联合回归 `.venv/bin/pytest tests/test_review_workflow_skills.py tests/test_execution_workflow_skills.py tests/test_writing_plans_skill.py tests/test_superpowers_reference_migration.py` 首次暴露 `requesting-code-review` 仍点名执行类 skill 的边界回退，修正触发条件后复跑通过 `16 passed`；联合 ruff 通过；`skills/requesting-code-review` 与 `skills/receiving-code-review` validator 通过；SF-SP-006 ledger 与 review-ledger JSONL 解析通过；旧路由短语扫描无匹配；`git diff --check` 通过。
- 2026-07-05：`SF-SP-004` iteration-2 将 `writing-plans/references` 完整中文化后，更新 `tests/test_writing_plans_skill.py` 覆盖中文模板字段，并禁止旧英文模板短语回退。验证：`.venv/bin/pytest tests/test_writing_plans_skill.py` 通过，`3 passed`；`.venv/bin/ruff check tests/test_writing_plans_skill.py` 通过；`python3 skills/skill-creator/scripts/quick_validate.py skills/writing-plans` 通过；旧英文模板短语扫描无匹配；`.venv/bin/pytest tests/test_writing_plans_skill.py tests/test_execution_workflow_skills.py tests/test_superpowers_reference_migration.py` 通过，`11 passed`；联合 ruff、尾随空白扫描和 `git diff --check` 通过。
- 2026-07-05：`SF-SP-005` iteration-2 流程边界修正后，更新 `tests/test_execution_workflow_skills.py`、`tests/test_writing_plans_skill.py`、`tests/test_superpowers_reference_migration.py`，覆盖 `using-shanforge` 作为流程总控 / CTO、工作 skill 不决定前置/后置/下一步 skill、执行类 skill 不再出现“与其他 skill 的关系”、不再硬编码 `requesting-code-review`、`verification-before-completion`、`receiving-code-review`、`gitcommitzh`、`REQUIRED NEXT SKILL` 等路由。`.venv/bin/pytest tests/test_execution_workflow_skills.py tests/test_writing_plans_skill.py tests/test_superpowers_reference_migration.py` 通过，`11 passed`；`.venv/bin/ruff check` 对上述 3 个测试通过；`python3 skills/skill-creator/scripts/quick_validate.py` 对 `using-shanforge`、`subagent-driven-development`、`executing-plans`、`writing-plans` 均通过；旧路由短语扫描无匹配；尾随空白和 `git diff --check` 通过。
- 2026-07-05：按用户反馈将 `brainstorming` 可视化伴侣路径说明改为正向保存规则，并更新 `tests/test_brainstorming_skill.py`。验证：`.venv/bin/pytest tests/test_brainstorming_skill.py` 通过，`5 passed`；`.venv/bin/ruff check tests/test_brainstorming_skill.py` 通过；`python3 skills/skill-creator/scripts/quick_validate.py skills/brainstorming` 通过；任务范围 `git diff --check` 通过。
- 2026-07-05：`SF-SP-005` 新增 `tests/test_execution_workflow_skills.py`，先用 `.venv/bin/pytest tests/test_execution_workflow_skills.py` 复现执行类 workflow skill 目录缺失失败 `4 failed`；补齐 `subagent-driven-development` 和 `executing-plans` 后，`.venv/bin/pytest tests/test_execution_workflow_skills.py` 通过，`4 passed`；`.venv/bin/ruff check tests/test_execution_workflow_skills.py` 通过；`.venv/bin/python skills/skill-creator/scripts/quick_validate.py skills/subagent-driven-development` 和 `skills/executing-plans` 均通过；`git diff --check` 通过。`uv` 当前不在 PATH，本轮沿用 `.venv/bin/*`。

- 2026-07-05：Superpowers 流程集成方案补齐记忆分层和人工确认门后，扩展 `tests/test_superpowers_reference_migration.py` 覆盖入口压缩层、主题摘要层、work item 执行层、ledger 审计层、正式文档层、`pending_human_confirmation`、`human_approved` 与 reviewer `approved` 分离，以及确认包输出要求。`.venv/bin/pytest tests/test_superpowers_reference_migration.py` 通过，`3 passed`；`.venv/bin/ruff check tests/test_superpowers_reference_migration.py` 通过；`awk '/[ \t]$/' ...` 扫描本轮文档、测试和 memory 文件未发现尾随空白。
- 2026-07-05：`SF-SP-004` 新增 `tests/test_writing_plans_skill.py`。第一次运行暴露测试自身不应依赖缺失的 `yaml` 模块，改成纯文本检查后，`.venv/bin/pytest tests/test_writing_plans_skill.py` 复现 `writing-plans` 目录缺失失败 `3 failed`；补齐 skill 后，`.venv/bin/pytest tests/test_writing_plans_skill.py` 通过，`3 passed`；`.venv/bin/ruff check tests/test_writing_plans_skill.py` 通过；`.venv/bin/python skills/skill-creator/scripts/quick_validate.py skills/writing-plans` 通过；`git diff --check` 通过。`uv` 当前不在 PATH，本轮沿用 `.venv/bin/*`。

- 2026-07-05：`SF-SP-003` 现有 skill references 迁移切片新增 `tests/test_superpowers_reference_migration.py`。先用 `.venv/bin/pytest tests/test_superpowers_reference_migration.py` 复现缺少 `prd-template.md` 和未记录 future workflow references 边界的失败；补齐 references 后，`.venv/bin/pytest tests/test_superpowers_reference_migration.py` 通过，`2 passed`；`.venv/bin/ruff check tests/test_superpowers_reference_migration.py` 通过；`.venv/bin/python skills/skill-creator/scripts/quick_validate.py skills/requirements-engineering`、`skills/document-templates`、`skills/tdd-workflow`、`skills/gitcommitzh` 均通过。`uv` 当前不在 PATH，本轮沿用 `.venv/bin/*`。

- 2026-07-05：`brainstorming` skill 流程评审修正后，更新 `tests/test_brainstorming_skill.py` 覆盖 shanforge 当前状态/work item 驱动、禁止旧 `docs/superpowers` 保存路径、禁止固定“最后只调用 writing-plans”、禁止默认展开读取项目背景文件清单、优先消费 `project-memory` 会话卡、reviewer 模板路径、`visual-companion.md` 中文化、`--workitem-id` 持久化目录和 Shanforge 浏览器标题。验证结果：`python3 skills/skill-creator/scripts/quick_validate.py skills/brainstorming` 通过；`.venv/bin/pytest tests/test_brainstorming_skill.py` 通过，`5 passed`；`.venv/bin/ruff check tests/test_brainstorming_skill.py` 通过；`bash -n skills/brainstorming/scripts/start-server.sh` 通过；`bash -n skills/brainstorming/scripts/stop-server.sh` 通过；bundled Node `node --check skills/brainstorming/scripts/server.cjs` 通过；`git diff --check -- <brainstorming scope>` 通过；`skills/brainstorming/scripts/start-server.sh --project-dir .` 按预期拒绝并要求 `--workitem-id`。

- 2026-07-05：新增并继续收紧 `tests/test_project_memory_skill.py`，覆盖 `project-memory` skill frontmatter、先复用已有会话卡和压缩记忆、同一 work item 新鲜会话卡不得重复读取源 summary、会话卡“未读 / 已排除上下文”、禁止默认读取阶段 `docs/` 长文、`factory-agent-session` 仅作为迁移来源、禁止中心脚本主控、references 会话启动/相关性判断/会话卡/current-state 更新清单、ledger 防重复规则和中文 OpenAI 元数据。当前 `.venv/bin/pytest tests/test_project_memory_skill.py tests/test_brainstorming_skill.py` 通过，`9 passed`；`.venv/bin/ruff check tests/test_project_memory_skill.py tests/test_brainstorming_skill.py` 通过；`python3 skills/skill-creator/scripts/quick_validate.py skills/project-memory` 与 `skills/brainstorming` 均通过。
- 2026-07-05：`skill-creator` 已新增通用 skill 编写/修改原则测试 `tests/test_skill_creator_skill_principles.py`，覆盖中文默认、短句保真、含义保留清单、作者/评审隔离、`ready_for_review`、独立 reviewer、`references/` 与 skill-scoped helper code 边界。`python3 skills/skill-creator/scripts/quick_validate.py skills/skill-creator` 通过；`.venv/bin/pytest tests/test_skill_creator_skill_principles.py` 通过，`3 passed`；`.venv/bin/ruff check tests/test_skill_creator_skill_principles.py` 通过；`git diff --check -- skills/skill-creator/SKILL.md tests/test_skill_creator_skill_principles.py` 通过。
- 2026-07-04：`stratix-nodejs-backend` 重命名为 `stratix-service` 后，新增 `tests/test_stratix_service_skill.py` 覆盖新 skill 名称、旧目录退役、`@stratix/create` / `@stratix/forge` 工具链分工、`obsync-root` 根质量命令、插件/preset 选择、移除 tasks preset、`STRATIX_SENSITIVE_CONFIG` 与 `STRATIX_ENCRYPTION_KEY` 规则，以及 `agents/openai.yaml` 新默认提示。当前 shell 中 `uv` 不在 PATH，改用仓库 `.venv` 执行验证：`.venv/bin/pytest tests/test_stratix_service_skill.py` 通过，`5 passed`；`.venv/bin/ruff check tests/test_stratix_service_skill.py` 通过；`python3 skills/skill-creator/scripts/quick_validate.py skills/stratix-service` 通过；任务范围 `git diff --check` 通过。
- 2026-07-04：`brainstorming` skill 中文化后，新增 `tests/test_brainstorming_skill.py` 覆盖中文触发描述、实施前批准硬门、按需可视化伴侣、子项目规范/计划/实施循环、`docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md` 输出路径、仅调用 `writing-plans` 以及 `agents/openai.yaml` 中文元数据。`python3 skills/skill-creator/scripts/quick_validate.py skills/brainstorming` 通过；`.venv/bin/pytest tests/test_brainstorming_skill.py` 通过，`2 passed`；`.venv/bin/ruff check tests/test_brainstorming_skill.py` 通过；`git diff --check` 通过。
- 2026-07-04：按用户要求退役 `writer-room`、`director-room`、`art-room` 本地 skill 后，同步删除 `tests/test_writer_room_skill.py`、`tests/test_director_room_skill.py`、`tests/test_art_room_skill.py`，避免已删除目录继续被结构测试要求存在。`uv` 当前不在 PATH，改用 `.venv/bin/pytest` 执行全量回归通过，`223 passed`；`git diff --check` 通过；`.venv/bin/ruff check` 仍因仓内既有 scripts/tests import 排序和超长行等问题失败，失败点不属于本轮删除改动。
- 2026-06-20：新增 `tests/test_bug_fix_root_cause_skill_rules.py`，固定 `tdd-workflow`、`ai-regression-testing`、`ai-first-engineering`、`python-uv-project` 中“修 Bug 必须先定位根因，禁止用未验证兜底替代修复”的规则；`.venv/bin/pytest tests/test_bug_fix_root_cause_skill_rules.py` 通过，`4 passed`；`.venv/bin/ruff check tests/test_bug_fix_root_cause_skill_rules.py` 通过；`python3 skills/skill-creator/scripts/quick_validate.py` 分别验证上述 4 个 skill 均通过。当前 shell 中 `uv` 不在 PATH，定向测试改用仓库既有 `.venv` 工具执行。
- 2026-06-14：新增 `browser-control` skill 后，执行 `python3 skills/skill-creator/scripts/quick_validate.py skills/browser-control` 通过，确认 skill frontmatter 与结构有效；`uv run pytest tests/test_browser_control_skill.py` 通过，`4 passed`，覆盖本地浏览器触发描述、`browser-use` 优先路由、启用后的 URL 打开示例、安全确认与输出契约；`uv run ruff check tests/test_browser_control_skill.py` 通过。
- 2026-06-07：修正 `art-room` 分集状态卡目录路由后，新增 `tests/test_art_room_skill.py` 覆盖 master card 与 episode state card 的路径分流规则，确认 `asset-manifest`、`art-image-prompts`、`asset-index` 和 `thread-plan.output_format_contracts` 都按 `asset_type + asset_subtype` 约束 canonical path；`python3 -m json.tool` 已验证相关 schema 可解析，`uv run pytest tests/test_art_room_skill.py` 通过，`13 passed`，`uv run pytest tests/test_ai_drama_production_skill_plan_doc.py` 通过，`8 passed`，`uv run ruff check tests/test_art_room_skill.py` 通过。
- 2026-06-04：将 `prompt-room` 合并进 `director-room` 后，新增/更新 `tests/test_director_room_skill.py` 覆盖统一 skill 的双语分镜结构、JSON context passing、prompt-engineering 角色卡、ComfyUI prompt schemas、双语 prompt 字段和全局 metadata；同时更新 `art-room` handoff 不再指向独立 `prompt-room`。`python3 skills/skill-creator/scripts/quick_validate.py skills/director-room` 与 `skills/art-room` 通过，`uv run pytest tests/test_writer_room_skill.py tests/test_director_room_skill.py tests/test_art_room_skill.py` 通过，`23 passed`，`uv run ruff check tests/test_writer_room_skill.py tests/test_director_room_skill.py tests/test_art_room_skill.py` 通过。
- 2026-06-03：将 `skills/prompt-room/` 链接到 Codex 全局 skills 后，执行 `python3 skills/skill-creator/scripts/quick_validate.py skills/prompt-room` 通过，确认本地 skill 结构有效。
- 2026-06-01：调整 `writer-room`、`director-room`、`art-room` 的目录契约到 `./project/{project-name}/` 与 `{episode-id}/` 分集目录；`python3 skills/skill-creator/scripts/quick_validate.py skills/writer-room`、`skills/director-room`、`skills/art-room` 均通过，`uv run pytest tests/test_writer_room_skill.py tests/test_director_room_skill.py tests/test_art_room_skill.py` 通过，`18 passed`，`uv run ruff check tests/test_writer_room_skill.py tests/test_director_room_skill.py tests/test_art_room_skill.py` 通过。
- 2026-06-01：新增 `tests/test_prompt_room_skill.py`，覆盖 `skills/prompt-room/` 的触发描述、共享项目根输入契约、固定 `prompts/comfyui-*` 与 `reports/comfyui-prompt-qc.md` 输出、7 个提示词工程角色任务卡、ComfyUI prompting/tuning reference、prompt/workflow/tuning schema，以及 ComfyUI render feedback 最小调优闭环；`uv run python skills/skill-creator/scripts/quick_validate.py skills/prompt-room` 通过，`uv run pytest tests/test_prompt_room_skill.py` 通过，`6 passed`，`uv run ruff check tests/test_prompt_room_skill.py` 通过。普通沙盒运行 validator 时被 uv cache 权限阻止，已按规则提升权限重跑通过。
- 2026-06-01：新增 `tests/test_art_room_skill.py`，覆盖 `skills/art-room/` 的触发描述、`./project/{project-name}/` 输入契约、固定 `{episode-id}/art/` / `{episode-id}/prompts/art-image-prompts.json` / 项目级与单集级 `assets/` 输出、9 个美术角色任务卡、thread workflow reference、asset/prompt/thread schema，以及 `codex_app.create_thread/read_thread/send_message_to_thread` 的父协调器调用边界；`python3 skills/skill-creator/scripts/quick_validate.py skills/art-room` 通过，`uv run pytest tests/test_art_room_skill.py` 通过，`6 passed`，`uv run ruff check tests/test_art_room_skill.py` 通过。
- 2026-06-01：新增并扩充 `tests/test_director_room_skill.py`、`tests/test_writer_room_skill.py`，覆盖 `writer-room` 固定写入 `./project/{project-name}/` 与 `{episode-id}/` 分集目录、最终剧本统一为 `{episode-id}/script/final-script.md`、`director-room` 固定从项目根读取项目级 `bible/` 与单集 `script/`、`reports/` 标准文件、8 个 director role cards、references/schemas 资源存在性、generation method 枚举和 `agents/openai.yaml` 默认触发提示；同时断言 director-room skill 不硬链接上游具体 skill 名称。`python3 skills/skill-creator/scripts/quick_validate.py skills/director-room` 与 `python3 skills/skill-creator/scripts/quick_validate.py skills/writer-room` 通过，`uv run pytest tests/test_director_room_skill.py tests/test_writer_room_skill.py` 通过，`12 passed`，`uv run ruff check tests/test_director_room_skill.py tests/test_writer_room_skill.py` 通过。系统级 skill validator 因当前 Python 环境缺少 `yaml` 依赖未运行成功，已改用本仓 validator 完成校验。
- 2026-05-31：新增 `tests/test_writer_room_skill.py`，覆盖 `skills/writer-room/` 的 Codex-native 触发描述、11 个角色任务卡、references/templates/schemas 资源存在性和 agent handoff schema；`python3 skills/skill-creator/scripts/quick_validate.py skills/writer-room` 通过，`uv run pytest tests/test_writer_room_skill.py` 通过，`4 passed`，`uv run ruff check tests/test_writer_room_skill.py` 通过。系统级 skill validator 因当前 Python 环境缺少 `yaml` 依赖未运行成功，已改用本仓 validator 完成校验；另用一个 Codex worker 前向试跑 showrunner 单步，发现并修正“角色子 agent 不应要求 spawn_agent”的说明歧义。
- 2026-04-20：继续把 lifecycle governance 推到 provider-aware writeback：扩充 `tests/test_domain_memory_governance.py`、`tests/test_runtime_memory_provider_manager.py`、`tests/test_memory_governance_regression.py`、`tests/test_settings_profile_governance.py` 与 `tests/test_settings_layer_scaffold.py`，覆盖 `allow_lifecycle_writeback`、`on_lifecycle_apply(...)`、`remote_http` 专门 `lifecycle_apply` endpoint、以及 `apply_lifecycle()` 后的 session durable 保存与 preview explainability 回读；定向回归 `45 passed`，`python -m py_compile` 通过。
- 2026-04-20：继续把 lifecycle governance 推到 durable review queue：扩充 `tests/test_domain_memory_governance.py`、`tests/test_settings_memory_stores.py`、`tests/test_session_inspection_service.py`、`tests/test_composition_registry.py`、`tests/test_settings_layer_scaffold.py` 与 `tests/test_platform_scaffold.py`，覆盖 `update_lifecycle_queue(...)`、`MemoryLifecycleQueueRepositoryPort`、JSONL queue store roundtrip、跨 container `dismissed` review state 持久化，以及 queue item 在 `apply_lifecycle()` 后进入 `applied`；受影响主链回归 `61 passed`。
- 2026-04-20：继续把 lifecycle governance 推到 durable audit trail：扩充 `tests/test_domain_memory_governance.py`、`tests/test_session_inspection_service.py`、`tests/test_settings_memory_stores.py`、`tests/test_composition_registry.py`、`tests/test_settings_layer_scaffold.py` 与 `tests/test_platform_scaffold.py`，覆盖 `load_lifecycle_audit(...)`、`MemoryLifecycleAuditRepositoryPort`、JSONL audit store roundtrip、跨 container 审计轨迹回读，以及 `review_status_updated / lifecycle_applied` 两类审计事件；受影响主链回归仍为 `61 passed`。
- 2026-04-20：继续把 lifecycle governance 推到显式 review workflow：扩充 `tests/test_domain_memory_governance.py`、`tests/test_session_inspection_service.py`、`tests/test_platform_scaffold.py` 与 `tests/test_memory_governance_regression.py`，覆盖 `reopen_lifecycle_queue(...)`、`review_reopened`、`review_note_updated` 与 `lifecycle_audit_summary`；受影响主链回归仍为 `61 passed`。
- 2026-04-21：继续把 lifecycle governance 推到 queue 运维批量面：扩充 `tests/test_domain_memory_governance.py`、`tests/test_session_inspection_service.py`、`tests/test_memory_governance_regression.py` 与 `tests/test_platform_scaffold.py`，覆盖 `queue_filter` 驱动的 batch `dismiss / reopen`、缺少 selection source 的显式报错，以及 access/application/domain 对新签名的透传；当前定向回归 `38 passed`。
- 2026-04-21：继续把 lifecycle governance 推到 reviewer resolution taxonomy：扩充 `tests/test_session_inspection_service.py`、`tests/test_memory_governance_regression.py`、`tests/test_platform_scaffold.py` 与 `tests/test_settings_memory_stores.py`，覆盖 `update_lifecycle_queue(..., resolution=...)` 透传、`lifecycle_audit_summary.resolution_counts`、queue/audit JSONL round-trip，以及跨 container reopen 后 resolution 清空；本轮小范围回归 `20 passed`。
- 2026-04-21：继续把 lifecycle governance 推到更明确的 audit read model：扩充 `tests/test_domain_memory_governance.py` 与 `tests/test_memory_governance_regression.py`，覆盖 `latest_per_record_only`、`lifecycle_audit_summary.latest_entries` 的最新优先语义，以及 `latest_by_record` 投影；专项回归 `25 passed`。
- 2026-04-21：继续把 lifecycle governance 推到 reviewer guidance：扩充 `tests/test_domain_memory_governance.py`，覆盖 conflict/decay queue item 的 `resolution_required`、推荐 `resolution_options` 与建议说明；专项回归 `20 passed`。
- 2026-04-19：继续把 lifecycle governance 推到 queue/apply 闭环：新增 `MemoryLifecycleQueue*` 读模型，扩充 `MemoryGovernanceService`、`MemoryAPI`、`tests/test_session_inspection_service.py`、`tests/test_domain_memory_governance.py` 与 `tests/test_memory_governance_regression.py`，验证 `review_lifecycle / load_lifecycle_queue / apply_lifecycle(queue_filter)` 能经由 access/application/use case 落到 durable memory store 更新；专项回归 `18 passed`。
- 2026-04-19：继续完成 `MG-WP-004` 首轮生命周期治理回归：扩充 `tests/test_domain_memory_governance.py`，覆盖 conflict supersede、forced manual override 与 metadata-driven decay；同时让 `tests/test_memory_governance_regression.py` 通过 `explain_session_memory().lifecycle_evaluations` 验证 hidden memory 的 `conflict_superseded / decay_expired` 原因。记忆主链相关回归 `52 passed`。
- 2026-04-19：新增 `tests/test_memory_governance_regression.py`，将 `TC-013 ~ TC-016` 收口为独立记忆治理专项回归入口；同时补齐 `MemoryStatus.FORGOTTEN` 的最小 lifecycle 状态机，并扩充 `explain_session_memory()` 的治理解释输出。`uv run pytest tests/test_memory_governance_regression.py tests/test_domain_memory_governance.py` 通过，`10 passed`。
- 2026-04-19：继续扩充 `tests/test_runtime_memory_provider_manager.py` 与 `tests/test_runtime_memory_recall_pipeline.py`，覆盖 `MemoryProviderManagerPort` 改为消费 `MemoryProviderGovernanceDecision` 后 manager 仅执行已允许调用、以及 `RecallPlan.within_scope_order / overflow_order` 驱动 ranker 排序；相关专项回归 `13 passed`。
- 2026-04-19：围绕 `src/domain/memory/assembly_models.py`、`src/runtime/memory/provider_manager.py`、`src/runtime/memory/recall_ranker.py` 与 `src/domain/memory/service.py` 的本轮治理收口，记忆主链回归 `55 passed`，`uv run ruff check` 通过，`git diff --check` 通过。
- 2026-04-19：新增 `tests/test_domain_memory_governance.py`，覆盖 `RecallGovernancePolicy / MemoryProviderGovernancePolicy / MemoryLifecyclePolicy` 的默认决策形状，以及 `DefaultMemoryDomainService` 对 recall/provider 治理决策的消费；同时调整 `tests/test_runtime_memory_recall_pipeline.py`，把 `DefaultRecallPlanner` 回归切到 `RecallGovernanceDecision -> RecallPlan` 新入口；专项回归 `8 passed`。
- 2026-04-19：围绕 `src/domain/memory/governance.py`、`src/domain/memory/service.py`、`src/runtime/memory/recall_planner.py` 与相关测试文件，主链记忆回归 `48 passed`，`uv run ruff check` 通过，`git diff --check` 通过。
- 2026-04-18：退役 legacy `tests/test_memory_runtime.py`，并把现役覆盖迁入 `tests/test_domain_services.py`、`tests/test_runtime_memory_summarizer.py` 与 `tests/test_settings_memory_stores.py`；配套移除 `src/runtime/memory/runtime.py` 与 `src/runtime/memory/policy.py` 后，记忆专项回归继续验证 domain distill 幂等、缺证据拒绝、LLM summarizer 契约与 JSONL store round-trip。
- 2026-04-14：记忆初始专项回归通过，覆盖 evidence 投影、candidate promotion gate、context long-term memory segment 和跨 session recall；对应 legacy 测试文件已在 2026-04-18 退役并迁入当前主链测试。
- 2026-04-14：记忆主链相关静态检查已通过；当前代码基线已在后续重构中统一迁移到 `src/application/`、`src/runtime/`、`src/settings/` 新口径。
- 2026-04-14：新增验证点覆盖 summarizer 草案接入、`candidate -> decision` dataset 样本沉淀、以及 `JSONL-backed` store 的跨容器持久化 recall。
- 2026-04-14：新增验证点覆盖自定义 `MemoryPromotionPolicy` 拒绝低置信 declarative candidate，以及容器在 mock provider 下启用 `LLMMemorySummarizer`。
- 2026-04-14：新增验证点覆盖 `LLMMemorySummarizer` 对 invalid candidate schema 的拒绝，以及对模型 `kind/scope/confidence` override 的忽略。
- 2026-04-14：新增验证点覆盖同一 session repeated distill 的幂等性，以及容器按 settings 应用自定义 promotion policy。
- 2026-04-14：仓库全量 `uv run pytest` 通过，`105 passed`，记忆系统收口未引入跨模块回归。
- 2026-04-15：新增 `tests/test_infrastructure_scaffold.py`，覆盖 Hermes bridge config、Hermes-backed container switch 和 gateway round-trip；仓库全量 `uv run pytest` 通过，`109 passed`。
- 2026-04-16：新增 `tests/test_composition_container.py`、`tests/test_composition_loader.py`、`tests/test_composition_registry.py`、`tests/test_composition_resolver.py`，覆盖 `shanforge-di` 接入后的 business bindings、解析器 allowlist、生命周期与 Hermes adapter 解析；专项回归 `12 passed`。
- 2026-04-16：仓库全量 `uv run pytest` 通过，`134 passed`，未发现新的失败用例。
- 2026-04-16：`uv lock --check` 通过，确认引入 `shanforge-di` 后锁文件一致。
- 2026-04-16：`git diff --check` 通过，当前工作区无空白或补丁格式问题。
- 2026-04-16：新增 `tests/test_runtime_execution_bridges.py`，覆盖 `web_access`、`terminal`、`browser` 的首轮 local bridge、治理闸门和容器接线；文件内 `3` 个测试全部通过。
- 2026-04-16：清理 `scripts/factory_core.py` 中 `datetime.utcnow()` 弃用用法后，仓库全量 `uv run pytest` 再次通过，`137 passed`，warnings 归零。
- 2026-04-16：新增 `tests/test_session_inspection_service.py`，并在 `tests/test_domain_services.py` 补 `assembly_manifest` 断言，覆盖 session archive / transcript slice / assembly explain 查询面；仓库全量 `uv run pytest` 通过，`138 passed`。
- 2026-04-16：新增 `InMemory/JsonlSessionAssemblyStore`、扩充 `tests/test_platform_scaffold.py` 的 assembly snapshot 断言，并让 inspection 流优先读取专门 assembly store；仓库全量 `uv run pytest` 通过，`139 passed`。
- 2026-04-16：在 `tests/test_domain_services.py` 与 `tests/test_session_inspection_service.py` 补 `child_digests` 断言，覆盖 `SubAgentDigest` 进入 assembly manifest 和 inspection explain；仓库全量 `uv run pytest` 继续通过，`139 passed`。
- 2026-04-16：曾新增 `tests/test_access_cli.py` 覆盖注入式 CLI demo；该 demo 与测试已在 2026-07-06 按用户要求删除。
- 2026-04-16：新增 `tests/test_runtime_support_services.py`，覆盖 `ProfileSourceService`、`RuleSourceService`、`ClockIdentityService` 的 fallback/provider 行为；`tests/test_platform_scaffold.py` 新增 session assembly runtime support 接线断言；专项回归 `26 passed`。
- 2026-04-16：`src/runtime/` 已无 `NotImplementedError` scaffold；`env PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile ...` 覆盖新增 runtime/settings 模块通过；`uv run ruff check` 通过；仓库全量 `uv run pytest` 通过，`146 passed`。
- 2026-04-16：新增 `tests/test_settings_layer_scaffold.py`，覆盖 `src/settings/catalog.py`、`embedding/http/blob/search/vector` 骨架入口与稳定行为；`tests/test_composition_registry.py` 已补 skeleton families 断言；专项回归 `10 passed`。
- 2026-04-16：设置层骨架收口后，仓库全量 `uv run pytest` 通过，`149 passed`；针对新增 settings/test 文件的 `uv run ruff check` 通过；`git diff --check` 通过。
- 2026-04-16：新增 `tests/test_settings_profile_governance.py`，覆盖 workspace profile catalog、profile-specific rule bundle 与 profile 驱动的默认 provider/store 选择；专项回归 `18 passed`。
- 2026-04-16：profile 治理接线后，仓库全量 `uv run pytest` 通过，`152 passed`；针对 profile-governance 改动文件的 `uv run ruff check` 通过。
- 2026-04-16：扩充 `tests/test_domain_services.py`、`tests/test_session_inspection_service.py`、`tests/test_platform_scaffold.py` 与 `tests/test_settings_profile_governance.py`，覆盖 `SessionAssemblyManifest.selected_model / model_bindings / backend_bindings`、默认装配冻结和 step 级真实模型调用回写；专项回归 `19 passed`。
- 2026-04-16：`env PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile src/application/execution/service.py src/domain/session/assembly_models.py src/domain/memory/service.py src/runtime/capability/executor.py src/runtime/session_search/service.py src/settings/composition/container.py` 通过；针对 explainability 改动文件的 `uv run ruff check` 通过；`git diff --check` 通过。
- 2026-04-16：扩充 `tests/test_composition_container.py`、`tests/test_platform_scaffold.py` 与 `tests/test_domain_services.py`，覆盖 `capability_registry / approval_policy / delegation_transport` 进入 `backend_bindings`，以及 Hermes adapter `bridge_modules / fallback_class / contract_ready` metadata 进入 assembly explain；广义专项回归 `27 passed`。
- 2026-04-16：针对 Hermes adapter、container、domain 和记忆 explainability 相关文件的 `uv run ruff check` 通过；仓库全量 `uv run pytest` 再次通过，`152 passed`；`git diff --check` 通过。
- 2026-04-16：扩充 `tests/test_settings_profile_governance.py`，覆盖 workspace `backend-bindings.json`、profile-specific backend override、governance adapter 按 backend source 切换，以及 Hermes bridge 不可用时 `requested_binding_id` fallback explainability；专项回归 `6 passed`，相关容器/平台/领域回归 `16 passed`。
- 2026-04-16：针对 backend governance 改动文件的 `uv run ruff check` 通过；仓库全量 `uv run pytest` 通过，`155 passed`；`git diff --check` 通过。
- 2026-04-17：扩充 `tests/test_settings_profile_governance.py`，覆盖 workspace `provider-bindings.json`、profile-specific provider override、provider readiness fallback 与 `selected_model` 默认装配元数据保留；专项回归 `8 passed`，相关容器/平台/领域回归 `17 passed`。
- 2026-04-17：针对 provider manager 改动文件的 `uv run ruff check` 通过；仓库全量 `uv run pytest` 通过，`157 passed`；`git diff --check` 通过。
- 2026-04-17：新增 `tests/test_runtime_memory_provider_manager.py`，并扩充 `tests/test_domain_services.py`、`tests/test_settings_profile_governance.py`，覆盖 external `MemoryProviderPort + provider_manager` 的 single-provider lifecycle、sanitize/read-only recall fence、session-end writeback、child digest 默认无 shared provider write capability，以及容器把 `memory_provider_binding` 投影进 assembly explain；专项回归 `16 passed`。
- 2026-04-17：external memory provider 边界收口后，`uv run ruff check` 通过，`PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile ...` 通过，`git diff --check` 通过；仓库全量 `uv run pytest` 通过，`160 passed`。
- 2026-04-17：新增 `tests/test_runtime_memory_recall_pipeline.py`，并扩充 `tests/test_domain_services.py`、`tests/test_settings_profile_governance.py`，覆盖 `RecallPlannerPort / RecallRankerPort` 的预算分配、scope 排序、`scan_memory_records()` 分离，以及默认容器使用 `memory_provider:jsonl` 作为 durable external backend/source；专项回归 `17 passed`。
- 2026-04-17：针对 recall pipeline、memory provider、container 与相关测试文件的 `uv run ruff check` 通过；`PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile ...` 通过；`git diff --check` 通过；仓库全量 `uv run pytest` 通过，`163 passed`。
- 2026-04-17：扩充 `tests/test_domain_services.py`、`tests/test_session_inspection_service.py` 与 `tests/test_platform_scaffold.py`，覆盖独立 `preview_recall` 治理接口、`MemoryAPI` 聚合 session inspection + memory preview，以及默认容器的 preview 接线；专项回归 `17 passed`。
- 2026-04-17：针对 `preview_recall` 改动文件的 `uv run ruff check` 通过；`PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile ...` 通过；`git diff --check` 通过；仓库全量 `uv run pytest` 通过，`164 passed`。
- 2026-04-17：再次扩充 `tests/test_domain_services.py`、`tests/test_session_inspection_service.py` 与 `tests/test_platform_scaffold.py`，覆盖 `RecallPreview.scope_breakdowns`、`RecallPreview.record_rankings` 与 `RecallPreview.augmentation_preview`，验证 preview mode 会显式解释 per-scope budget、overflow 排序轨迹和 recall block provenance；专项回归 `17 passed`。
- 2026-04-17：针对 `preview_recall` explainability 增强文件的 `uv run ruff check` 通过；`PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile ...` 通过；`git diff --check` 通过；仓库全量 `uv run pytest` 通过，`164 passed`。
- 2026-04-17：扩充 `tests/test_runtime_memory_provider_manager.py` 与 `tests/test_settings_profile_governance.py`，覆盖 provider manager 合并 `contract_metadata + prefetch_diagnostics`、默认容器接入 `memory_provider:jsonl_vector`，以及 `preview_recall` 回读 `retrieval_kind / hit_count` 等 augmentation provenance；相关专项回归 `14 passed`。
- 2026-04-17：针对 `jsonl_vector` 和 provider provenance 改动文件的 `uv run ruff check` 通过；`PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile ...` 通过；`git diff --check` 通过；仓库全量 `uv run pytest` 通过，`166 passed`。
- 2026-04-17：扩充 `tests/test_composition_registry.py`、`tests/test_settings_layer_scaffold.py` 与 `tests/test_settings_profile_governance.py`，覆盖 `remote_http` 注册、`LocalHttpClientProvider` 的 `file://` transport，以及默认容器接入 `memory_provider:remote_http` 后 session/preview 都能回读 `endpoint_url + retrieval_kind=remote_http`；相关专项回归 `18 passed`。
- 2026-04-17：针对 `remote_http` bridge 改动文件的 `uv run ruff check` 通过；`PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile ...` 通过；`git diff --check` 通过；仓库全量 `uv run pytest` 通过，`167 passed`。
- 2026-04-17：再次扩充 `tests/test_settings_profile_governance.py`，覆盖 `backend_binding_metadata.metadata_file` 的 durable source、`signature_secret_env + signature_key_id` 的签名 auth，以及 `session_end_failure_policy=record` 下 `writeback_reports` 的失败回读；相关专项回归 `13 passed`。
- 2026-04-17：针对 `remote_http` durable source / signed auth / failure policy 改动文件的 `uv run ruff check`、`PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile ...` 与 `git diff --check` 通过；相关广义回归 `26 passed`，仓库全量 `uv run pytest` 通过，`170 passed`。
- 2026-04-17：再次扩充 `tests/test_settings_layer_scaffold.py` 与 `tests/test_settings_profile_governance.py`，覆盖 `signature_secret_file` 的 canonical `hmac-sha256` 签名串、`backend_binding_metadata.metadata_file` 对相对 `*_file` secret source 的解析，以及 `prefetch_response_validation=record` 下 `response_validation_error` 的 explainability 回读；相关广义回归 `27 passed`，针对 settings 相关专项回归 `17 passed`。
- 2026-04-17：针对 `remote_http` response validation / canonical signature / stable secret source 改动文件的 `uv run ruff check`、`PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile ...` 与 `git diff --check` 通过；仓库全量 `uv run pytest` 通过，`171 passed`。
- 2026-04-17：再次扩充 `tests/test_settings_profile_governance.py`，覆盖 `secret_catalog_file` 驱动的 `default_signature_key_id / signature_keys / default_bearer_token_id / bearer_tokens` key rotation，以及 `sync_response_validation=record` 下 `writeback_reports.sync.response_validation_error` 的失败回读；相关广义回归 `29 passed`，settings 相关专项回归 `19 passed`。
- 2026-04-17：针对 `remote_http` key rotation / writeback response validation / durable secret governance 改动文件的 `uv run ruff check`、`PYTHONPYCACHEPREFIX=/tmp/pycache python3 -m py_compile ...` 与 `git diff --check` 通过；仓库全量 `uv run pytest` 通过，`173 passed`。
- 2026-04-17：再次扩充 `tests/test_settings_profile_governance.py`，覆盖 `remote_http` 的内建 `remote_memory_prefetch_v1 / remote_memory_writeback_ack_v1` response contract 投影、`signature_key_selection_source / bearer_token_selection_source` secret rotation audit，以及 `writeback_reports.sync` 的稳定成功读模型（`response_ok / response_status / response_message / response_report_id`）；专项回归 `17 passed`。
- 2026-04-17：针对 `src/settings/memory/provider.py`、`src/settings/gateway/http_client.py`、`src/domain/memory/service.py` 与 `tests/test_settings_profile_governance.py` 的本轮治理收口，`uv run ruff check` 通过，`git diff --check` 通过，仓库全量 `uv run pytest` 再次通过，`174 passed`。
- 2026-04-17：扩充 `tests/test_settings_layer_scaffold.py`、`tests/test_composition_registry.py` 与 `tests/test_settings_profile_governance.py`，覆盖 `LocalSecretCatalogProvider` 的相对 `secret_catalog_file` 解析、metadata-only `signature_key_id` fallback、`secret_catalog` family/capability 注册，以及默认容器在 metadata 直配签名 key 的兼容路径；相关专项回归 `23 passed`。
- 2026-04-17：针对 `src/settings/workspace/secret_catalog.py`、`src/settings/composition/component_bindings.py`、`src/settings/memory/provider.py`、`src/settings/catalog.py` 与相关测试文件的本轮 durable secret provider 抽取，`uv run ruff check` 通过。
- 2026-04-17：扩充 `tests/test_settings_layer_scaffold.py`，直接覆盖 `RemoteHttpMetadataResolver` 对 canonical `bearer_token*`、legacy `auth_bearer_token*` alias fallback、`response_validation=strict -> raise` 与 `writeback_*` policy 解析的统一行为；同时保持 `tests/test_settings_profile_governance.py` 的默认容器远端 bridge 回归不变；相关专项回归 `24 passed`。
- 2026-04-17：针对 `src/settings/memory/remote_http_metadata.py`、`src/settings/memory/provider.py`、`src/settings/memory/__init__.py`、`src/settings/catalog.py` 与相关测试文件的本轮 remote_http metadata resolver 收口，`uv run ruff check` 通过。
- 2026-04-17：再次扩充 `tests/test_settings_layer_scaffold.py` 与 `tests/test_settings_profile_governance.py`，直接覆盖 `RemoteHttpMetadataResolver` 对 `recall_endpoint_url / recall_response_contract / sync_response_contract` 的 canonical 优先级，以及默认容器在 canonical endpoint/contract 键与 legacy alias 同时存在时会优先消费 canonical 口径；相关专项回归 `22 passed`。
- 2026-04-17：针对 `src/settings/memory/remote_http_metadata.py`、`src/settings/memory/provider.py`、设计文档与相关测试文件的本轮 endpoint/contract metadata 收口，`uv run ruff check` 通过，`git diff --check` 通过，仓库全量 `uv run pytest` 再次通过，`176 passed`。
- 2026-04-17：再次扩充 `tests/test_settings_layer_scaffold.py` 与 `tests/test_settings_profile_governance.py`，覆盖 `RemoteHttpRequestGovernance` 对 canonical `recall_response_validation / sync_response_validation / sync_failure_policy` 的优先级，以及默认容器在 canonical validation/failure-policy 键与 legacy alias 同时存在时会优先消费 canonical 口径；相关专项回归 `24 passed`。
- 2026-04-17：针对 `src/settings/memory/remote_http_metadata.py`、`src/settings/memory/provider.py`、`src/settings/memory/__init__.py`、设计文档与相关测试文件的本轮 request governance 收口，`uv run ruff check` 通过，`git diff --check` 通过，仓库全量 `uv run pytest` 再次通过，`178 passed`。
- 2026-04-17：再次扩充 `tests/test_settings_profile_governance.py`，覆盖默认容器在 `memory_provider:jsonl / jsonl_vector / remote_http` 下都会把 `query_terms / source_breakdown / result_truncated` 稳定投影到 `preview_recall().augmentation_preview.diagnostics`；相关专项回归 `20 passed`。
- 2026-04-17：针对 `src/settings/memory/provider.py`、`src/domain/memory/service.py` 与 `tests/test_settings_profile_governance.py` 的本轮跨 backend explainability 对齐，`uv run ruff check` 通过。
- 2026-04-17：在同步设计文档与 `.factory/memory/*` 后，`git diff --check` 通过，仓库全量 `uv run pytest` 再次通过，`178 passed`。
- 2026-04-17：再次扩充 `tests/test_settings_profile_governance.py`，覆盖默认容器在 `memory_provider:jsonl / jsonl_vector / remote_http` 下都会把 `budget_trace / rank_trace` 稳定投影到 `preview_recall().augmentation_preview.diagnostics`；相关专项回归 `20 passed`。
- 2026-04-17：针对 `src/settings/memory/provider.py`、`src/domain/memory/service.py` 与 `tests/test_settings_profile_governance.py` 的本轮 budget/rank trace explainability 对齐，`uv run ruff check` 通过。
- 2026-04-17：在同步设计文档与 `.factory/memory/*` 后，`git diff --check` 通过，仓库全量 `uv run pytest` 再次通过，`178 passed`。
- 2026-04-17：再次扩充 `tests/test_settings_profile_governance.py`，覆盖默认容器在 `memory_provider:jsonl / jsonl_vector / remote_http` 下都会把 `hit_provenance` 稳定投影到 `preview_recall().augmentation_preview.diagnostics`；相关专项回归 `20 passed`。
- 2026-04-17：针对 `src/settings/memory/provider.py`、`src/domain/memory/service.py` 与 `tests/test_settings_profile_governance.py` 的本轮 hit provenance explainability 对齐，`uv run ruff check` 通过。
- 2026-04-17：在同步设计文档与 `.factory/memory/*` 后，`git diff --check` 通过，仓库全量 `uv run pytest` 再次通过，`178 passed`。
- 2026-04-17：再次扩充 `tests/test_settings_profile_governance.py`，覆盖默认容器在 `memory_provider:jsonl / jsonl_vector / remote_http` 下都会把 `contract_trace / access_trace / writeback_trace` 稳定投影到 `preview_recall().augmentation_preview.diagnostics`；相关专项回归 `20 passed`。
- 2026-04-17：针对 `src/settings/memory/provider.py`、`src/domain/memory/service.py` 与 `tests/test_settings_profile_governance.py` 的本轮 contract/access/writeback trace explainability 对齐，`uv run ruff check` 通过。
- 2026-04-17：在同步设计文档与 `.factory/memory/*` 后，`git diff --check` 通过，仓库全量 `uv run pytest` 再次通过，`178 passed`。
- 2026-04-17：新增 `tests/test_runtime_memory_provider_manager.py` 的 trace-only provider 场景，并保持 `tests/test_settings_profile_governance.py` 的容器级 preview 回归；验证 `DefaultMemoryProviderManager` 现可从 `contract_trace / access_trace / writeback_trace` 统一回填 `bridge_kind / retrieval_kind / endpoint_url / response_contract / attempt_count / writeback_enabled / writeback_reports` 等 legacy 顶层字段；相关专项回归 `24 passed`。
- 2026-04-17：针对 `src/runtime/memory/provider_manager.py`、`src/settings/memory/provider.py`、`src/domain/memory/service.py`、`tests/test_runtime_memory_provider_manager.py` 与 `tests/test_settings_profile_governance.py` 的本轮 legacy alias 去重收口，`uv run ruff check` 通过。
- 2026-04-17：扩充 `tests/test_domain_services.py`，新增冻结 session/manifest 仅保留 legacy augmentation diagnostics 时 `preview_recall()` 仍会自动补出 `contract_trace / access_trace / writeback_trace` 的回放回归；并继续保持 `tests/test_runtime_memory_provider_manager.py` 与 `tests/test_settings_profile_governance.py` 的 trace/alias 兼容回归，相关专项回归 `30 passed`。
- 2026-04-17：针对 `src/domain/memory/augmentation_diagnostics.py`、`src/domain/memory/service.py`、`src/runtime/memory/provider_manager.py` 与相关测试文件的本轮 trace-first normalizer 收口，`uv run ruff check` 通过，`git diff --check` 通过，仓库全量 `uv run pytest` 通过 `180 passed`。
- 2026-04-17：再次扩充 `tests/test_domain_services.py`，新增 `prepare_session()` 把 `memory_provider_diagnostics` 与 `assembly_manifest.metadata.diagnostics` 压成 compact trace-first 口径、但 `preview_recall()` 仍能回填 `retrieval_kind / endpoint_url / response_contract / attempt_count / writeback_enabled / writeback_reports` 等 legacy 顶层键的回归；相关专项回归 `31 passed`。
- 2026-04-17：针对 `src/domain/memory/augmentation_diagnostics.py`、`src/domain/memory/service.py` 与相关测试文件的本轮 compact diagnostics 收口，`uv run ruff check` 通过。
- 2026-04-17：在同步设计文档与 `.factory/memory/*` 后，`git diff --check` 通过，仓库全量 `uv run pytest` 再次通过，`181 passed`。
- 2026-04-17：再次扩充 `tests/test_domain_services.py` 与 `tests/test_settings_profile_governance.py`，把 `preview_recall().augmentation_preview.diagnostics` 的旧顶层兼容键改为断言 `legacy_aliases` 子读模型，覆盖 `snapshot / vector / remote_http` 三类 provider 的 `retrieval_kind / endpoint_url / response_contract / writeback_reports` 等兼容字段下沉；相关专项回归 `31 passed`。
- 2026-04-17：针对 `src/domain/memory/augmentation_diagnostics.py`、`src/domain/memory/service.py` 与相关测试文件的本轮 preview diagnostics 形状收口，`uv run ruff check` 通过。
- 2026-04-17：再次扩充 `tests/test_domain_services.py` 与 `tests/test_settings_profile_governance.py`，验证 `legacy_aliases` 进一步收缩后，本地 `jsonl / jsonl_vector` preview 在没有非空兼容字段时不再暴露该子对象，而 `remote_http` 仍保留 `endpoint_url / response_contract / writeback_reports` 这类远端迁移兼容字段；相关专项回归 `31 passed`。
- 2026-04-17：再次扩充 `tests/test_domain_services.py` 与 `tests/test_settings_profile_governance.py`，把 `preview_recall().augmentation_preview.diagnostics` 的最后一层远端兼容壳也切到 `contract_trace / access_trace / writeback_trace`，验证 `snapshot / vector / remote_http` 三类 provider 的 preview 现均不再输出 `legacy_aliases`；相关专项回归 `27 passed`。
- 2026-04-17：针对 `src/domain/memory/augmentation_diagnostics.py`、`tests/test_domain_services.py` 与 `tests/test_settings_profile_governance.py` 的本轮 preview canonical-only 收口，`uv run ruff check` 通过，仓库全量 `uv run pytest` 通过 `181 passed`，`git diff --check` 通过。
- 2026-04-17：再次扩充 `tests/test_domain_services.py`、`tests/test_runtime_memory_provider_manager.py` 与 `tests/test_settings_profile_governance.py`，覆盖旧存量 diagnostics 中 `signature_key_id / bearer_token_id / timeout_seconds / max_retries / response_validation_error` 的 trace 回放归并，以及 `remote_http` 在 preview 侧把 transport auth、retry/timeout、prefetch validation、writeback `failure_policies / response_validation_errors` 摘要统一回收到 `access_trace / contract_trace / writeback_trace`；相关专项回归 `31 passed`。
- 2026-04-17：再次扩充 `tests/test_domain_services.py`、`tests/test_runtime_memory_provider_manager.py` 与 `tests/test_settings_profile_governance.py`，覆盖旧存量 diagnostics 中 `hit_count / hit_ids / query_text_present` 会在 normalize 阶段继续并回 `budget_trace.selected_hit_count / selected_hit_ids / query_text_present`，并验证 `prepare_session()` 落盘与 `preview_recall()` 输出都不再平铺这些顶层命中摘要；相关专项回归 `31 passed`。
- 2026-04-17：针对 `src/domain/memory/augmentation_diagnostics.py`、`src/settings/memory/provider.py` 与相关测试文件的本轮 `budget_trace` explainability 收口，`uv run ruff check` 通过，仓库全量 `uv run pytest` 通过 `181 passed`，`git diff --check` 通过。
- 2026-04-17：再次扩充 `tests/test_runtime_memory_provider_manager.py`，验证 `DefaultMemoryProviderManager.start_session()` 现会直接返回 compact canonical diagnostics，不再平铺 `bridge_kind / retrieval_kind / hit_count / endpoint_url / writeback_reports` 这类 legacy 顶层键；同时继续保持 `tests/test_domain_services.py` 与 `tests/test_settings_profile_governance.py` 的 legacy 输入回放兼容，相关专项回归 `31 passed`。
- 2026-04-18：再次扩充 `tests/test_domain_services.py`，新增 `project_stored_augmentation_diagnostics()` 直测，验证 stored replay 路径会统一过滤未知键、兼容 legacy 顶层输入，并输出 compact canonical diagnostics；同时继续保持 `tests/test_runtime_memory_provider_manager.py` 与 `tests/test_settings_profile_governance.py` 的 replay/preview 回归，相关专项回归 `32 passed`。
- 2026-04-18：针对 `src/domain/memory/augmentation_diagnostics.py`、`src/domain/memory/service.py` 与相关测试文件的本轮 stored replay 收口，`uv run ruff check` 通过，仓库全量 `uv run pytest` 通过 `182 passed`，`git diff --check` 通过。
- 2026-04-18：再次扩充 `tests/test_domain_services.py`，验证 stored replay 在 legacy manifest 不再提供 `bridge_kind / provider_kind / retrieval_kind` 顶层键时，仍可从 `memory_provider_binding.provider_id` 推回 `contract_trace` 默认值；相关专项回归 `32 passed`。
- 2026-04-18：再次扩充 `tests/test_domain_services.py`，验证 stored replay 在 legacy manifest 不再提供 `response_contract / response_contract_source` 顶层键时，仍可从 `memory_provider_binding.provider_id=remote_http` 推回预取 contract 默认值；相关专项回归 `32 passed`。
- 2026-04-18：再次扩充 `tests/test_domain_services.py`，验证 stored replay 在 legacy manifest 不再提供顶层 `endpoint_url` 时，仍可从 `memory_provider_binding.metadata.recall_endpoint_url` 恢复 `access_trace.access_ref`；相关专项回归 `32 passed`。
- 2026-04-18：再次扩充 `tests/test_domain_services.py` 与 `tests/test_settings_profile_governance.py`，验证 `writeback_trace` 现会稳定暴露 `successes / response_oks / response_statuses / response_messages / response_report_ids` outcome summary，并且 stored replay 可从 legacy 顶层 `writeback_reports` 与旧的 canonical `writeback_trace.reports` 自动补齐这些摘要；相关专项回归 `28 passed`。
- 2026-04-18：再次扩充 `tests/test_settings_profile_governance.py`，验证本地 `jsonl / jsonl_vector` 的 `writeback_trace` 在没有实际写回明细时不再输出空 `reports`，只保留 `supported / configured / session_writable / enabled` 等 canonical 字段；相关专项回归 `32 passed`。
- 2026-04-18：针对 `src/domain/memory/augmentation_diagnostics.py` 与 `tests/test_settings_profile_governance.py` 的本轮 writeback detail-only 收口，`uv run ruff check` 通过，仓库全量 `uv run pytest` 通过 `182 passed`，`git diff --check` 通过。
- 2026-04-18：再次扩充 `tests/test_domain_services.py`、`tests/test_runtime_memory_provider_manager.py` 与 `tests/test_settings_profile_governance.py`，把 canonical drill-down 字段从 `writeback_trace.reports` 切到 `writeback_trace.detail_reports`，并保持旧的 `writeback_trace.reports` 仅作为 replay/normalize 输入兼容；相关专项回归 `34 passed`。
- 2026-04-18：针对 `src/domain/memory/augmentation_diagnostics.py`、`src/settings/memory/provider.py` 与相关测试文件的本轮 `detail_reports` canonical 收口，`uv run ruff check` 通过，当前工作区因 `tests/test_memory_runtime.py` 处于删除态，仓库全量 `uv run pytest` 实际 collect 并通过 `178 passed`，`git diff --check` 通过。
- 2026-04-18：针对 `src/domain/memory/augmentation_diagnostics.py`、`src/settings/memory/provider.py` 与相关测试文件的本轮 writeback diagnostics 收口，`uv run ruff check` 通过，仓库全量 `uv run pytest` 通过 `182 passed`，`git diff --check` 通过。
- 2026-07-19：`TASK-QUALITY-001-P001` 最终全仓门：Ruff `33 -> 0`；mypy `73 -> 0`，`242 source files`；Ruff formatter 首次发现 67 文件债务并机械闭合，最终 `302 files already formatted`；pytest `817 passed / 0 failed / 0 skipped / 0 not_run`；`git diff --check` 通过。R001 builder `--verify-only` 通过，304 artifacts/27 governance inputs 匹配，12/12 隔离漂移攻击被拒绝。
- 2026-07-19：R001 撤销后的 `TASK-QUALITY-002-P001` 最终门：runtime/settings Skill 目录不存在，顶层保留 38 个 `SKILL.md`；Ruff 通过，format `296/296`，mypy `0/236`，pytest `817/817`，`git diff --check` 通过，stale memory 和 runtime Skill 管理符号扫描无命中。独立复审 `approved / 100 / 0-0-0`。
- 2026-07-19：`TASK-SKILL-001-P001` 新增项目位置/连续执行正反契约。RED `6 failed / 1 passed`，GREEN `7 passed`；旧独立 review Gate 消费者同步后定向 `12 passed`、相邻 `40 passed`、全仓 `824 passed`。首次全仓真实发现并修复 Ruff I001 和 1 条过期授权断言。
- 2026-07-19：TASK-SKILL-001 评审整改后保持 Ruff 0、format 297、mypy 0/236、pytest 824/824、diff/JSONL 通过；I-001/I-002/M-001 全部关闭，最终独立 review `approved / 100 / 0-0-0`。
- 2026-07-20：TASK-SKILL-003 初始 RED `9 failed / 19 passed`、GREEN `28 passed`；两轮评审整改后目标与相邻回归 `51 passed`，fresh-context `fast-path smoke` `22/22`，逐断言均精确 `2/2`。最终 Skill validator、Ruff、format `311/311`、mypy `0/245`、JSONL 与 diff check 通过；全仓 pytest `961 passed / 3 failed`，仅失败于范围外 R002 `expected 299, got 311`。
- 2026-07-21：TASK-SKILL-004 初始 owner RED `5 failed / 3 passed`、首轮 GREEN `8 passed`；review `I-001` 原样透传测试 `1 failed -> 1 passed`，最终 owner `9 passed`、Skill 相邻 `141 passed`、流程相邻 `30 passed`、black-box `10/10`、33/33 Skill valid、目标 Ruff/format、mypy `0/253`、JSONL/diff check 通过。全仓 pytest `1143 passed / 3 failed`，仅范围外 R002 `expected 299, got 323`；全仓 Ruff/format 的既有 552 项/56 文件债务不归本任务。
- 2026-07-23：TASK-IMPLEMENT-003 最终候选定向 `87 passed`，extractor + renderer `19 passed`，文档/路由 `15 passed`；Ruff、40 文件 format、mypy `0/279`、diff/ignore 检查通过。Chromium 四主视口与代码详情桌面/手机 `1 passed`、8 张截图；axe 7 页 `0 violation`。全仓 `1322 passed / 3 failed`，失败仅属于范围外 `ui-ux-pro-max` / `writing-plans` 既有改动。
- 2026-07-23：PK-SOURCE-MIGRATION-001-T04 初始新增行为 `4 failed`，合并优先级补充 `1 failed`；首轮修复后项目知识回归 `64 passed`。独立评审整改新增英文/编号简报、138 份真实语料和非推测空态测试，红灯 `3 failed` 后转绿；父工作项限定身份补充 SQLite 单实体合并回归后目标测试 `69 passed`、Mypy `0/279`、限定 Ruff 通过，固定 CLI 全量增量刷新成功，Chrome 桌面/390px 移动端控制台错误为 0。
- 2026-07-23：`PROJECT-ARTIFACTS-001` T03/T04 联合回归 `89 passed`，Renderer 回归 `21 passed`；三条固定资产 CLI 均返回 `valid=true`，桌面和 390px 移动端静态页面无横向溢出、控制台错误为 0。最终干净候选验证见本工作项 completion evidence。
- 2026-07-27：`TASK-WORKFLOW-SEMANTICS-001` 新鲜关闭验证为 `50 passed`，同范围 Ruff、JSONL 和限定 diff check 通过。
- 2026-07-27：`SKILL-FLOW-AUDIT-001` 初次为 `37 passed`，最终回读为 `36 passed / 1 failed`；失败来自未冻结的 `stratix-service` 并行改动，关闭门当前不通过。
- 2026-07-27：`PK-SOURCE-MIGRATION-001-T04` 最终回归真实结果为 `61 passed / 1 failed`；失败节点发现 4 份新登记任务简报未投影任一正式任务语义。未把后续 Ruff、Mypy、文档发布或快照刷新写成通过。
- 2026-07-27：`PM-DASHBOARD-003-T01` 静态合同通过；Chrome 关闭回归在 390px 与 1440px 下分别为 `scrollWidth=viewport`，十要素、五 lane、五详情页签和抽屉均通过，控制台错误为 0。
- 2026-07-27：`PK-SOURCE-MIGRATION-001-T04-SCHEMA-REPAIR` 初始 RED
  `3 failed / 1 passed` 转 GREEN `4 passed`；评审 I1 身份元数据负例再由
  `1 failed` 转 GREEN，最终 Task 身份负例与 Task 章节正例 `2 passed`。
  项目知识五文件最终 `67 passed`，
  Ruff、Mypy 290 source、docs-stratego、固定快照通过，Chrome 5 页×2 视口
  共 10/10 无横向溢出或控制台错误。
- 2026-07-27：PK T04 正式化后新鲜复验为五文件 `67 passed`、Ruff format/lint、
  Mypy 290、docs-stratego 全绿；最终快照
  `generation:8f6481166c22bf392f45e63ae3b9e836098780770e0ae8a4aeff3069108f78b3`
  成功，Chrome 10/10 通过。
- 2026-07-27：SKILL Iteration 6 隔离关闭门拆分后收集
  `test_skill_flow_process_audit.py` 共 8 个节点；冻结的 9 个候选/共享合同节点
  最终关闭复验 `9 passed`，Ruff format/lint 通过，候选 SHA-256 为 9/9，
  WorkItem/review ledger JSONL 与限定 diff-check 通过。
- 2026-07-28：`PM-DASHBOARD-004-T02` 根因测试从 `4 failed / 28 passed`
  转为 `32 passed`；中文冒号任务身份、P0/P1/P2 严格校验、PRD v4.2.0
  新需求和强关系均通过。T02 Python 范围 Ruff format/lint、关系 JSON 和限定
  diff check 通过；尚未执行 T03 看板测试或最终全仓回归。
- 2026-07-28：T02 评审 `T02-I1` fenced-code 污染反例从 `1 failed` 转为
  `1 passed`；T02 定向回归更新为 `33 passed`，extractor 与测试 Ruff
  format/lint、限定 diff check 通过。
- 2026-07-28：`PM-DASHBOARD-004-T03` renderer 初始合同 `4 failed`，SQLite
  合并根因 `1 failed`，首轮联合 Green `41 passed`；独立评审三项反例
  `3 failed → 3 passed`，最终索引与 renderer 联合 `43 passed`，Ruff
  format/lint 和限定 diff check 通过。
- 2026-07-28：`PM-DASHBOARD-004-T04` 质量与文档阅读合同 `4 failed` 转 Green；
  基础/资产 renderer `31 passed`，连同 SQLite 投影相邻回归 `50 passed`，
  Ruff format/lint、限定 diff check和真实 snapshot 通过。
- 2026-07-29：`SKILL-FIRST-PM-001-T01` 快照行为测试 `3 passed`；Ruff、
  format、Mypy、依赖锁、边界扫描和 diff check 通过。Shanforge / ITA Club
  390×844、1440×900 共 4/4 浏览器验收通过，无横向溢出或控制台错误，键盘跳转与
  折叠区通过。全仓 `212 passed / 7 failed`，失败属于已登记的范围外事实或测试漂移。
- 2026-08-23：`TEST-GOVERNANCE-001` Red 为 `4 failed / 9 passed`；实现提交 `c4534ba` 的干净克隆最终为 `236 passed + 4 subtests`，Ruff、两个 Skill validator、25 JSON、36 JSONL、Git 状态和 diff check 全绿，失败/错误/跳过/未运行均为 0。
- 2026-08-23：`TEST-GOVERNANCE-CLOSURE-001` 初始 Red `6 failed / 9 passed`，Review 整改 Red `3 failed / 12 passed / 1 deselected`；正式发布后治理专项 `16 passed`，实现提交 `ca436c9` 干净克隆 `246 passed / 4 subtests passed`，Ruff、案例/报告校验、两个 Skill validator、44 JSON、38 JSONL、diff 与 Git clean 全绿。
- 2026-08-24：`MODEL-DISPATCH-RUNTIME-001` 首轮基线 Red `5 failed / 1 passed`、review 整改基线 Red `8 failed / 1 passed`，模型路由 Green `9 passed`；实现提交 `b270ae4` 干净克隆完整 pytest `273 passed`，Ruff、38/38 Skill validator、6 TOML、25 JSON、40 JSONL、diff 与 Git clean 全绿。
- 2026-09-04：`UI-DESIGN-MASTER-001` 最终全仓 `360 passed / 11 subtests passed`，Ruff、`ui-ux-pro-max`、`art-asset-pipeline`、`using-shanforge` 三个 Skill validator 和 diff check 全部通过；失败、错误、阻塞、跳过、未运行均为 0。
