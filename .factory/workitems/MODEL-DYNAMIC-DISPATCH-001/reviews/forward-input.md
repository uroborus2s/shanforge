# 独立前向试用输入

使用仓库当前 using-shanforge/SKILL.md 与 references/codex-tools.md，模拟父会话对以下子任务进行派发决策。只读试用；不创建任务、不运行 spawn、不修改文件。输出每个案例的 role、complexity、risk、reasoning_demand、selected model/effort、dispatch 或阻止理由、是否新 spawn，以及未确认项。不得以当前宿主模型直接填入全部案例。

除案例特注，项目身份、既有任务卡、授权读取/写入范围和 Gate 已完整，模型/强度与角色能力均已从当前宿主确认；没有外部或破坏性副作用。

1. 父会话是 Astra/Max。修改内部 CLI 一条已确认错误的提示文案，单个文件；已给正确文案及验证命令，失败可直接回滚。
2. 按已批准的内部数据结构和现有模式补普通列表筛选与测试，影响两个内部模块，不变更公共契约或权限，需求与验收明确。
3. 只读梳理陌生仓库三个模块的依赖并给出职责划分建议，模块职责需要判断；可与父会话编写验收项并行，不实施设计。
4. 调整跨进程任务的取消、重试和断线恢复，需处理重复执行与事务一致性；有授权的隔离测试库与既定接口约束。
5. 对一批常规内部工具文案和输入验证变更作独立只读 review，实现与验证已完成；不涉及权限、资金或并发。
6. 单个一致性故障已充分复现，有冲突事件序列、调用链、候选根因和多轮实验，仍有两种互斥解释；需要判断哪个解释成立。
7. 数量很大的明确文案更新可以拆成三个互不交叉的目录；父会话处于 Ultra，子任务内容均局部且规则确定。
8. 需要深度审查认证权限边界；当前宿主只提供 Luna/low、Terra/medium 和固定 Terra/high 只读 reviewer，没有 Astra 或 xhigh 可用。
9. 现有 worker 为 Terra/medium，连续两轮验证失败，发现问题来自此前没有覆盖的并发状态；父会话准备调用 followup_task 并只在消息中写“升到 xhigh”。
10. 调用计划写了 Astra/high，但省略 reasoning_effort，并使用 fork_turns=all。主会话配置刚好也是 Astra/high。
11. 想让固定 Terra/high 的 terra-reviewer role 执行 Astra/xhigh。磁盘上新建了 task-reviewer role，但本次宿主工具尚未列出它。
12. 用户只问“这条正则是什么意思”，当前无项目任务、不需要读仓库，不需要持久化任何内容。
