# MODEL-ROUTING-001-T02 实现摘要

## 结果

- 正式流程与 `using-shanforge` 统一保存七个模型路由字段。
- `writing-plans` 只复制 Sol 路由，task brief 提供固定字段；执行 skill 只消费已授权路由包。
- 用户指南说明三模型职责及“当前 Codex 宿主能力”边界。
- 新增一个静态合同测试文件，覆盖 owner、确定性映射、授权兜底、五类升级信号和能力声明。

## 有意未做

- 未增加模型 API、路由服务、数据库、动态评分器或新依赖；现有流程合同已足以承载该能力。
