# T02 最小范围增补评审

- reviewer：`/root/project_artifacts_plan_review`
- 状态：`approved`
- Critical / Important：0 / 0

真实索引重建暴露的七个旧需求 endpoint 缺失，允许仅在既有标题前增加同名
`sf:section-id`，不改正文、验收、版本或状态。`REQ-VIS-*` 表格引用替换为已有稳定
实体 `REQ-PKI-009`。该增补符合稳定 locator、强关系完整性和干净候选隔离策略。
