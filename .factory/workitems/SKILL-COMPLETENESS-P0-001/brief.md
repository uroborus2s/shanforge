# SKILL-COMPLETENESS-P0-001

## 目标

按 P0 顺序修复 Skill 发现、正式文档解析、美术候选资产生命周期、能力目录事实和最小行为守卫，消除已确认的完整性缺口。

## 验收标准

- Skill 同步只发现包含有效 `SKILL.md` 的一级目录。
- 复用型 Skill 优先通过目标项目的 `doc-map.md` 解析正式文档，四模块布局仅作为未登记项目的回退。
- 美术候选资产可跨会话等待人工选择，确认后只保留批准资产进入交付包。
- 当前能力事实由 Skill 文件系统派生，正式设计和默认配置不再保留已确认的旧路径或旧运行时表述。
- 每项根因修复都有最小可执行回归守卫，完整测试与静态检查通过。

## 非目标

- 不新增 Skill 注册表、平台运行时、依赖或中心校验服务。
- 不改写历史审计报告中的历史快照。
- 不覆盖 `TEST-GOVERNANCE-001` 的并发改动，不执行 push、PR、merge 或部署。

## 状态

`completed`
