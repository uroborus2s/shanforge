# ITA Coach 三页静态候选

## 已观察的输入

- 实际查看 `workbench-390.png`、`schedule-390.png`、`student-detail-390.png`：米白底、深绿文本与操作、浅绿状态、细灰绿边界；工作台已将紧急课次放在统计前。
- 只取 `data.js` 的三个页面最小 fixture 子集；日期明确标为样例日期 `2026-09-05`。未复制账号、令牌、支付参数或内部编号。
- 尝试打开 Linear 原作者文章的“Introducing the new Linear UI”展示图；文章正文可访问，但本执行会话的图像请求失败，未实际看图。因此它不是本候选的优秀案例视觉证据；没有下载或复用外部资产。

## 方向与转化

同品牌的 `operate + read` 方向：墨绿作为场上信息锚点，浅砂与黄绿色仅标记时间和下一步；不用外部图片、字体或素材。工作台按“紧急课次 → 下一节 → 跟进”排序；日程用连续时间线让时段关系一眼可见；学员详情先给训练档案、当前观察与下一次重点，权益降为次级摘要。保留 44px 操作尺寸、页面栈返回和小程序的单列阅读节奏。

反方向：不使用持续底栏、仪表盘式大统计、泛化商品卡或把真实文字烘焙成图片。

## 交互与边界

- 可点击：工作台到日程/王小雨详情，两个子页可返回；训练建议可展开/收起。日程行不做学员下钻，避免把少儿班或田中健错误映射为王小雨。
- 明确不可用：签到、改约、取消、完成、保存，按钮在静态候选中禁用；没有 API、权限、支付或业务写入。长期观察（教练可见）与最近复盘（对客户可见）分区呈现，未混用来源。
- 未验证：微信真机、安全区和真实 Taro 页面栈；没有外部视觉参考或素材。

## 截图 critic

390px 截图复核后整改并复核：

1. 首轮脚本空白：已定位为 `top` 与浏览器全局冲突；改为 `pageTop`，复核正文已渲染。
2. 日程把所有场次下钻到王小雨且总时长写为 5.5 小时：改为 4.5 小时，并禁用该范围外下钻。
3. 长期观察与最近复盘混在同一段：已按教练可见/对客户可见和来源分区；最近复盘显示 fixture 日期，长期观察未额外显示来源时间。

当前实际查看 workbench、schedule、student 的 390px 截图；自动化只证明内容、行为、脚本错误和溢出约束，不构成视觉或业务批准。候选仍需独立人工评审。

## 有反馈整改（集中一次）

- 工作台删除重复口号、圆环和整行泛化主按钮，改由 14:00、王小雨、截止 14:10、场地和候选禁用状态组织首屏；统计退为一行辅助信息。
- 日程改为连续时间轴，时间比课程标题更先进入扫描路径；三条公共限制说明合并到列表后，不为每行重复，也不添加学生下钻。
- 学员详情先给当前水平、长期观察和下一次重点；已安排课次降为单行，权益折叠为摘要。长期观察与最近复盘继续带各自的可见性和日期。

仍存在的不足：静态样板未验证微信真机字号/安全区，权益摘要展开后的长文案未覆盖真实多权益组合；这两项不以本次浏览器验证替代。
