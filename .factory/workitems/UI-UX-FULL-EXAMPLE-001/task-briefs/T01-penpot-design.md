# T01：Penpot 设计基线

- 交付物：六个 Penpot 页面、共享设计系统、四端关键流程、导出预览。
- UI：必需；遵守 Apple、Android、微信小程序、Web 和跨平台映射规范。
- 接口：使用当前 Penpot MCP 的 `execute_code`、`export_shape`。
- 测试：读取页面/画板结构；导出关键画板并视觉检查。
- 禁止：关闭或删除用户既有页面；当前空白 `Page 1` 只允许重命名。
- 完成状态：`ready_for_review`。
