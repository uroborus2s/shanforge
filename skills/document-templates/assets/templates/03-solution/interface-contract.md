# 接口契约文档

**项目名称：**  
**文档状态：** 草稿 | 评审中 | 已批准 | 已废弃  
**负责人：**  
**主要读者：** 前后端 | 集成方 | 测试 | 运维  
**上游输入：** PRD | 系统架构设计 | 模块边界文档  
**下游输出：** OpenAPI / AsyncAPI / Proto / Schema | 测试用例 | 集成实现  
**关联 ID：** `API-xxx`, `EVT-xxx`, `INT-xxx`, `REQ-xxx`, `MOD-xxx`  
**最后更新：** YYYY-MM-DD  

> 要求：人类说明文档与机器契约文件成对出现。本文解释语义、边界和约束；字段级结构放在配套契约文件中。

## 1. 接口目录

| 接口 ID | 类型 | 提供方 | 消费方 | 目的 | 契约文件 |
|---|---|---|---|---|---|
| `API-001` | REST |  |  |  | `contracts/api/openapi.yaml` |

## 2. 接口详情

### `API-001` 接口名称

- 类型：REST | GraphQL | gRPC | Event | Batch | Internal
- 业务目的：
- 提供方：
- 消费方：
- 触发场景：
- 版本策略：

输入：

- 请求/消息摘要：
- 关键字段：
- 校验规则：

输出：

- 响应/消息摘要：
- 关键字段：
- 一致性要求：

运行约束：

- 认证/授权：
- 限流：
- 超时：
- 幂等性：
- 重试策略：

错误处理：

| 场景 | 错误码/状态 | 调用方处理方式 |
|---|---|---|
|  |  |  |

示例：

- 请求样例：
- 响应样例：

兼容性说明：

- 向后兼容规则：
- 废弃字段策略：
- 变更通知机制：

## 3. 监控与运维要求

- 关键日志：
- 指标：
- 告警阈值：

## 4. 契约文件位置

- `contracts/api/openapi.yaml`
- `contracts/events/asyncapi.yaml`
- `contracts/internal/*.proto`
- `contracts/**/schemas/*.json`

## 5. 变更记录

| 日期 | 变更内容 | 变更人 |
|---|---|---|
| YYYY-MM-DD | 初始版本 |  |
