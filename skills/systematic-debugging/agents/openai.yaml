interface:
  display_name: "系统化调试"
  short_description: "在修复前先复现问题、定位根因、验证假设，再做最小修复。"
  default_prompt: "使用 $systematic-debugging：针对当前 bug 或失败先读取错误输出，复现问题，检查最近变化，追踪数据流并记录根因；修复前先建立失败测试或验收用例，修复后写入 evidence、report 和 ledger。"
