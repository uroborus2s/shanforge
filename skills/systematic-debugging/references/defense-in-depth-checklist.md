# 防御式校验清单

用于根因已明确后，让同类问题结构性不可复现。

## 层级

1. 入口校验：在 API、CLI、文件读取或用户输入边界拒绝明显非法数据。
2. 业务逻辑校验：在核心操作前验证数据满足业务契约。
3. 环境保护：在测试、沙箱、高风险路径中拒绝危险目录、危险命令或错误配置。
4. 诊断日志：在关键边界记录输入、输出、路径、配置和调用栈。

## 检查

- 坏数据从哪里进入。
- 数据经过哪些层。
- 哪些层可以绕过前置校验。
- 每一层是否有清晰错误信息。
- 测试是否覆盖绕过第一层后的保护。

## 结论

目标不是“这次修好了”，而是让问题结构性不可复现。
