# 人类可读状态正文合同

本合同保留三段式人类响应外壳：共享状态头和按工作类型正文都放在“处理结果”部分。先用业务语言说明结论；内部 ID、Gate、路径和命令只作为末尾技术记录，不替代正文。

## 共享状态头

项目化进度更新必须说明当前阶段、当前任务、已完成结果、剩余范围、阻塞和唯一下一动作。

- 开发 / 计划的事实源只能是已批准 WBS、TaskCard 和 ledger。
- 逐项区分已完成、进行中、未开始和阻塞；已完成项必须写可观察结果和验证状态。
- system、评审、提交等任务不得算作产品功能进度；可单列为治理或交付状态。
- 没有完整 WBS 或已批准分母时，明确写“无法计算”，不得猜测百分比或分母。
- 仅在已批准分母可得时，才汇总 WBS 项；剩余范围仍须逐项说明，不能只报百分比。

## 进度事实对账边界

所有展示的 WBS/产品进度变化必须先与已批准 WBS、TaskCard 和 ledger 对账。

| 输入事实 | 总控处理 | 对产品完成度的影响 |
|---|---|---|
| 已批准 WBS、TaskCard 和 ledger 可匹配 | 映射为对应 WBS 项状态 | 可推进该项产品完成度 |
| worker facts 无法匹配 | 仅作为本轮执行观察或技术记录 | 不得推进产品完成度 |

## 按工作类型正文

### 开发 / 计划

按 WBS 与 TaskCard 展示四态、每个已完成项的可观察结果和验证状态；说明当前实现或计划正在改变什么，以及哪些已批准项尚未开始或被阻塞。

### 测试

按完整测试基线与本轮报告说明覆盖范围、总数和通过、失败、错误、阻塞、跳过、未运行、取消；局部通过只能表述为局部通过。失败或错误写测试 ID、关联功能、可观察现象和当前归因；根因未知写“待调查”。

### Bug / 修复

说明现象、影响、复现、直接原因、根源原因、事实 owner、风险、修复状态和回归验证。每个修复位置再逐项给出 `file`、`symbol`、`change`、`reason` 和 `verification`；没有函数边界时写实际模块、配置项或文档章节，不得伪造函数名。根因未定位时只写已知事实与唯一诊断动作；是否创建 Bug TaskCard 由适用的修复任务卡决策合同决定。

### 评审 / 交付

说明评审或交付结论、发现和验证证据，以及对已批准产品范围的影响；不得把 review、verification、commit 或 system 状态写成新增产品功能或项目完成。

## 开发示例

### 处理结果

WBS 总数：5；已完成：2；进行中：1；未开始：1；阻塞：1。当前 TaskCard：SOFTWARE-ENGINEERING-SKILL-REMEDIATION-001-T04。已完成的可观察功能是工作 Skill 可回传本职事实，尚未完成范围是四类状态正文示例；T04 正在补齐它们，另有一项等待外部确认而阻塞。

### 验证与风险

验证：已运行定向合同测试，当前结果以本轮命令回执为准。风险：未完成和阻塞项不能计入产品完成度。

### 下一步

唯一下一动作：完成 T04 的定向验证；无需回复。

## 测试示例

### 处理结果

总数：8；passed：5；failed：1；error：1；blocked：0；skipped：1；not_run：0；cancelled：0。TEST-ID：TEST-CONTRACT-STATUS-002（failed）；关联功能：人类可读测试状态；现象：失败用例未列出关联功能；当前归因：测试报告缺少该字段。TEST-ID：TEST-CONTRACT-STATUS-003（error）；关联功能：错误用例事实；现象：测试执行中断，未生成归因字段；当前归因：待调查。

### 验证与风险

本轮覆盖状态正文合同；未覆盖范围：未运行全仓测试。失败和错误不能表述为整体通过。

### 下一步

唯一下一动作：补齐 TEST-CONTRACT-STATUS-002 的关联功能字段；无需回复。

## Bug 示例

### 处理结果

现象：状态回复只显示内部状态码。影响：用户无法判断开发进度和测试结论。复现：执行状态更新后阅读回复。直接原因：回复缺少面向用户的事实字段。根源原因：待调查。owner：状态回复合同。风险：错误进度判断。Bug TaskCard 决策：当前整改 TaskCard 已覆盖，不新建。

### 验证与风险

尚未验证根源原因；在根源原因明确前不得声称已修复。

### 下一步

唯一下一动作：调查状态回复遗漏事实字段的根源原因；无需回复。

## 修复示例

### 处理结果

file：skills/using-shanforge/references/human-readable-status.md；symbol：文档章节“测试”；change：补充七态计数和失败事实；reason：让用户能读懂测试结果；verification：定向合同测试通过。完成：测试正文事实已补齐；未完成：全仓回归尚未运行。回归结果：仅定向合同测试通过。

### 验证与风险

风险：定向通过不代表全仓通过；无函数边界时使用实际文档章节，未编造函数名。

### 下一步

唯一下一动作：运行受影响的回归测试；无需回复。

## 发布示例

### 处理结果

发布候选：`registry.example/app@sha256:abc123`；环境：production。发布状态：released；健康检查：passed；冒烟结果：passed；缺陷：无。file：skills/release-deployment/SKILL.md；symbol：文档章节“发布回执”；change：补充最小发布回执边界；reason：让发布结果可被总控合并；verification：定向合同测试通过。

### 验证与风险

健康检查和关键冒烟均通过。风险：本回执不替代后续观察窗口；未发现缺陷。

### 下一步

唯一下一动作：继续既有授权范围内的发布后观察；无需回复。

## 技术记录

正文之后可以附最小技术记录：相关内部 ID、Gate、路径、命令和原始状态。它只能支撑可追溯性，不能替代共享状态头、工作类型正文或用户可见的唯一下一动作。
