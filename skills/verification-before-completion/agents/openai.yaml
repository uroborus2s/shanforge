interface:
  display_name: "完成前验证"
  short_description: "在完成声明前运行新鲜验证证据，读取输出和 exit code，再报告真实状态。"
  default_prompt: "使用 $verification-before-completion：运行能证明声明的新鲜命令并读取输出、exit code 和失败数量。缺陷修复只跑目标回归；最终候选跑完整必需发布测试，并输出脱敏的紧凑报告。"
